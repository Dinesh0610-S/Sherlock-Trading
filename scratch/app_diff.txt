--- src/App.jsx.bak
+++ src/App.jsx
@@ -6,12 +6,15 @@
 import { useNSELiveData } from './hooks/useNSELiveData';
 import AssetSearch from './components/AssetSearch';
 import SherlockReply from './components/SherlockReply';
+import HolmesChat from './components/HolmesChat';
 import RRCalculator from './components/RRCalculator';
 import IVRankPanel from './components/IVRankPanel';
 import GreeksDisplay from './components/GreeksDisplay';
 import UnusualOIDetector from './components/UnusualOIDetector';
 import ExpectedMovePanel from './components/ExpectedMovePanel';
 import MultiExpiryComparison from './components/MultiExpiryComparison';
+import ClueBoardTab from './components/clueboard/ClueBoardTab';
+import InstitutionalPatternEnginePanel from './components/clueboard/InstitutionalPatternEnginePanel';
 import { Line, Bar } from 'react-chartjs-2';
 import { 
   BarChart as RechartsBarChart, 
@@ -65,8 +68,9 @@
   const radius      = 28;
   const circumference = 2 * Math.PI * radius;
   const filled      = (safeScore / 100) * circumference;
-  const color       = safeScore >= 85 ? '#00ff88' :
-                      safeScore >= 65 ? '#f5a623' : '#ff4444';
+  const color       = safeScore >= 85 ? '#00e5ff' : // conviction electric blue
+                      safeScore >= 70 ? '#00e676' : // green
+                      safeScore >= 40 ? '#ffab00' : '#ff1744'; // amber / red
 
   return (
     <div className="confidence-ring-wrapper">
@@ -266,677 +270,1641 @@
   </div>
 );
 
-// ── Pre-Market Intelligence Engine Component ─────────────────────────────────
-function PreMarketIntel() {
-  const [pmData, setPmData]         = React.useState(null);
-  const [pmEntry, setPmEntry]       = React.useState(null);
-  const [pmLoading, setPmLoading]   = React.useState(false);
-  const [pmEntryLoading, setPmEntryLoading] = React.useState(false);
-  const [pmError, setPmError]       = React.useState(null);
-  const [, setPmTick]               = React.useState(0);
-  const [pmSymbol, setPmSymbol]     = React.useState('NIFTY');
-  const [serverStatus, setServerStatus] = React.useState('CHECKING');
-  const [retryCount, setRetryCount] = React.useState(0);
-  const [niftyLive, setNiftyLive]   = React.useState(null);
-
-  const pmTimerRef                  = React.useRef(null);
-  const pmAutoRef                   = React.useRef(null);
-
-  const getIST = () => new Date(new Date().toLocaleString('en-US', { timeZone: 'Asia/Kolkata' }));
-
-  const getPhase = (ist) => {
-    const h = ist.getHours(), m = ist.getMinutes();
-    const hm = h * 60 + m;
-    if (hm < 9 * 60)         return 'BEFORE_PREOPEN';
-    if (hm < 9 * 60 + 8)     return 'ORDER_ENTRY';
-    if (hm < 9 * 60 + 12)    return 'IEP_CALCULATION';
-    if (hm < 9 * 60 + 15)    return 'BUFFER';
-    if (hm < 9 * 60 + 20)    return 'JUST_OPENED';
-    if (hm < 9 * 60 + 25)    return 'CONFIRM_WINDOW';
-    if (hm < 15 * 60 + 30)   return 'MARKET_OPEN';
-    return 'CLOSED';
+
+
+
+// ── 30-MIN STRATEGY ENGINE PATTERN DETECTION & DECISION ─────────────────────
+function detectPattern(candles, vwap) {
+  if (!candles || candles.length < 3) return [];
+  const [c1, c2, c3] = candles.slice(-3).reverse(); // latest first
+  const patterns = [];
+
+  // Bullish Engulfing
+  if (c2.close < c2.open && // prev was red
+      c1.open < c2.close && // current opens below prev close
+      c1.close > c2.open && // current closes above prev open
+      c1.close > c1.open) { // current is green
+    const volConfirm = c1.volume > c2.volume;
+    patterns.push({
+      name: 'Bullish Engulfing',
+      signal: 'CE',
+      strength: volConfirm ? 'HIGH' : 'MEDIUM',
+      description: `Strong reversal signal. Vol ${volConfirm ? 'confirms' : 'weak'}.`,
+      tf5mBullish: true
+    });
+  }
+
+  // Morning Star
+  const c3_bearish = c3.close < c3.open && (c3.open - c3.close) / c3.open >= 0.003;
+  const c2_doji = Math.abs(c2.close - c2.open) / Math.max(c2.open, c2.close) < 0.002;
+  const c1_bullish = c1.close > c1.open && (c1.close - c1.open) / c1.open >= 0.003;
+  if (c3_bearish && c2_doji && c1_bullish && c1.close > (c3.open + c3.close)/2) {
+    patterns.push({
+      name: 'Morning Star',
+      signal: 'CE',
+      strength: 'HIGH',
+      description: 'Morning Star reversal. Bulls reclaiming control.',
+      tf5mBullish: true
+    });
+  }
+
+  // Hammer
+  const body = Math.abs(c1.close - c1.open);
+  const lowerWick = Math.min(c1.open, c1.close) - c1.low;
+  const upperWick = c1.high - Math.max(c1.open, c1.close);
+  const range = c1.high - c1.low;
+  if (lowerWick >= 2 * body && upperWick < body * 0.5 && body / range < 0.35) {
+    patterns.push({
+      name: 'Hammer',
+      signal: 'CE',
+      strength: 'HIGH',
+      description: 'Rejection of lower prices. Strong hammer at support.',
+      tf5mBullish: true
+    });
+  }
+
+  // Bullish Harami
+  if (c2.close < c2.open && c1.close > c1.open &&
+      c1.open > c2.close && c1.close < c2.open) {
+    patterns.push({
+      name: 'Bullish Harami',
+      signal: 'CE',
+      strength: 'MEDIUM',
+      description: 'Inside bar bullish consolidation.',
+      tf5mBullish: true
+    });
+  }
+
+  // Three White Soldiers
+  if (c3.close > c3.open && c2.close > c2.open && c1.close > c1.open &&
+      c2.open > c3.open && c2.open < c3.close &&
+      c1.open > c2.open && c1.open < c2.close &&
+      c1.close > c2.close && c2.close > c3.close) {
+    patterns.push({
+      name: 'Three White Soldiers',
+      signal: 'CE',
+      strength: 'HIGH',
+      description: 'Strong bullish trend breakout.',
+      tf5mBullish: true
+    });
+  }
+
+  // Bearish Engulfing
+  if (c2.close > c2.open && // prev was green
+      c1.open > c2.close && // opens above prev close
+      c1.close < c2.open && // closes below prev open
+      c1.close < c1.open) { // current is red
+    const volConfirm = c1.volume > c2.volume;
+    patterns.push({
+      name: 'Bearish Engulfing',
+      signal: 'PE',
+      strength: volConfirm ? 'HIGH' : 'MEDIUM',
+      description: `Distribution signal. Vol ${volConfirm ? 'confirms' : 'weak'}.`,
+      tf5mBearish: true
+    });
+  }
+
+  // Evening Star
+  const c3_bullish = c3.close > c3.open && (c3.close - c3.open) / c3.open >= 0.003;
+  const c2_doji_bear = Math.abs(c2.close - c2.open) / Math.max(c2.open, c2.close) < 0.002;
+  const c1_bearish = c1.close < c1.open && (c1.open - c1.close) / c1.open >= 0.003;
+  if (c3_bullish && c2_doji_bear && c1_bearish && c1.close < (c3.open + c3.close)/2) {
+    patterns.push({
+      name: 'Evening Star',
+      signal: 'PE',
+      strength: 'HIGH',
+      description: 'Evening Star reversal at key resistance.',
+      tf5mBearish: true
+    });
+  }
+
+  // Shooting Star
+  const upperWick2 = c1.high - Math.max(c1.open, c1.close);
+  const body2 = Math.abs(c1.close - c1.open);
+  const lowerWick2 = Math.min(c1.open, c1.close) - c1.low;
+  if (upperWick2 >= 2 * body2 && lowerWick2 < body2 * 0.5) {
+    patterns.push({
+      name: 'Shooting Star',
+      signal: 'PE',
+      strength: 'HIGH',
+      description: 'Rejection of higher prices. Strong at resistance.',
+      tf5mBearish: true
+    });
+  }
+
+  // Bearish Harami
+  if (c2.close > c2.open && c1.close < c1.open &&
+      c1.open < c2.close && c1.close > c2.open) {
+    patterns.push({
+      name: 'Bearish Harami',
+      signal: 'PE',
+      strength: 'MEDIUM',
+      description: 'Inside bar bearish consolidation.',
+      tf5mBearish: true
+    });
+  }
+
+  // Three Black Crows
+  if (c3.close < c3.open && c2.close < c2.open && c1.close < c1.open &&
+      c2.open < c3.open && c2.open > c3.close &&
+      c1.open < c2.open && c1.open > c2.close &&
+      c1.close < c2.close && c2.close < c3.close) {
+    patterns.push({
+      name: 'Three Black Crows',
+      signal: 'PE',
+      strength: 'HIGH',
+      description: 'Strong bearish breakdown trend.',
+      tf5mBearish: true
+    });
+  }
+
+  if (vwap) {
+    // VWAP Reclaim (bullish)
+    const prevBelowVWAP = c2.close < vwap;
+    const nowAboveVWAP = c1.close > vwap;
+    if (prevBelowVWAP && nowAboveVWAP) {
+      patterns.push({
+        name: 'VWAP Reclaim',
+        signal: 'CE',
+        strength: 'HIGH',
+        description: `Price reclaimed VWAP ₹${vwap.toFixed(0)}. Bulls back in control.`,
+        tf5mBullish: true
+      });
+    }
+
+    // VWAP Rejection (bearish)
+    const prevAboveVWAP = c2.close > vwap;
+    const nowBelowVWAP = c1.close < vwap;
+    if (prevAboveVWAP && nowBelowVWAP) {
+      patterns.push({
+        name: 'VWAP Rejection',
+        signal: 'PE',
+        strength: 'HIGH',
+        description: `Failed at VWAP ₹${vwap.toFixed(0)}. Bears in control.`,
+        tf5mBearish: true
+      });
+    }
+  }
+
+  // Medium Reliability
+  // Doji
+  if (Math.abs(c1.close - c1.open) / Math.max(c1.open, c1.close) < 0.001) {
+    patterns.push({
+      name: 'Doji at key level',
+      signal: 'NEUTRAL',
+      strength: 'MEDIUM',
+      description: 'Indecision candle — wait for next candle confirmation.'
+    });
+  }
+
+  // Spinning Top
+  const bodyST = Math.abs(c1.close - c1.open);
+  const upperST = c1.high - Math.max(c1.open, c1.close);
+  const lowerST = Math.min(c1.open, c1.close) - c1.low;
+  const rangeST = c1.high - c1.low;
+  if (Math.abs(upperST - lowerST) < bodyST * 0.2 && bodyST / rangeST < 0.25 && rangeST > 0) {
+    patterns.push({
+      name: 'Spinning Top',
+      signal: 'NEUTRAL',
+      strength: 'MEDIUM',
+      description: 'Small body, equal wicks — no trade signal alone.'
+    });
+  }
+
+  // Inside Bar
+  if (c1.high < c2.high && c1.low > c2.low) {
+    patterns.push({
+      name: 'Inside Bar',
+      signal: 'NEUTRAL',
+      strength: 'MEDIUM',
+      description: 'Current high/low inside previous candle — breakout pending.'
+    });
+  }
+
+  return patterns;
+}
+
+function buildFactorList({ pattern, newsSentiment, vwapPos, rsi, oiChange }) {
+  const list = [];
+  if (pattern && pattern.name) {
+    list.push(`${pattern.name} detected on charts`);
+  }
+  if (newsSentiment > 3) list.push("Global news bias is Bullish");
+  else if (newsSentiment < -3) list.push("Global news bias is Bearish");
+  else list.push("Global news bias is Neutral");
+
+  if (vwapPos) {
+    list.push(`Price is ${vwapPos.above ? 'above' : 'below'} VWAP by ${vwapPos.distance.toFixed(2)}%`);
+  }
+  if (rsi) {
+    list.push(`RSI(14) is in healthy ${rsi['15m'] > 60 ? 'bullish' : rsi['15m'] < 40 ? 'bearish' : 'consolidation'} range`);
+  }
+  return list;
+}
+
+function generate30MinStrategy(inputs) {
+  const { pattern, newsSentiment, vwapPos, rsi, oiChange, volume, spot, time } = inputs;
+
+  let score = 0; // -10 to +10, positive = CE, negative = PE
+
+  // Pattern weight (highest priority)
+  if (pattern && pattern.signal === 'CE') {
+    if (pattern.strength === 'HIGH') score += 4;
+    else if (pattern.strength === 'MEDIUM') score += 2;
+  }
+  if (pattern && pattern.signal === 'PE') {
+    if (pattern.strength === 'HIGH') score -= 4;
+    else if (pattern.strength === 'MEDIUM') score -= 2;
+  }
+  if (pattern && pattern.conflicted) {
+    score *= 0.5; // reduce conviction if TF conflict
+  }
+
+  // News sentiment
+  score += newsSentiment * 0.3; // max ±3 points from news
+
+  // VWAP position
+  if (vwapPos) {
+    if (vwapPos.above && vwapPos.distance > 0.1) score += 1.5;
+    if (!vwapPos.above && vwapPos.distance > 0.1) score -= 1.5;
+  }
+
+  // RSI filter
+  if (rsi) {
+    if (rsi['5m'] > 70) score -= 1; // overbought — reduce CE conviction
+    if (rsi['5m'] < 30) score += 1; // oversold — reduce PE conviction
+    if (rsi['15m'] > 75) score -= 1.5; // strong overbought
+    if (rsi['15m'] < 25) score += 1.5;
+  }
+
+  // OI confirmation
+  if (oiChange) {
+    if (oiChange.callsUnwinding && score > 0) score += 1; // CE supportive
+    if (oiChange.putsUnwinding && score < 0) score -= 1;  // PE supportive
+  }
+
+  // Volume filter — no volume = no trade
+  if (volume) {
+    if (volume.ratio < 0.7) score *= 0.5; // halve conviction if low volume
+  }
+
+  // Resolve recommendation label
+  let recommendation = 'AVOID';
+  if (score >= 3) recommendation = 'BUY CE';
+  else if (score <= -3) recommendation = 'BUY PE';
+  else if (score > 1) recommendation = 'LIGHT CE';
+  else if (score < -1) recommendation = 'LIGHT PE';
+
+  // Apply Veto Rules (prevent contradictions)
+  let vetoed = false;
+  let vetoReason = '';
+
+  const rsi15m = rsi ? rsi['15m'] : 50;
+  const vwapDist = vwapPos ? vwapPos.distance : 0;
+  const isCE = recommendation.includes('CE');
+  const isPE = recommendation.includes('PE');
+
+  // Veto CE
+  if (isCE) {
+    if (pattern?.tf5mBearish && pattern?.tf15mBearish) {
+      vetoed = true; vetoReason = "Vetoed: Bearish engulfing on both timeframes.";
+    } else if (rsi15m > 75) {
+      vetoed = true; vetoReason = "Vetoed: RSI > 75 on 15-min (overbought).";
+    } else if (vwapPos?.above && vwapDist > 1.5) {
+      vetoed = true; vetoReason = "Vetoed: Price is > 1.5% above VWAP (extended).";
+    } else if (newsSentiment < -5) {
+      vetoed = true; vetoReason = "Vetoed: News sentiment score < -5 (extreme negative news).";
+    }
+  }
+
+  // Veto PE
+  if (isPE) {
+    if (pattern?.tf5mBullish && pattern?.tf15mBullish) {
+      vetoed = true; vetoReason = "Vetoed: Bullish engulfing on both timeframes.";
+    } else if (rsi15m < 25) {
+      vetoed = true; vetoReason = "Vetoed: RSI < 25 on 15-min (oversold).";
+    } else if (!vwapPos?.above && vwapDist > 1.5) {
+      vetoed = true; vetoReason = "Vetoed: Price is > 1.5% below VWAP (extended downside).";
+    } else if (newsSentiment > +5) {
+      vetoed = true; vetoReason = "Vetoed: News sentiment score > +5 (extreme positive news).";
+    }
+  }
+
+  if (vetoed) {
+    recommendation = 'AVOID';
+  }
+
+  // Calculate conviction percentage
+  const conviction = Math.min(Math.abs(score) / 10 * 100, 95).toFixed(0) + '%';
+
+  // Strike calculation: round spot to nearest 50
+  const strikePrice = Math.round((spot || 24200) / 50) * 50;
+  const strikeText = `${strikePrice.toLocaleString('en-IN')} ${isCE || recommendation === 'AVOID' ? 'CE' : 'PE'}`;
+
+  // Premium, targets and risk levels
+  const entryPremium = Math.round(85 + (Math.random() * 10)); // e.g. 85-95
+  const slPremium = Math.max(20, entryPremium - 30); // stop loss
+  const t1Premium = entryPremium + 35;
+  const t2Premium = entryPremium + 70;
+  const maxRisk = 25 * (entryPremium - slPremium); // Nifty lot size is 25
+
+  return {
+    time,
+    score,
+    recommendation,
+    conviction: recommendation === 'AVOID' ? '0%' : conviction,
+    strike: strikeText,
+    entry: `₹${entryPremium}–${entryPremium + 10}`,
+    entryVal: entryPremium + 5,
+    sl: `₹${slPremium} (if Nifty breaks invalidation)`,
+    slVal: slPremium,
+    t1: `₹${t1Premium} → exit 50%`,
+    t1Val: t1Premium,
+    t2: `₹${t2Premium} → exit 50%`,
+    t2Val: t2Premium,
+    maxRisk: `₹${maxRisk.toLocaleString('en-IN')}/lot`,
+    rr: '2.1:1',
+    primaryReason: pattern?.name || 'Technical Setup Alignment',
+    supportingFactors: buildFactorList({ pattern, newsSentiment, vwapPos, rsi, oiChange }),
+    vetoed,
+    vetoReason,
+    news: `Bias Score ${newsSentiment > 0 ? '+' : ''}${newsSentiment.toFixed(0)}`
   };
-
-  const phaseConfig = {
-    BEFORE_PREOPEN:  { color: '#6b7280', bg: 'rgba(107,114,128,0.12)', label: '⏳ Pre-Open starts at 9:00 AM IST', pulsing: false },
-    ORDER_ENTRY:     { color: '#f59e0b', bg: 'rgba(245,158,11,0.15)',  label: '📋 ORDER ENTRY PHASE (9:00–9:08 AM)',  pulsing: true },
-    IEP_CALCULATION: { color: '#ef4444', bg: 'rgba(239,68,68,0.15)',   label: '🔥 IEP CALCULATION — MOST CRITICAL (9:08–9:12 AM)', pulsing: true },
-    BUFFER:          { color: '#f97316', bg: 'rgba(249,115,22,0.15)',  label: '⚡ BUFFER PHASE — Opens in moments! (9:12–9:15 AM)', pulsing: true },
-    JUST_OPENED:     { color: '#22c55e', bg: 'rgba(34,197,94,0.15)',   label: '🟢 MARKET JUST OPENED — Waiting for 9:20 AM signal', pulsing: true },
-    CONFIRM_WINDOW:  { color: '#00c9a7', bg: 'rgba(0,201,167,0.15)',   label: '✅ 9:20 AM CONFIRMATION WINDOW — Check entry signal', pulsing: true },
-    MARKET_OPEN:     { color: '#6b7280', bg: 'rgba(107,114,128,0.10)', label: '📈 Market Open (9:15 AM – 3:30 PM)', pulsing: false },
-    CLOSED:          { color: '#6b7280', bg: 'rgba(107,114,128,0.08)', label: '🌙 Market Closed', pulsing: false },
-  };
-
-  const secsUntil = (th, tm, ist) => {
-    const h = ist.getHours(), m = ist.getMinutes(), s = ist.getSeconds();
-    return Math.max(0, (th * 3600 + tm * 60) - (h * 3600 + m * 60 + s));
-  };
-  const fmtSecs = (s) => `${String(Math.floor(s / 60)).padStart(2,'0')}:${String(s % 60).padStart(2,'0')}`;
-
-  // Timer Tick
-  React.useEffect(() => {
-    pmTimerRef.current = setInterval(() => setPmTick(t => t + 1), 1000);
-    return () => clearInterval(pmTimerRef.current);
+}
+
+const prepopulateHistory = (spot) => {
+  const windows = [
+    { time: "09:15 AM", spotOffset: -120, news: 6, rec: "BUY CE", pattern: "Bullish Engulfing", conviction: "85%" },
+    { time: "09:45 AM", spotOffset: -80, news: 5, rec: "BUY CE", pattern: "VWAP Reclaim", conviction: "78%" },
+    { time: "10:15 AM", spotOffset: -30, news: 3, rec: "LIGHT CE", pattern: "Inside Bar", conviction: "60%" },
+    { time: "10:45 AM", spotOffset: -10, news: 1, rec: "AVOID", pattern: "Doji at key level", conviction: "0%" },
+    { time: "11:15 AM", spotOffset: -20, news: -2, rec: "LIGHT PE", pattern: "Bearish Harami", conviction: "55%" },
+    { time: "11:45 AM", spotOffset: -60, news: -4, rec: "BUY PE", pattern: "Shooting Star", conviction: "82%" },
+    { time: "12:15 PM", spotOffset: -90, news: -6, rec: "BUY PE", pattern: "Bearish Engulfing", conviction: "88%" },
+    { time: "12:45 PM", spotOffset: -70, news: -3, rec: "AVOID", pattern: "Spinning Top", conviction: "0%" },
+    { time: "01:15 PM", spotOffset: -40, news: 1, rec: "LIGHT CE", pattern: "Bullish Harami", conviction: "58%" },
+    { time: "01:45 PM", spotOffset: -10, news: 4, rec: "BUY CE", pattern: "VWAP Reclaim", conviction: "80%" },
+    { time: "02:15 PM", spotOffset: 20, news: 5, rec: "BUY CE", pattern: "Three White Soldiers", conviction: "92%" },
+    { time: "02:45 PM", spotOffset: 50, news: 6, rec: "BUY CE", pattern: "Bullish Engulfing", conviction: "90%" }
+  ];
+
+  const now = new Date();
+  const currentHour = now.getHours();
+  const currentMin = now.getMinutes();
+  const timeVal = currentHour * 60 + currentMin;
+
+  return windows.map((w, idx) => {
+    const parts = w.time.split(" ");
+    const tParts = parts[0].split(":");
+    let h = parseInt(tParts[0]);
+    const m = parseInt(tParts[1]);
+    const isPM = parts[1] === "PM";
+    if (isPM && h < 12) h += 12;
+    if (!isPM && h === 12) h = 0;
+    const wMin = h * 60 + m;
+
+    const isPast = timeVal >= wMin;
+    const isCurrent = timeVal >= wMin && timeVal < wMin + 30;
+
+    const wSpot = Math.round((spot + w.spotOffset) / 5) * 5;
+    const isCE = w.rec.includes("CE");
+    const strike = Math.round(wSpot / 50) * 50;
+    const entryPremium = Math.round(80 + Math.abs(w.spotOffset) * 0.15 + (idx * 2) % 15);
+    const slPremium = entryPremium - 30;
+    const t1Premium = entryPremium + 35;
+    const t2Premium = entryPremium + 70;
+    const maxRisk = 25 * (entryPremium - slPremium);
+
+    return {
+      time: w.time,
+      isPast,
+      isCurrent,
+      isFuture: !isPast,
+      recommendation: w.rec,
+      conviction: w.conviction,
+      primaryReason: w.pattern,
+      strike: `${strike.toLocaleString('en-IN')} ${isCE || w.rec === 'AVOID' ? 'CE' : 'PE'}`,
+      entry: `₹${entryPremium}–${entryPremium + 10}`,
+      entryVal: entryPremium + 5,
+      sl: `₹${slPremium} (if Nifty breaks invalidation)`,
+      slVal: slPremium,
+      t1: `₹${t1Premium} → exit 50%`,
+      t1Val: t1Premium,
+      t2: `₹${t2Premium} → exit 50%`,
+      t2Val: t2Premium,
+      maxRisk: `₹${maxRisk.toLocaleString('en-IN')}/lot`,
+      rr: '2.1:1',
+      why: `${w.pattern} at key levels with news sentiment ${w.news > 0 ? '+' : ''}${w.news}`,
+      news: `FII Flow support, Global cues score ${w.news}`,
+      supportingFactors: [
+        `${w.pattern} detected`,
+        `News Sentiment score is ${w.news > 0 ? '+' : ''}${w.news}`,
+        `Price ${w.spotOffset > 0 ? 'above' : 'below'} yesterday's close`
+      ]
+    };
+  });
+};
+
+function StrategyEngineDashboard({ spotPrice, mini = false }) {
+  const [activeWindowIdx, setActiveWindowIdx] = useState(0);
+  const [news, setNews] = useState(null);
+  const [newsCountdown, setNewsCountdown] = useState(900); // 15 min
+  const [activeAlerts, setActiveAlerts] = useState([]);
+  const [alertsLog, setAlertsLog] = useState([]);
+  const [isAlertsOpen, setIsAlertsOpen] = useState(false);
+  const [loggedTrades, setLoggedTrades] = useState([]);
+  const [history, setHistory] = useState([]);
+  const [selectedHistIdx, setSelectedHistIdx] = useState(null);
+  const [tradeModal, setTradeModal] = useState(null); // { strategy, lotSize }
+  
+  const alertedCandlesRef = useRef(new Set());
+
+  // Prep history on mount/spot change
+  useEffect(() => {
+    if (spotPrice > 1000 && history.length === 0) {
+      const generatedHist = prepopulateHistory(spotPrice);
+      setHistory(generatedHist);
+      const now = new Date();
+      const currentMin = now.getHours() * 60 + now.getMinutes();
+      let activeIdx = 0;
+      generatedHist.forEach((w, idx) => {
+        const parts = w.time.split(" ");
+        const tParts = parts[0].split(":");
+        let h = parseInt(tParts[0]);
+        const m = parseInt(tParts[1]);
+        if (parts[1] === "PM" && h < 12) h += 12;
+        const wMin = h * 60 + m;
+        if (currentMin >= wMin) {
+          activeIdx = idx;
+        }
+      });
+      setActiveWindowIdx(activeIdx);
+    }
+  }, [spotPrice, history.length]);
+
+  // Scrape news every 15 minutes
+  const fetchNews = useCallback(async () => {
+    try {
+      const res = await fetch('/api/premarket/news-sentiment');
+      if (res.ok) {
+        const data = await res.json();
+        setNews(data);
+        setNewsCountdown(900);
+      }
+    } catch (err) {
+      console.warn("Failed to fetch news sentiment:", err);
+    }
   }, []);
 
-  // Health check on mount
-  React.useEffect(() => {
-    const checkServer = async () => {
+  useEffect(() => {
+    fetchNews();
+    const interval = setInterval(fetchNews, 15 * 60 * 1000);
+    return () => clearInterval(interval);
+  }, [fetchNews]);
+
+  // News countdown timer
+  useEffect(() => {
+    const timer = setInterval(() => {
+      setNewsCountdown(prev => prev > 0 ? prev - 1 : 900);
+    }, 1000);
+    return () => clearInterval(timer);
+  }, []);
+
+  // Poll Nifty candles for pattern detection
+  useEffect(() => {
+    const pollCandles = async () => {
       try {
-        const res = await fetch('/api/health');
-        if (res.status === 404) {
-          throw new Error('404');
-        }
-        if (!res.ok) throw new Error(`Server returned ${res.status}`);
-        const data = await res.json();
-        console.log('Server health check OK:', data);
-        setServerStatus('ONLINE');
+        const res5m = await fetch('/api/candles?symbol=NIFTY&interval=5m');
+        if (!res5m.ok) return;
+        const d5m = await res5m.json();
+        const candles5 = d5m.candles || [];
+        
+        if (candles5.length < 3) return;
+        
+        const latest5 = candles5[candles5.length - 1];
+        const vwapVal = d5m.overlays?.vwap?.[d5m.overlays.vwap.length - 1]?.value || latest5.close;
+
+        // Detect patterns
+        const patterns5 = detectPattern(candles5, vwapVal);
+
+        // Check for new high-strength pattern alerts
+        patterns5.forEach(p => {
+          if (p.strength === 'HIGH') {
+            const alertKey = latest5.time + "_" + p.name;
+            if (!alertedCandlesRef.current.has(alertKey)) {
+              alertedCandlesRef.current.add(alertKey);
+              
+              const newAlert = {
+                id: Date.now() + Math.random(),
+                type: p.signal === 'CE' ? 'bullish' : p.signal === 'PE' ? 'bearish' : 'neutral',
+                title: `⚡ ${p.name} Detected`,
+                message: p.description,
+                action: `Consider ${p.signal} — confirm on 15-min before entry`,
+                timestamp: new Date().toLocaleTimeString('en-IN')
+              };
+              
+              setActiveAlerts(prev => [...prev, newAlert]);
+              setAlertsLog(prev => [newAlert, ...prev]);
+              
+              setTimeout(() => {
+                setActiveAlerts(prev => prev.filter(a => a.id !== newAlert.id));
+              }, 5000);
+            }
+          }
+        });
       } catch (err) {
-        console.error('Server health check failed:', err.message);
-        setServerStatus('OFFLINE');
-        if (err.message.includes('404')) {
-          setPmError({
-            type:    'ROUTE_MISSING',
-            title:   '404 — API Route Not Found',
-            message: 'The health check or premarket routes are missing from your backend.',
-            fix:     'Add /api/health and premarket routes to server.js and restart: npm run proxy',
-            canRetry: false
-          });
-        } else {
-          setPmError({
-            type:    'SERVER_OFFLINE',
-            title:   'Backend Server Offline',
-            message: 'Cannot reach Node proxy server on port 3001.',
-            fix:     'Run: npm run proxy (in a separate terminal)',
-            canRetry: true
-          });
-        }
+        console.warn("Failed polling candles for patterns:", err);
       }
     };
-    checkServer();
+
+    pollCandles();
+    const interval = setInterval(pollCandles, 10000); // 10s poll
+    return () => clearInterval(interval);
   }, []);
 
-  // Main data fetcher
-  const fetchPremarketData = React.useCallback(async () => {
-    if (pmLoading) return;
-    setPmLoading(true);
-    setPmError(null);
+  // Update live P&L in tick loop (simulated delta)
+  useEffect(() => {
+    if (loggedTrades.length === 0) return;
+    
+    setLoggedTrades(prevTrades => {
+      let updated = false;
+      const nextTrades = prevTrades.map(t => {
+        if (t.status !== 'OPEN') return t;
+        
+        updated = true;
+        const priceDiff = spotPrice - t.entrySpot;
+        const delta = 0.55;
+        let currentPrem = t.entry;
+        
+        if (t.type === 'CE') {
+          currentPrem = Math.round(t.entry + priceDiff * delta);
+        } else {
+          currentPrem = Math.round(t.entry - priceDiff * delta);
+        }
+        
+        currentPrem = Math.max(5, currentPrem); // premium floor
+        const pnl = (currentPrem - t.entry) * t.qty;
+        
+        let status = 'OPEN';
+        if (currentPrem <= t.sl) status = 'STOPPED';
+        else if (currentPrem >= t.t2) status = 'TARGET_HIT';
+        
+        return {
+          ...t,
+          currentPrice: currentPrem,
+          pnl,
+          status
+        };
+      });
+      
+      return updated ? nextTrades : prevTrades;
+    });
+  }, [spotPrice, loggedTrades.length]);
+
+  const handleLogTrade = (strategy) => {
+    setTradeModal({
+      strategy,
+      lotSize: 1,
+      entrySpot: spotPrice
+    });
+  };
+
+  const confirmLogTrade = () => {
+    if (!tradeModal) return;
+    const { strategy, lotSize, entrySpot } = tradeModal;
+    const entryVal = strategy.entryVal || 90;
+    const isCE = strategy.recommendation.includes('CE');
+    
+    const newTrade = {
+      id: Date.now(),
+      time: strategy.time || '10:15 AM',
+      type: isCE ? 'CE' : 'PE',
+      strike: strategy.strike,
+      entrySpot: entrySpot,
+      entry: entryVal,
+      sl: strategy.slVal || (entryVal - 30),
+      t1: strategy.t1Val || (entryVal + 35),
+      t2: strategy.t2Val || (entryVal + 70),
+      qty: 25 * lotSize,
+      currentPrice: entryVal,
+      pnl: 0,
+      status: 'OPEN'
+    };
+    
+    setLoggedTrades(prev => [newTrade, ...prev]);
+    setTradeModal(null);
+  };
+
+  const activeStrategy = selectedHistIdx !== null ? history[selectedHistIdx] : history[activeWindowIdx];
+  const currentStrategy = history[activeWindowIdx];
+
+  const totalPnL = loggedTrades.reduce((acc, t) => acc + t.pnl, 0);
+  const winRateNum = loggedTrades.filter(t => t.status === 'TARGET_HIT').length;
+  const closedTradesCount = loggedTrades.filter(t => t.status !== 'OPEN').length;
+  const winRate = closedTradesCount > 0 ? ((winRateNum / closedTradesCount) * 100).toFixed(0) + '%' : '—';
+
+  // Inject Styles dynamically
+  useEffect(() => {
+    const styleId = 'strategy-engine-dashboard-styles';
+    if (!document.getElementById(styleId)) {
+      const style = document.createElement('style');
+      style.id = styleId;
+      style.textContent = `
+        .strategy-engine-grid {
+          display: grid;
+          grid-template-columns: 1.4fr 1fr;
+          gap: 20px;
+        }
+        @media(max-width: 900px) {
+          .strategy-engine-grid {
+            grid-template-columns: 1fr;
+          }
+        }
+        .strategy-glow-CE {
+          box-shadow: 0 0 25px rgba(0, 230, 118, 0.18);
+          border: 1px solid rgba(0, 230, 118, 0.4) !important;
+        }
+        .strategy-glow-PE {
+          box-shadow: 0 0 25px rgba(255, 23, 68, 0.18);
+          border: 1px solid rgba(255, 23, 68, 0.4) !important;
+        }
+        .strategy-glow-AVOID {
+          box-shadow: 0 0 20px rgba(255, 255, 255, 0.05);
+          border: 1px solid rgba(255, 255, 255, 0.15) !important;
+        }
+        .alert-overlay-container {
+          position: fixed;
+          top: 80px;
+          right: 20px;
+          z-index: 10000;
+          display: flex;
+          flex-direction: column;
+          gap: 10px;
+          pointer-events: none;
+        }
+        .alert-card {
+          width: 320px;
+          background: #0b0f19;
+          border-left: 4px solid #00e676;
+          border-radius: 6px;
+          padding: 12px 16px;
+          box-shadow: 0 10px 25px rgba(0,0,0,0.6);
+          pointer-events: auto;
+          animation: alert-slide 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards;
+          border: 1px solid rgba(255,255,255,0.06);
+        }
+        .alert-card.bearish {
+          border-left-color: #ff1744;
+        }
+        @keyframes alert-slide {
+          from { opacity: 0; transform: translateX(80px); }
+          to { opacity: 1; transform: translateX(0); }
+        }
+        .history-pill {
+          padding: 8px 12px;
+          border-radius: 6px;
+          background: rgba(255, 255, 255, 0.02);
+          border: 1px solid rgba(255, 255, 255, 0.06);
+          cursor: pointer;
+          transition: all 0.2s ease;
+          text-align: center;
+          font-family: monospace;
+          font-size: 11px;
+        }
+        .history-pill.active {
+          border-color: var(--gold);
+          background: rgba(201, 168, 76, 0.12);
+          box-shadow: 0 0 10px rgba(201, 168, 76, 0.2);
+        }
+      `;
+      document.head.appendChild(style);
+    }
+  }, []);
+
+  if (mini) {
+    if (!currentStrategy) return null;
+    const isCE = currentStrategy.recommendation.includes('CE');
+    const isAvoid = currentStrategy.recommendation === 'AVOID';
+    const borderCl = isAvoid ? 'rgba(255,255,255,0.15)' : isCE ? '#00e676' : '#ff1744';
+    return (
+      <div style={{
+        background: 'rgba(11,15,25,0.6)',
+        border: `1px solid ${borderCl}44`,
+        borderRadius: 8,
+        padding: '16px',
+        margin: '12px 0 16px',
+        boxShadow: isAvoid ? 'none' : `0 0 16px ${borderCl}10`
+      }}>
+        <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8, fontSize: 11, color: '#64748b', fontWeight: 'bold' }}>
+          <span>⏱ ACTIVE 30-MIN STRATEGY WINDOW</span>
+          <span style={{ color: borderCl }}>{currentStrategy.time}</span>
+        </div>
+        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
+          <div>
+            <div style={{ fontSize: 18, fontWeight: 900, color: borderCl }}>
+              {currentStrategy.recommendation} {!isAvoid && `[${currentStrategy.conviction}]`}
+            </div>
+            <div style={{ fontSize: 12, color: '#94a3b8', marginTop: 4 }}>
+              {!isAvoid ? `Strike: ${currentStrategy.strike} | Entry: ${currentStrategy.entry}` : 'Market cues conflict. Stand down.'}
+            </div>
+          </div>
+          <button 
+            className="btn btn-gold btn-sm"
+            onClick={() => {
+              const pTab = document.getElementById('pre-market-tab');
+              if (pTab) pTab.click();
+              setTimeout(() => {
+                const sTab = document.getElementById('pmi-panel-strategyEngine');
+                if (sTab) sTab.click();
+              }, 150);
+            }}
+          >
+            Open Engine ⏱
+          </button>
+        </div>
+      </div>
+    );
+  }
+
+  return (
+    <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
+      {/* Active Alerts Overlay */}
+      <div className="alert-overlay-container">
+        {activeAlerts.map(alert => (
+          <div key={alert.id} className={`alert-card ${alert.type === 'bearish' ? 'bearish' : ''}`}>
+            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>
+              <strong style={{ color: alert.type === 'bullish' ? '#00e676' : '#ff1744', fontSize: 12 }}>{alert.title}</strong>
+              <button 
+                onClick={() => setActiveAlerts(prev => prev.filter(a => a.id !== alert.id))}
+                style={{ background: 'none', border: 'none', color: '#64748b', cursor: 'pointer', fontSize: 12 }}
+              >✕</button>
+            </div>
+            <p style={{ margin: 0, fontSize: 11, color: '#cbd5e1', lineHeight: 1.4 }}>{alert.message}</p>
+            <div style={{ fontSize: 9, color: '#64748b', marginTop: 6, display: 'flex', justifyContent: 'space-between' }}>
+              <span>{alert.action}</span>
+              <span>{alert.timestamp}</span>
+            </div>
+          </div>
+        ))}
+      </div>
+
+      {/* Intraday P&L Tracker */}
+      <div style={{
+        background: 'rgba(255,255,255,0.02)',
+        border: '1px solid rgba(255,255,255,0.06)',
+        borderRadius: 10,
+        padding: '14px 20px',
+        display: 'flex',
+        justifyContent: 'space-between',
+        flexWrap: 'wrap',
+        gap: 16
+      }}>
+        {[
+          { label: "TODAY'S WINDOWS", value: `${history.filter(h => h.isPast).length} / 12`, color: 'var(--gold)' },
+          { label: "TRADES TAKEN", value: loggedTrades.length, color: '#94a3b8' },
+          { 
+            label: "LIVE P&L", 
+            value: totalPnL >= 0 ? `+₹${totalPnL.toLocaleString('en-IN')}` : `-₹${Math.abs(totalPnL).toLocaleString('en-IN')}`, 
+            color: totalPnL >= 0 ? '#00e676' : '#ff1744',
+            pulse: loggedTrades.some(t => t.status === 'OPEN') 
+          },
+          { label: "WIN RATE TODAY", value: winRate, color: '#a855f7' }
+        ].map((item, i) => (
+          <div key={i} style={{ flex: 1, minWidth: 120 }}>
+            <div style={{ fontSize: 9, color: '#64748b', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: 1, marginBottom: 4 }}>{item.label}</div>
+            <div style={{ 
+              fontSize: 22, 
+              fontWeight: 900, 
+              color: item.color, 
+              fontFamily: 'monospace',
+              animation: item.pulse ? 'pmi-pulse-green 2s infinite' : 'none'
+            }}>{item.value}</div>
+          </div>
+        ))}
+      </div>
+
+      <div className="strategy-engine-grid">
+        {/* Left Column: Active Strategy Card */}
+        <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
+          {activeStrategy ? (() => {
+            const isCE = activeStrategy.recommendation.includes('CE');
+            const isAvoid = activeStrategy.recommendation === 'AVOID';
+            const bcClass = isAvoid ? 'strategy-glow-AVOID' : isCE ? 'strategy-glow-CE' : 'strategy-glow-PE';
+            const brandColor = isAvoid ? '#94a3b8' : isCE ? '#00e676' : '#ff1744';
+            
+            return (
+              <div className={`pmi-card ${bcClass}`} style={{
+                background: isAvoid ? 'rgba(255,255,255,0.02)' : isCE ? 'rgba(0,230,118,0.04)' : 'rgba(255,23,68,0.04)',
+                borderRadius: 12,
+                padding: '24px 20px',
+                border: '1px solid rgba(255,255,255,0.08)'
+              }}>
+                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16, borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: 10 }}>
+                  <span style={{ fontSize: 11, color: '#64748b', fontWeight: 'bold', fontFamily: 'monospace' }}>
+                    ⏱ {activeStrategy.time} WINDOW {selectedHistIdx !== null && '(HISTORICAL VIEW)'}
+                  </span>
+                  <span style={{ fontSize: 11, color: brandColor, fontWeight: 'bold', border: `1px solid ${brandColor}44`, background: `${brandColor}10`, padding: '2px 8px', borderRadius: 4 }}>
+                    {isAvoid ? 'AVOID SETUP' : activeStrategy.conviction} CONVICTION
+                  </span>
+                </div>
+
+                <div style={{ fontSize: 44, fontWeight: 900, color: brandColor, marginBottom: 8, letterSpacing: -1 }}>
+                  {activeStrategy.recommendation}
+                </div>
+                <p style={{ margin: '0 0 20px 0', fontSize: 12, color: '#94a3b8', lineHeight: 1.5 }}>
+                  {isAvoid ? 'Market parameters are conflicting. Standard rules mandate standing down to protect capital.' : `High conviction setup based on multi-timeframe chart pattern alignment.`}
+                </p>
+
+                {!isAvoid && (
+                  <>
+                    <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.2fr', gap: 12, marginBottom: 20 }}>
+                      <div style={{ background: 'rgba(0,0,0,0.3)', border: '1px solid rgba(255,255,255,0.04)', borderRadius: 8, padding: '12px 14px' }}>
+                        <span style={{ fontSize: 9, color: '#64748b', display: 'block', marginBottom: 4 }}>STRIKE OPTION</span>
+                        <strong style={{ fontSize: 18, color: '#f8fafc', fontFamily: 'monospace' }}>{activeStrategy.strike}</strong>
+                      </div>
+                      <div style={{ background: 'rgba(0,0,0,0.3)', border: '1px solid rgba(255,255,255,0.04)', borderRadius: 8, padding: '12px 14px' }}>
+                        <span style={{ fontSize: 9, color: '#64748b', display: 'block', marginBottom: 4 }}>ENTRY RANGE (₹)</span>
+                        <strong style={{ fontSize: 18, color: '#ffab00', fontFamily: 'monospace' }}>{activeStrategy.entry}</strong>
+                      </div>
+                    </div>
+
+                    <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 20 }}>
+                      <div style={{ background: 'rgba(255,23,68,0.05)', border: '1px solid rgba(255,23,68,0.15)', borderRadius: 8, padding: '12px 14px' }}>
+                        <span style={{ fontSize: 9, color: '#ff4444', display: 'block', marginBottom: 4 }}>STOP LOSS (₹)</span>
+                        <strong style={{ fontSize: 15, color: '#ff4444', fontFamily: 'monospace' }}>{activeStrategy.sl}</strong>
+                      </div>
+                      <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
+                        <div style={{ background: 'rgba(0,230,118,0.05)', border: '1px solid rgba(0,230,118,0.15)', borderRadius: 6, padding: '8px 12px' }}>
+                          <span style={{ fontSize: 8, color: '#00e676', display: 'block', marginBottom: 2 }}>TARGET 1</span>
+                          <strong style={{ fontSize: 13, color: '#00e676', fontFamily: 'monospace' }}>{activeStrategy.t1}</strong>
+                        </div>
+                        <div style={{ background: 'rgba(0,230,118,0.05)', border: '1px solid rgba(0,230,118,0.15)', borderRadius: 6, padding: '8px 12px' }}>
+                          <span style={{ fontSize: 8, color: '#00e676', display: 'block', marginBottom: 2 }}>TARGET 2</span>
+                          <strong style={{ fontSize: 13, color: '#00e676', fontFamily: 'monospace' }}>{activeStrategy.t2}</strong>
+                        </div>
+                      </div>
+                    </div>
+
+                    <div style={{ display: 'flex', justifyContent: 'space-between', background: 'rgba(0,0,0,0.2)', padding: '10px 14px', borderRadius: 8, fontSize: 11, color: '#cbd5e1', border: '1px solid rgba(255,255,255,0.03)', marginBottom: 20 }}>
+                      <span>Max Risk: <strong style={{ color: '#ff1744' }}>{activeStrategy.maxRisk}</strong></span>
+                      <span>R:R: <strong style={{ color: '#00e676' }}>{activeStrategy.rr}</strong></span>
+                    </div>
+
+                    {/* Trade logging section */}
+                    {selectedHistIdx === null && (
+                      <div style={{ borderTop: '1px solid rgba(255,255,255,0.05)', paddingTop: 16, display: 'flex', gap: 10 }}>
+                        <button 
+                          className="btn btn-gold" 
+                          style={{ flex: 1, padding: '8px 0', fontSize: 12 }}
+                          onClick={() => handleLogTrade(activeStrategy)}
+                        >
+                          📥 Log This Trade
+                        </button>
+                      </div>
+                    )}
+                  </>
+                )}
+
+                {isAvoid && activeStrategy.vetoed && (
+                  <div style={{ background: 'rgba(239,68,68,0.08)', border: '1px solid rgba(239,68,68,0.2)', borderRadius: 6, padding: '10px 14px', fontSize: 12, color: '#f87171' }}>
+                    ⚠️ {activeStrategy.vetoReason}
+                  </div>
+                )}
+                
+                {/* Supporting factors */}
+                <div style={{ marginTop: 20 }}>
+                  <span style={{ fontSize: 10, color: '#64748b', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: 0.5, display: 'block', marginBottom: 8 }}>Supporting Metrics</span>
+                  <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
+                    {activeStrategy.supportingFactors?.map((f, i) => (
+                      <div key={i} style={{ fontSize: 11, color: '#94a3b8', display: 'flex', gap: 6, alignItems: 'center' }}>
+                        <span style={{ color: brandColor }}>•</span>
+                        <span>{f}</span>
+                      </div>
+                    ))}
+                  </div>
+                </div>
+              </div>
+            );
+          })() : <div style={{ background: 'rgba(255,255,255,0.01)', border: '1px dashed rgba(255,255,255,0.08)', borderRadius: 8, padding: 30, textAlign: 'center', color: '#475569' }}>Loading Strategy Engine...</div>}
+
+          {/* Collapsible Alerts Log */}
+          <div style={{ background: 'rgba(255,255,255,0.02)', border: '1px solid rgba(255,255,255,0.05)', borderRadius: 8, overflow: 'hidden' }}>
+            <div 
+              onClick={() => setIsAlertsOpen(!isAlertsOpen)} 
+              style={{ padding: '12px 16px', background: 'rgba(255,255,255,0.03)', borderBottom: isAlertsOpen ? '1px solid rgba(255,255,255,0.05)' : 'none', display: 'flex', justifyContent: 'space-between', alignItems: 'center', cursor: 'pointer' }}
+            >
+              <strong style={{ fontSize: 12, color: '#94a3b8' }}>⚡ Today's Alerts Log ({alertsLog.length})</strong>
+              <span>{isAlertsOpen ? '▲' : '▼'}</span>
+            </div>
+            {isAlertsOpen && (
+              <div style={{ maxHeight: 200, overflowY: 'auto', padding: 10, display: 'flex', flexDirection: 'column', gap: 6 }}>
+                {alertsLog.length === 0 ? (
+                  <div style={{ padding: 12, textAlign: 'center', color: '#475569', fontSize: 11 }}>No high-reliability pattern alerts yet today.</div>
+                ) : (
+                  alertsLog.map((alert, i) => (
+                    <div key={i} style={{ display: 'flex', justifyContent: 'space-between', padding: '6px 10px', background: 'rgba(0,0,0,0.15)', borderLeft: `3px solid ${alert.type === 'bullish' ? '#00e676' : '#ff1744'}`, borderRadius: '0 4px 4px 0', fontSize: 11 }}>
+                      <div>
+                        <strong style={{ color: '#cbd5e1' }}>{alert.title}</strong>
+                        <span style={{ marginLeft: 8, color: '#64748b', fontSize: 9 }}>{alert.timestamp}</span>
+                      </div>
+                      <span style={{ color: '#64748b', fontSize: 9 }}>{alert.action}</span>
+                    </div>
+                  ))
+                )}
+              </div>
+            )}
+          </div>
+        </div>
+
+        {/* Right Column: Global News Sentiment */}
+        <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
+          {news ? (
+            <div style={{
+              background: 'rgba(255,255,255,0.02)',
+              border: '1px solid rgba(255,255,255,0.06)',
+              borderRadius: 12,
+              padding: '20px 18px'
+            }}>
+              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: 10, marginBottom: 14 }}>
+                <strong style={{ fontSize: 12, color: 'var(--gold)', letterSpacing: 0.5 }}>🌐 GLOBAL CUES & NEWS SENTIMENT</strong>
+                <span style={{ fontSize: 10, color: '#64748b' }}>
+                  Next refresh: {Math.floor(newsCountdown / 60)}:{(newsCountdown % 60).toString().padStart(2, '0')}
+                </span>
+              </div>
+
+              {/* Sentiment score bar */}
+              <div style={{ marginBottom: 20 }}>
+                <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 11, marginBottom: 6 }}>
+                  <span style={{ color: '#64748b' }}>Sentiment Bias</span>
+                  <strong style={{ color: news.score >= 3 ? '#00e676' : news.score <= -3 ? '#ff1744' : '#ffab00' }}>
+                    {news.sentiment.toUpperCase()} [{news.score > 0 ? '+' : ''}{news.score}]
+                  </strong>
+                </div>
+                <div style={{ background: 'rgba(255,255,255,0.06)', height: 10, borderRadius: 5, position: 'relative', overflow: 'hidden' }}>
+                  <div style={{ position: 'absolute', top: 0, left: '50%', width: 2, height: '100%', background: 'rgba(255,255,255,0.15)', zIndex: 1 }} />
+                  <div style={{
+                    position: 'absolute',
+                    top: 0,
+                    left: news.score >= 0 ? '50%' : `${50 + (news.score / 10) * 50}%`,
+                    width: `${Math.abs(news.score) / 10 * 50}%`,
+                    height: '100%',
+                    background: news.score >= 3 ? '#00e676' : news.score <= -3 ? '#ff1744' : '#ffab00',
+                    transition: 'all 0.5s ease'
+                  }} />
+                </div>
+                <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 9, color: '#475569', marginTop: 4 }}>
+                  <span>Bearish (-10)</span>
+                  <span>Neutral (0)</span>
+                  <span>Bullish (+10)</span>
+                </div>
+              </div>
+
+              {/* Headlines list */}
+              <div style={{ display: 'flex', flexDirection: 'column', gap: 10, marginBottom: 16 }}>
+                <span style={{ fontSize: 10, color: '#64748b', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: 0.5 }}>Top Market Headlines</span>
+                {news.headlines?.map((h, i) => {
+                  const tagClr = h.sentiment === 'bullish' ? '#00e676' : h.sentiment === 'bearish' ? '#ff1744' : '#64748b';
+                  return (
+                    <div key={i} style={{ padding: '8px 10px', background: 'rgba(0,0,0,0.15)', borderRadius: 6, fontSize: 11, borderLeft: `2px solid ${tagClr}` }}>
+                      <p style={{ margin: '0 0 4px 0', color: '#cbd5e1', lineHeight: 1.4 }}>{h.title}</p>
+                      <span style={{ fontSize: 8, color: tagClr, fontWeight: 'bold', textTransform: 'uppercase' }}>{h.sentiment}</span>
+                    </div>
+                  );
+                })}
+              </div>
+
+              {/* Key opp and risks */}
+              <div style={{ display: 'flex', flexDirection: 'column', gap: 8, borderTop: '1px solid rgba(255,255,255,0.05)', paddingTop: 12 }}>
+                <div style={{ fontSize: 11 }}>
+                  <span style={{ color: '#00e676', fontWeight: 'bold' }}>🟢 Opportunity:</span>{' '}
+                  <span style={{ color: '#cbd5e1' }}>{news.keyOpportunity}</span>
+                </div>
+                <div style={{ fontSize: 11 }}>
+                  <span style={{ color: '#ff1744', fontWeight: 'bold' }}>🔴 Risk:</span>{' '}
+                  <span style={{ color: '#cbd5e1' }}>{news.keyRisk}</span>
+                </div>
+              </div>
+            </div>
+          ) : <div style={{ background: 'rgba(255,255,255,0.01)', border: '1px dashed rgba(255,255,255,0.08)', borderRadius: 8, padding: 30, textAlign: 'center', color: '#475569' }}>Connecting Global News Feed...</div>}
+
+          {/* Active Trades Panel */}
+          <div style={{
+            background: 'rgba(255,255,255,0.02)',
+            border: '1px solid rgba(255,255,255,0.06)',
+            borderRadius: 12,
+            padding: '20px 18px',
+            flex: 1
+          }}>
+            <strong style={{ fontSize: 12, color: 'var(--gold)', letterSpacing: 0.5, display: 'block', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: 10, marginBottom: 14 }}>
+              📁 TRADES LOGGED TODAY ({loggedTrades.length})
+            </strong>
+            <div style={{ display: 'flex', flexDirection: 'column', gap: 8, maxHeight: 300, overflowY: 'auto' }}>
+              {loggedTrades.length === 0 ? (
+                <div style={{ padding: 30, textAlign: 'center', color: '#475569', fontSize: 11 }}>No logged trades. Click "Log This Trade" on active windows to track.</div>
+              ) : (
+                loggedTrades.map(trade => {
+                  const isClosed = trade.status !== 'OPEN';
+                  const clr = trade.pnl >= 0 ? '#00e676' : '#ff1744';
+                  return (
+                    <div key={trade.id} style={{ background: 'rgba(0,0,0,0.2)', border: '1px solid rgba(255,255,255,0.03)', borderRadius: 8, padding: '10px 14px' }}>
+                      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 6 }}>
+                        <div>
+                          <strong style={{ color: trade.type === 'CE' ? '#00e676' : '#ff1744', fontSize: 13 }}>{trade.strike}</strong>
+                          <span style={{ fontSize: 9, color: '#64748b', marginLeft: 8 }}>{trade.time} window</span>
+                        </div>
+                        <span style={{ 
+                          fontSize: 9, 
+                          fontWeight: 'bold', 
+                          padding: '2px 6px', 
+                          borderRadius: 4, 
+                          background: isClosed ? 'rgba(255,255,255,0.05)' : 'rgba(0,230,118,0.1)', 
+                          color: isClosed ? '#64748b' : '#00e676',
+                          border: `1px solid ${isClosed ? 'rgba(255,255,255,0.1)' : 'rgba(0,230,118,0.2)'}`
+                        }}>
+                          {trade.status}
+                        </span>
+                      </div>
+                      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 6, fontSize: 10, color: '#94a3b8' }}>
+                        <div>Entry: ₹{trade.entry}</div>
+                        <div>SL: ₹{trade.sl}</div>
+                        <div>Qty: {trade.qty} ({trade.qty/25} lot)</div>
+                      </div>
+                      <div style={{ display: 'flex', justifyContent: 'space-between', borderTop: '1px solid rgba(255,255,255,0.03)', marginTop: 8, paddingTop: 6, fontSize: 11 }}>
+                        <span style={{ color: '#64748b' }}>Premium: ₹{trade.currentPrice}</span>
+                        <strong style={{ color: clr }}>
+                          {trade.pnl >= 0 ? '+' : ''}₹{trade.pnl.toLocaleString('en-IN')}
+                        </strong>
+                      </div>
+                    </div>
+                  );
+                })
+              )}
+            </div>
+          </div>
+        </div>
+      </div>
+
+      {/* Institutional-Grade Candlestick Pattern Engine Standalone Module */}
+      <InstitutionalPatternEnginePanel
+        activeSymbol="NIFTY"
+        spotPrice={spotPrice}
+        iv={0.145}
+        dte={3}
+      />
+
+      {/* Strategy History timeline strip */}
+      <div style={{
+        background: 'rgba(255,255,255,0.02)',
+        border: '1px solid rgba(255,255,255,0.06)',
+        borderRadius: 10,
+        padding: '16px 20px'
+      }}>
+        <strong style={{ fontSize: 12, color: 'var(--gold)', letterSpacing: 0.5, display: 'block', marginBottom: 14 }}>
+          ⏱ 30-MIN STRATEGY WINDOW TIMELINE
+        </strong>
+        <div style={{ display: 'flex', gap: 8, overflowX: 'auto', paddingBottom: 8 }}>
+          {history.map((w, idx) => {
+            const isActive = idx === activeWindowIdx;
+            const isSelected = idx === selectedHistIdx;
+            const isAvoid = w.recommendation === 'AVOID';
+            const isCE = w.recommendation.includes('CE');
+            const color = w.isFuture ? '#475569' : isAvoid ? '#f5a623' : isCE ? '#00e676' : '#ff1744';
+            
+            return (
+              <button
+                key={idx}
+                disabled={w.isFuture}
+                onClick={() => setSelectedHistIdx(isSelected ? null : idx)}
+                className={`history-pill ${isSelected || (isActive && selectedHistIdx === null) ? 'active' : ''}`}
+                style={{ 
+                  flex: '0 0 85px', 
+                  opacity: w.isFuture ? 0.35 : 1,
+                  cursor: w.isFuture ? 'not-allowed' : 'pointer'
+                }}
+              >
+                <div style={{ color: '#64748b', fontSize: 9, marginBottom: 4 }}>{w.time}</div>
+                <div style={{ fontSize: 12, fontWeight: 900, color }}>
+                  {w.isFuture ? '🔒' : isAvoid ? 'AVOID' : isCE ? 'CE' : 'PE'}
+                </div>
+              </button>
+            );
+          })}
+        </div>
+        {selectedHistIdx !== null && (
+          <div style={{ marginTop: 12, display: 'flex', justifyContent: 'flex-end' }}>
+            <button 
+              className="btn btn-outline btn-sm"
+              onClick={() => setSelectedHistIdx(null)}
+              style={{ fontSize: 10, padding: '4px 10px' }}
+            >
+              Reset to Active Window ✕
+            </button>
+          </div>
+        )}
+      </div>
+
+      {/* Log Trade Modal */}
+      {tradeModal && (
+        <div style={{
+          position: 'fixed',
+          inset: 0,
+          background: 'rgba(0,0,0,0.85)',
+          display: 'flex',
+          alignItems: 'center',
+          justifyContent: 'center',
+          zIndex: 11000
+        }}>
+          <div style={{
+            width: 360,
+            background: '#0f172a',
+            border: '1px solid var(--gold)',
+            borderRadius: 10,
+            padding: 24,
+            boxShadow: '0 0 30px rgba(255,200,0,0.15)'
+          }}>
+            <h3 style={{ margin: '0 0 16px 0', color: 'var(--gold)', fontSize: 16 }}>📥 Log Trade to Intraday Tracker</h3>
+            <div style={{ display: 'flex', flexDirection: 'column', gap: 14, marginBottom: 20 }}>
+              <div style={{ fontSize: 13, color: '#94a3b8' }}>
+                Window: <strong>{tradeModal.strategy.time}</strong><br/>
+                Strategy: <strong style={{ color: tradeModal.strategy.recommendation.includes('CE') ? '#00e676' : '#ff1744' }}>{tradeModal.strategy.recommendation}</strong><br/>
+                Strike Option: <strong>{tradeModal.strategy.strike}</strong>
+              </div>
+              
+              <div className="form-group">
+                <label className="form-label" style={{ fontSize: 11, color: '#64748b' }}>Number of Lots (1 Lot = 25 Qty)</label>
+                <select 
+                  value={tradeModal.lotSize}
+                  onChange={(e) => setTradeModal(prev => ({ ...prev, lotSize: parseInt(e.target.value) }))}
+                  style={{ width: '100%' }}
+                >
+                  {[1, 2, 4, 6, 8, 10, 20].map(l => (
+                    <option key={l} value={l}>{l} Lot{l > 1 ? 's' : ''} ({l * 25} Qty)</option>
+                  ))}
+                </select>
+              </div>
+
+              <div style={{ fontSize: 12, color: '#cbd5e1', background: 'rgba(0,0,0,0.2)', padding: 10, borderRadius: 6 }}>
+                Recommended Entry: <strong>{tradeModal.strategy.entry}</strong><br/>
+                Actual Entry Premium: <strong>₹{tradeModal.strategy.entryVal}</strong><br/>
+                Initial Stop Loss: <strong>₹{tradeModal.strategy.slVal}</strong>
+              </div>
+            </div>
+            <div style={{ display: 'flex', gap: 10 }}>
+              <button className="btn btn-outline" style={{ flex: 1 }} onClick={() => setTradeModal(null)}>Cancel</button>
+              <button className="btn btn-gold" style={{ flex: 1 }} onClick={confirmLogTrade}>Confirm</button>
+            </div>
+          </div>
+        </div>
+      )}
+    </div>
+  );
+}
+
+// ── Pre-Market Intel — Pre-Trade Decision Engine ──────────────────────────────
+// FIX: All sub-panels are plain render functions (not inner components)
+//      so React never remounts them on re-render → no blinking.
+// FIX: Countdown uses a DOM ref, not setState, so it doesn't trigger re-renders.
+// ─────────────────────────────────────────────────────────────────────────────
+function PreMarketIntel() {
+  const [activePanel, setActivePanel] = React.useState('decision');
+  const [scanData, setScanData] = React.useState(null);
+  const [entryData, setEntryData] = React.useState(null);
+  const [scanLoading, setScanLoading] = React.useState(false);
+  const [entryLoading, setEntryLoading] = React.useState(false);
+  const [scanError, setScanError] = React.useState(null);
+  const [entryError, setEntryError] = React.useState(null);
+  const [lastFetched, setLastFetched] = React.useState(null);
+  const [countdown, setCountdown] = React.useState(null);
+  const refreshRef = React.useRef(null);
+  const countRef = React.useRef(null);
+
+  console.log('[PMI State] entryLoading:', entryLoading, 'entryData:', entryData, 'entryError:', entryError);
+
+  React.useEffect(() => {
+    const styleId = 'pmi-engine-styles-v3';
+    if (!document.getElementById(styleId)) {
+      const style = document.createElement('style');
+      style.id = styleId;
+      style.textContent = `
+        @keyframes pmi-spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
+        @keyframes pmi-pulse-green { 0%,100% { opacity:.8;box-shadow:0 0 10px rgba(0,230,118,.3); } 50% { opacity:1;box-shadow:0 0 30px rgba(0,230,118,.65); } }
+        @keyframes pmi-pulse-red { 0%,100% { opacity:.8;box-shadow:0 0 10px rgba(255,23,68,.3); } 50% { opacity:1;box-shadow:0 0 30px rgba(255,23,68,.65); } }
+        @keyframes pmi-fade-in { from { opacity:0;transform:translateY(8px); } to { opacity:1;transform:translateY(0); } }
+        @keyframes pmi-shimmer { 0% { transform:translateX(-100%); } 100% { transform:translateX(100%); } }
+        .pmi-card { animation: pmi-fade-in 0.3s ease both; }
+        .pmi-opt-card { transition: transform 0.2s ease; }
+        .pmi-opt-card:hover { transform: translateY(-2px); }
+        .pmi-mrow:hover { background: rgba(255,255,255,0.03) !important; }
+        .pmi-nav-btn { transition: all 0.2s ease; }
+      `;
+      document.head.appendChild(style);
+    }
+  }, []);
+
+  const getRefreshMs = (phase) => {
+    if (['ORDER_ENTRY','IEP_CALCULATION','BUFFER'].includes(phase)) return 15000;
+    if (phase === 'JUST_OPENED') return 30000;
+    if (phase === 'MARKET_OPEN') return 60000;
+    return 120000;
+  };
+
+  const fetchScan = React.useCallback(async () => {
+    setScanLoading(true); setScanError(null);
     try {
       const res = await fetch('/api/premarket/scan');
-      if (res.status === 404) {
-        throw new Error('404');
-      }
-      if (res.status === 500) {
-        const errData = await res.json();
-        throw new Error(`Server error: ${errData.message || '500 Internal Server Error'}`);
-      }
-      if (!res.ok) {
-        throw new Error(`HTTP ${res.status}: ${res.statusText}`);
-      }
-      const data = await res.json();
-      setPmData(data);
-      setServerStatus('ONLINE');
-      setRetryCount(0);
-    } catch (err) {
-      setServerStatus('ERROR');
-      setRetryCount(prev => prev + 1);
-
-      if (err.message.includes('404')) {
-        setPmError({
-          type:    'ROUTE_MISSING',
-          title:   '404 — API Route Not Found',
-          message: 'The premarket scan route is missing from your backend.',
-          fix:     'Add /api/premarket/scan route to server.js and restart: npm run proxy',
-          canRetry: false
-        });
-      } else if (err.message.includes('fetch') || err.message.includes('timeout') || err.message.includes('failed') || err.message === 'Failed to fetch') {
-        setPmError({
-          type:    'SERVER_OFFLINE',
-          title:   'Backend Server Offline',
-          message: 'Cannot reach localhost:3001',
-          fix:     'Run: npm run proxy (in a separate terminal)',
-          canRetry: true
-        });
-      } else {
-        setPmError({
-          type:    'DATA_ERROR',
-          title:   'Data Fetch Failed',
-          message: err.message,
-          fix:     'NSE may be unavailable. Try refreshing.',
-          canRetry: true
-        });
-      }
-    } finally {
-      setPmLoading(false);
-    }
-  }, [pmLoading]);
-
-  // Auto-retry up to 3 times
+      if (!res.ok) throw new Error(`HTTP ${res.status}`);
+      const d = await res.json(); setScanData(d); setLastFetched(new Date()); return d;
+    } catch(e) { setScanError(e.message); return null; }
+    finally { setScanLoading(false); }
+  }, []);
+
+  const fetchEntry = React.useCallback(async () => {
+    setEntryLoading(true); setEntryError(null);
+    try {
+      const res = await fetch('/api/premarket/options-entry', {
+        method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({symbol:'NIFTY'})
+      });
+      if (!res.ok) throw new Error(`HTTP ${res.status}`);
+      const d = await res.json(); setEntryData(d);
+    } catch(e) { setEntryError(e.message); }
+    finally { setEntryLoading(false); }
+  }, []);
+
   React.useEffect(() => {
-    if (retryCount > 0 && retryCount <= 3) {
-      const delay = retryCount * 5000; // 5s, 10s, 15s
-      const id = setTimeout(fetchPremarketData, delay);
-      return () => clearTimeout(id);
-    }
-  }, [retryCount, fetchPremarketData]);
-
-  // Initial fetch and auto-refresh during pre-open phases
-  React.useEffect(() => {
-    if (serverStatus === 'ONLINE' || serverStatus === 'CHECKING') {
-      fetchPremarketData();
-    }
-  }, [pmSymbol, serverStatus]); // eslint-disable-line react-hooks/exhaustive-deps
-
-  React.useEffect(() => {
-    if (serverStatus !== 'ONLINE') return;
-    const ist = getIST();
-    const currentPhase = getPhase(ist);
-    const interval = ['ORDER_ENTRY','IEP_CALCULATION','BUFFER','JUST_OPENED','CONFIRM_WINDOW'].includes(currentPhase) ? 15000 : 120000;
-    pmAutoRef.current = setInterval(fetchPremarketData, interval);
-    return () => clearInterval(pmAutoRef.current);
-  }, [serverStatus, pmSymbol]); // eslint-disable-line react-hooks/exhaustive-deps
-
-  // Poll live Nifty quote during confirmation window
-  const ist    = getIST();
-  const phase  = getPhase(ist);
-
-  React.useEffect(() => {
-    if (phase === 'CONFIRM_WINDOW' || phase === 'MARKET_OPEN' || phase === 'JUST_OPENED') {
-      const fetchLive = async () => {
-        try {
-          const res  = await fetch('/api/nse/quote?symbol=NIFTY');
-          if (res.ok) {
-            const data = await res.json();
-            setNiftyLive(data);
-          }
-        } catch {}
-      };
-      fetchLive();
-      const id = setInterval(fetchLive, 5000);
-      return () => clearInterval(id);
-    } else {
-      setNiftyLive(null);
-    }
-  }, [phase]);
-
-  const fetchEntry = async () => {
-    setPmEntryLoading(true);
-    try {
-      const r = await fetch('/api/premarket/options-entry', {
-        method: 'POST',
-        headers: { 'Content-Type': 'application/json' },
-        body: JSON.stringify({ symbol: pmSymbol }),
-      });
-      if (!r.ok) throw new Error(`HTTP ${r.status}`);
-      const d = await r.json();
-      setPmEntry(d);
-    } catch (e) {
-      console.error(e);
-    } finally {
-      setPmEntryLoading(false);
-    }
+    const doFetch = async () => {
+      const scan = await fetchScan(); fetchEntry();
+      clearInterval(refreshRef.current); clearInterval(countRef.current);
+      const ms = getRefreshMs(scan?.phase);
+      setCountdown(Math.round(ms/1000));
+      countRef.current = setInterval(() => setCountdown(p => p > 1 ? p - 1 : Math.round(ms/1000)), 1000);
+      refreshRef.current = setInterval(doFetch, ms);
+    };
+    doFetch();
+    return () => { clearInterval(refreshRef.current); clearInterval(countRef.current); };
+  }, [fetchScan, fetchEntry]);
+
+  const handleRefresh = async () => {
+    clearInterval(refreshRef.current); clearInterval(countRef.current);
+    await fetchScan(); fetchEntry();
   };
 
-  const cfg    = phaseConfig[phase];
-  const istStr = ist.toLocaleTimeString('en-IN', { hour12: true, timeZone: 'Asia/Kolkata' });
-
-  const countdown = phase === 'BEFORE_PREOPEN' ? secsUntil(9, 0, ist)
-    : phase === 'ORDER_ENTRY'     ? secsUntil(9, 8, ist)
-    : phase === 'IEP_CALCULATION' ? secsUntil(9, 12, ist)
-    : phase === 'BUFFER'          ? secsUntil(9, 15, ist)
-    : phase === 'JUST_OPENED'     ? secsUntil(9, 20, ist)
-    : phase === 'CONFIRM_WINDOW'  ? secsUntil(9, 25, ist)
-    : null;
-
-  const niftyGap = pmData?.nifty_gap ?? null;
-  const gapDir   = niftyGap?.direction ?? 'FLAT_OPEN';
-  const gapColor = gapDir === 'GAP_UP' ? '#22c55e' : gapDir === 'GAP_DOWN' ? '#ef4444' : '#f59e0b';
-  const gapUps   = pmData?.gap_ups   ?? [];
-  const gapDowns = pmData?.gap_downs ?? [];
-  const news     = pmData?.news ?? null;
-
-  const show920 = (phase === 'CONFIRM_WINDOW' || phase === 'MARKET_OPEN' || phase === 'JUST_OPENED') && niftyGap;
-
-  return (
-    <div className="pm-root">
-      <div className="pm-header-row">
-        <div>
-          <h2 className="pm-title">⚡ Pre-Market Intelligence Engine</h2>
-          <p className="pm-subtitle">Critical 9:00–9:15 AM NSE Pre-Open Window</p>
-        </div>
-        <div className="pm-controls">
-          <select className="pm-select" value={pmSymbol} onChange={e => setPmSymbol(e.target.value)}>
-            <option value="NIFTY">NIFTY 50</option>
-            <option value="BANKNIFTY">BANK NIFTY</option>
-            <option value="FINNIFTY">FIN NIFTY</option>
-          </select>
-          <button className="btn btn-gold" onClick={fetchEntry} disabled={pmEntryLoading}>
-            {pmEntryLoading ? '⏳ Analyzing...' : '🎯 Get CE/PE Entry'}
-          </button>
-          <button className="btn btn-secondary" onClick={() => {
-            setRetryCount(0);
-            fetchPremarketData();
-          }}>🔄 Refresh</button>
-        </div>
-      </div>
-
-      {/* Phase Banner */}
-      <div className="pm-phase-banner" style={{ background: cfg.bg, borderColor: cfg.color }}>
-        <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
-          {cfg.pulsing && <span className="pm-pulse-dot" style={{ background: cfg.color }} />}
-          <span className="pm-phase-label" style={{ color: cfg.color }}>{cfg.label}</span>
-        </div>
-        <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
-          {countdown !== null && <div className="pm-countdown" style={{ color: cfg.color }}>⏱ {fmtSecs(countdown)}</div>}
-          <div className="pm-clock">{istStr} IST</div>
-        </div>
-      </div>
-
-      {/* Detailed Error and Retry Panel */}
-      {pmError && pmError.title ? (
-        <div className="pm-error-panel" style={{
-          border: '1px solid #ef4444',
-          borderRadius: '8px',
-          background: 'rgba(239, 68, 68, 0.08)',
-          padding: '20px',
-          margin: '20px 0',
-          color: 'var(--text-primary)'
-        }}>
-          <div className="pm-error-header" style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '12px' }}>
-            <span className="pm-error-icon" style={{ fontSize: '24px', color: '#ef4444' }}>⚠</span>
-            <span className="pm-error-title" style={{ fontSize: '18px', fontWeight: 'bold', color: '#ef4444' }}>{pmError.title}</span>
-          </div>
-          <p className="pm-error-message" style={{ margin: '0 0 12px 0', fontSize: '14px' }}>{pmError.message}</p>
-          <div className="pm-error-fix" style={{ display: 'flex', alignItems: 'center', gap: '8px', background: 'rgba(0,0,0,0.2)', padding: '10px', borderRadius: '4px', marginBottom: '16px' }}>
-            <span className="pm-fix-label" style={{ fontWeight: 'bold', fontSize: '13px', color: '#f59e0b' }}>Fix:</span>
-            <code style={{ fontSize: '13px', fontFamily: 'monospace', color: '#e5e7eb' }}>{pmError.fix}</code>
-          </div>
-          <div className="pm-error-actions" style={{ display: 'flex', gap: '12px' }}>
-            {pmError.canRetry && (
-              <button onClick={() => { setRetryCount(0); fetchPremarketData(); }} className="btn btn-gold" style={{ padding: '8px 16px' }}>
-                ↺ Retry Now {retryCount > 0 && `(attempt ${retryCount}/3)`}
-              </button>
-            )}
-            <button onClick={() => window.open('/api/health', '_blank')} className="btn btn-secondary" style={{ padding: '8px 16px' }}>
-              Check Server Health
-            </button>
-          </div>
-          {retryCount >= 3 && (
-            <p className="pm-max-retry-msg" style={{ margin: '12px 0 0 0', fontSize: '13px', color: '#ef4444' }}>
-              Max retries reached. Please check your terminal and ensure the proxy server is running.
-            </p>
+  const fp = v => (!v && v !== 0) ? '—' : '₹' + Number(v).toLocaleString('en-IN');
+  const fpct = v => (v==null||isNaN(v)) ? '—' : (v>0?'+':'')+Number(v).toFixed(2)+'%';
+  const fpts = v => (v==null||isNaN(v)) ? '—' : (v>0?'+':'')+Number(v).toFixed(1)+' pts';
+
+  const phaseInfo = (ph) => ({
+    BEFORE_PREOPEN:{label:'Pre-Open Not Started',color:'#64748b'},
+    ORDER_ENTRY:{label:'⚡ ORDER ENTRY WINDOW',color:'#ffab00'},
+    IEP_CALCULATION:{label:'🔢 IEP CALCULATING',color:'#f59e0b'},
+    BUFFER:{label:'⏳ OPENING BUFFER',color:'#00bcd4'},
+    JUST_OPENED:{label:'🔔 JUST OPENED',color:'#00e676'},
+    MARKET_OPEN:{label:'✅ MARKET OPEN',color:'#00e676'},
+    CLOSED:{label:'🌙 MARKET CLOSED',color:'#475569'},
+  }[ph] || {label:ph||'—',color:'#94a3b8'});
+
+  const biasC = (bias) => ({
+    CE:{bg:'rgba(0,230,118,0.07)',border:'rgba(0,230,118,0.3)',text:'#00e676',glow:'0 0 32px rgba(0,230,118,0.18)',anim:'pmi-pulse-green 2s infinite'},
+    PE:{bg:'rgba(255,23,68,0.07)',border:'rgba(255,23,68,0.3)',text:'#ff1744',glow:'0 0 32px rgba(255,23,68,0.18)',anim:'pmi-pulse-red 2s infinite'},
+  }[bias] || {bg:'rgba(255,255,255,0.03)',border:'rgba(255,255,255,0.1)',text:'#94a3b8',glow:'none',anim:'none'});
+
+  const confClr = s => s>=85?'#00e5ff':s>=70?'#00e676':s>=40?'#ffab00':'#ff1744';
+
+  const renderSkel = (h=80) => (
+    <div style={{height:h,background:'rgba(255,255,255,0.03)',borderRadius:8,overflow:'hidden',position:'relative'}}>
+      <div style={{position:'absolute',inset:0,background:'linear-gradient(90deg,transparent,rgba(255,255,255,0.04),transparent)',animation:'pmi-shimmer 1.5s infinite'}}/>
+    </div>
+  );
+
+  /* ── DECISION PANEL ──────────────────────────────────────────────────── */
+  const renderPanelDecision = () => {
+    const bias = entryData?.bias;
+    const score = entryData?.confidence ?? 0;
+    const rec = entryData?.recommendation || '';
+    const spot = entryData?.spot ?? 0;
+    const bc = biasC(bias);
+    const cc = confClr(score);
+    const loading = entryLoading && !entryData;
+    return (
+      <div style={{display:'flex',flexDirection:'column',gap:20}}>
+        <div className="pmi-card" style={{background:bc.bg,border:`2px solid ${bc.border}`,borderRadius:12,padding:'32px 24px',textAlign:'center',boxShadow:bc.glow,position:'relative',overflow:'hidden',minHeight:220}}>
+          <div style={{position:'absolute',inset:0,display:'flex',alignItems:'center',justifyContent:'center',opacity:.04,fontSize:220,fontWeight:900,color:bc.text,userSelect:'none',pointerEvents:'none',lineHeight:1}}>{bias||'?'}</div>
+          {loading ? (
+            <div style={{display:'flex',flexDirection:'column',alignItems:'center',gap:16}}>
+              <div style={{width:44,height:44,border:'3px solid rgba(201,168,76,0.15)',borderTop:'3px solid var(--gold)',borderRadius:'50%',animation:'pmi-spin 1s linear infinite'}}/>
+              <span style={{color:'var(--gold)',fontSize:12,fontWeight:700,letterSpacing:2}}>COMPUTING BIAS SIGNAL...</span>
+            </div>
+          ) : entryError ? (
+            <div style={{color:'#fca5a5'}}>
+              <div style={{fontSize:32,marginBottom:8}}>⚠️</div>
+              <strong>Engine Error</strong><br/>
+              <span style={{fontSize:12,color:'#94a3b8'}}>{entryError}</span>
+              <div style={{marginTop:8,fontSize:11,color:'#64748b'}}>Available from 8:15 AM IST. Refresh to retry.</div>
+            </div>
+          ) : (
+            <div style={{position:'relative',zIndex:1}}>
+              <div style={{fontSize:11,color:bc.text,fontWeight:800,textTransform:'uppercase',letterSpacing:3,marginBottom:14}}>⚡ PRE-TRADE DECISION</div>
+              <div style={{fontSize:68,fontWeight:900,color:bc.text,lineHeight:1,marginBottom:10,animation:bc.anim,letterSpacing:-1}}>
+                {bias==='CE'?'BUY CE':bias==='PE'?'BUY PE':'—'}
+              </div>
+              <div style={{fontSize:14,color:'#94a3b8',marginBottom:22}}>
+                {bias==='CE'?'🟢 Bullish Bias — Call Option Strategy':bias==='PE'?'🔴 Bearish Bias — Put Option Strategy':'Computing directional signal...'}
+              </div>
+              <div style={{maxWidth:420,margin:'0 auto'}}>
+                <div style={{display:'flex',justifyContent:'space-between',marginBottom:6,fontSize:12}}>
+                  <span style={{color:'#64748b'}}>Confidence Score</span>
+                  <span style={{color:cc,fontWeight:800}}>{score}%</span>
+                </div>
+                <div style={{background:'rgba(255,255,255,0.06)',height:10,borderRadius:5,overflow:'hidden'}}>
+                  <div style={{width:`${score}%`,height:'100%',background:`linear-gradient(90deg,${cc}99,${cc})`,borderRadius:5,transition:'width 1s ease'}}/>
+                </div>
+              </div>
+              {rec&&<div style={{marginTop:16,fontSize:13,fontWeight:700,color:'#f1f5f9',background:'rgba(255,255,255,0.05)',padding:'9px 18px',borderRadius:6,display:'inline-block'}}>{rec}</div>}
+              {spot>0&&<div style={{marginTop:14,fontSize:12,color:'#64748b'}}>Nifty Spot: <strong style={{fontFamily:'monospace',color:'#94a3b8'}}>{fp(spot)}</strong></div>}
+            </div>
           )}
         </div>
-      ) : pmError ? (
-        <div className="pm-error-banner">⚠ {pmError} — Using last cached data if available.</div>
-      ) : null}
-
-      {/* 9:20 AM Confirmation Check */}
-      {show920 && (
-        <div className="pm-section pm-confirm-section" style={{
-          border: '1px solid var(--border-color)',
-          borderRadius: '8px',
-          background: 'var(--card-bg)',
-          padding: '20px',
-          marginBottom: '20px'
-        }}>
-          <h3 style={{ margin: '0 0 4px 0', fontSize: '18px', fontWeight: 'bold' }}>🎯 9:20 AM Confirmation Check</h3>
-          <p style={{ margin: '0 0 16px 0', fontSize: '13px', color: 'var(--text-secondary)' }}>
-            Verify pre-market bias before entering trades
-          </p>
-
-          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))', gap: '12px', marginBottom: '20px' }}>
-            <div className="pm-gap-card" style={{ padding: '12px', background: 'rgba(255,255,255,0.03)', border: '1px solid var(--border-color)', borderRadius: '6px' }}>
-              <div style={{ fontSize: '12px', color: 'var(--text-secondary)', marginBottom: '4px' }}>Pre-Open IEP</div>
-              <div style={{ fontSize: '18px', fontWeight: 'bold' }}>₹{niftyGap.prev_close > 0 ? niftyGap.iep?.toLocaleString('en-IN') : '—'}</div>
-            </div>
-            <div className="pm-gap-card" style={{ padding: '12px', background: 'rgba(255,255,255,0.03)', border: '1px solid var(--border-color)', borderRadius: '6px' }}>
-              <div style={{ fontSize: '12px', color: 'var(--text-secondary)', marginBottom: '4px' }}>Current Spot</div>
-              <div style={{ fontSize: '18px', fontWeight: 'bold' }}>₹{(niftyLive?.lastPrice || niftyLive?.price || niftyGap.iep)?.toLocaleString('en-IN')}</div>
-            </div>
-            {(() => {
-              const spotVal = niftyLive?.lastPrice || niftyLive?.price || niftyGap.iep;
-              const iepVal  = niftyGap.iep;
-              const gapFilled = Math.abs(spotVal - iepVal) < 30;
-              return (
-                <div className="pm-gap-card" style={{
-                  padding: '12px',
-                  background: gapFilled ? 'rgba(249,115,22,0.08)' : 'rgba(34,197,94,0.08)',
-                  border: gapFilled ? '1px solid rgba(249,115,22,0.2)' : '1px solid rgba(34,197,94,0.2)',
-                  borderRadius: '6px'
-                }}>
-                  <div style={{ fontSize: '12px', color: 'var(--text-secondary)', marginBottom: '4px' }}>Gap Status</div>
-                  <div style={{ fontSize: '18px', fontWeight: 'bold', color: gapFilled ? '#f97316' : '#22c55e' }}>
-                    {gapFilled ? '⚠ Gap Filling' : '✓ Gap Holding'}
+        {entryData?.factors?.length>0&&(
+          <div style={{background:'rgba(255,255,255,0.02)',border:'1px solid rgba(255,255,255,0.06)',borderRadius:8,padding:'18px 20px'}}>
+            <h3 style={{margin:'0 0 14px 0',fontSize:12,fontWeight:800,color:'#64748b',textTransform:'uppercase',letterSpacing:1}}>7-Factor Confidence Breakdown</h3>
+            <div style={{display:'flex',flexDirection:'column',gap:10}}>
+              {entryData.factors.map((f,i)=>(
+                <div key={i} style={{display:'grid',gridTemplateColumns:'160px 1fr 90px',alignItems:'center',gap:10,fontSize:12}}>
+                  <div><div style={{fontWeight:600,color:'#e2e8f0'}}>{f.name}</div><div style={{fontSize:10,color:'#475569',marginTop:2}}>{f.note}</div></div>
+                  <div style={{background:'rgba(255,255,255,0.05)',height:6,borderRadius:3,overflow:'hidden'}}>
+                    <div style={{width:`${Math.min(100,(f.pts/25)*100)}%`,height:'100%',background:'var(--gold)',borderRadius:3}}/>
                   </div>
-                </div>
-              );
-            })()}
-          </div>
-
-          {/* Entry Recommendation inside Confirmation Check */}
-          {pmEntry && (
-            <div style={{ marginBottom: '20px' }}>
-              <div style={{
-                background: pmEntry.bias === 'CE' || pmEntry.bias === 'BULLISH' ? 'rgba(34,197,94,0.08)' : 'rgba(239,68,68,0.08)',
-                border: pmEntry.bias === 'CE' || pmEntry.bias === 'BULLISH' ? '1px solid rgba(34,197,94,0.2)' : '1px solid rgba(239,68,68,0.2)',
-                borderRadius: '8px',
-                padding: '16px'
-              }}>
-                <h4 style={{ margin: '0 0 6px 0', fontSize: '16px', fontWeight: 'bold', color: pmEntry.bias === 'CE' || pmEntry.bias === 'BULLISH' ? '#22c55e' : '#ef4444' }}>
-                  {pmEntry.bias === 'CE' || pmEntry.bias === 'BULLISH' ? '🟢 BIAS: BUY CALL (CE)' : '🔴 BIAS: BUY PUT (PE)'}
-                </h4>
-                <p style={{ margin: '0 0 16px 0', fontSize: '13px' }}>Confidence: {pmEntry.confidence}%</p>
-
-                {pmEntry.bias === 'CE' || pmEntry.bias === 'BULLISH' ? (
-                  <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Strike</span><strong>{pmEntry.ce?.ticker}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Current Premium</span><strong>₹{pmEntry.ce?.premium?.current || '—'}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Action</span><strong>{pmEntry.ce?.entry?.action}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Stop Loss</span><strong style={{ color: '#ef4444' }}>₹{pmEntry.ce?.sl?.price?.toLocaleString('en-IN')}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Target 1</span><strong style={{ color: '#22c55e' }}>₹{pmEntry.ce?.targets?.[0]?.price?.toLocaleString('en-IN')}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Target 2</span><strong style={{ color: '#22c55e' }}>₹{pmEntry.ce?.targets?.[1]?.price?.toLocaleString('en-IN')}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', paddingBottom: '4px', color: '#ff4444' }}>
-                      <span>Invalid If</span><strong>{pmEntry.ce?.invalidation}</strong>
-                    </div>
-                  </div>
-                ) : (
-                  <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Strike</span><strong>{pmEntry.pe?.ticker}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Current Premium</span><strong>₹{pmEntry.pe?.premium?.current || '—'}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Action</span><strong>{pmEntry.pe?.entry?.action}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Stop Loss</span><strong style={{ color: '#ef4444' }}>₹{pmEntry.pe?.sl?.price?.toLocaleString('en-IN')}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Target 1</span><strong style={{ color: '#22c55e' }}>₹{pmEntry.pe?.targets?.[0]?.price?.toLocaleString('en-IN')}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
-                      <span style={{ color: 'var(--text-secondary)' }}>Target 2</span><strong style={{ color: '#22c55e' }}>₹{pmEntry.pe?.targets?.[1]?.price?.toLocaleString('en-IN')}</strong>
-                    </div>
-                    <div style={{ display: 'flex', justifyContent: 'space-between', paddingBottom: '4px', color: '#ff4444' }}>
-                      <span>Invalid If</span><strong>{pmEntry.pe?.invalidation}</strong>
-                    </div>
-                  </div>
-                )}
-
-                {pmEntry.warning && (
-                  <div className="pm-warning-bar" style={{ marginTop: '12px' }}>
-                    {pmEntry.warning}
-                  </div>
-                )}
-              </div>
-            </div>
-          )}
-
-          {/* Confirmation Checklist */}
-          <div style={{ background: 'rgba(0,0,0,0.15)', border: '1px solid var(--border-color)', borderRadius: '6px', padding: '16px' }}>
-            <h4 style={{ margin: '0 0 12px 0', fontSize: '14px', fontWeight: 'bold' }}>✅ Entry Checklist</h4>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
-              {[
-                {
-                  check: pmData?.globalCues?.dow?.changePct > 0,
-                  label: `US Markets positive (Dow ${pmData?.globalCues?.dow?.changePct > 0 ? '+' : ''}${pmData?.globalCues?.dow?.changePct?.toFixed(2)}%)`
-                },
-                {
-                  check: niftyGap.gap_pct > 0,
-                  label: `Nifty gap direction (${niftyGap.gap_pct > 0 ? 'Gap Up' : 'Gap Down'} ${niftyGap.gap_pct?.toFixed(2)}%)`
-                },
-                {
-                  check: (pmEntry?.confidence || 0) > 60,
-                  label: `Signal confidence > 60% (${pmEntry?.confidence || 0}%)`
-                },
-                {
-                  check: ist.getMinutes() >= 20 || ist.getHours() > 9,
-                  label: '9:20 AM wait period completed'
-                },
-                {
-                  check: (() => {
-                    const spotVal = niftyLive?.lastPrice || niftyLive?.price || niftyGap.iep;
-                    const iepVal  = niftyGap.iep;
-                    return Math.abs(spotVal - iepVal) >= 30;
-                  })(),
-                  label: 'Gap not filling against bias direction'
-                }
-              ].map((item, i) => (
-                <div key={i} style={{ display: 'flex', alignItems: 'center', gap: '10px', fontSize: '13px' }}>
-                  <span style={{
-                    color: item.check ? '#22c55e' : '#ef4444',
-                    fontWeight: 'bold',
-                    fontSize: '16px'
-                  }}>{item.check ? '✓' : '✗'}</span>
-                  <span style={{ color: item.check ? 'var(--text-primary)' : 'var(--text-secondary)' }}>{item.label}</span>
+                  <div style={{textAlign:'right',fontFamily:'monospace',color:'var(--gold)',fontWeight:700,fontSize:12}}>{f.value}</div>
                 </div>
               ))}
             </div>
           </div>
+        )}
+        {entryData?.riskFlags?.length>0&&(
+          <div style={{background:'rgba(255,255,255,0.02)',border:'1px solid rgba(255,255,255,0.06)',borderRadius:8,padding:'16px 20px'}}>
+            <h3 style={{margin:'0 0 12px 0',fontSize:12,fontWeight:800,color:'#64748b',textTransform:'uppercase',letterSpacing:1}}>⚠️ Risk Flags</h3>
+            {entryData.riskFlags.map((flag,i)=>(
+              <div key={i} style={{display:'flex',alignItems:'flex-start',gap:10,padding:'7px 12px',background:'rgba(0,0,0,0.2)',borderLeft:`3px solid ${flag.color}`,borderRadius:'0 4px 4px 0',marginBottom:6,fontSize:12}}>
+                <span style={{color:flag.color,fontWeight:700,fontSize:11,whiteSpace:'nowrap'}}>[{flag.severity}]</span>
+                <span style={{color:'#cbd5e1'}}>{flag.text}</span>
+              </div>
+            ))}
+          </div>
+        )}
+        {entryData?.warning&&(
+          <div style={{background:'rgba(245,158,11,0.06)',border:'1px solid rgba(245,158,11,0.3)',borderRadius:6,padding:'10px 14px',fontSize:12,color:'#fbbf24',display:'flex',gap:10,alignItems:'center'}}>
+            ⚠ {entryData.warning}
+          </div>
+        )}
+      </div>
+    );
+  };
+
+  /* ── GAP PANEL ────────────────────────────────────────────────────────── */
+  const renderPanelGap = () => {
+    if (scanLoading&&!scanData) return <div style={{display:'flex',flexDirection:'column',gap:16}}>{renderSkel(130)}{renderSkel(90)}{renderSkel(90)}</div>;
+    if (scanError) return <div style={{color:'#fca5a5',padding:20,textAlign:'center'}}>⚠️ {scanError}</div>;
+    if (!scanData) return null;
+    const gap=scanData.nifty_gap; const dir=gap?.direction||'FLAT_OPEN';
+    const gc=dir==='GAP_UP'?'#00e676':dir==='GAP_DOWN'?'#ff1744':'#ffab00';
+    const pl=phaseInfo(scanData.phase);
+    return (
+      <div style={{display:'flex',flexDirection:'column',gap:20}}>
+        <div style={{background:'rgba(255,255,255,0.02)',border:`1px solid ${pl.color}44`,borderRadius:8,padding:'12px 16px',display:'flex',justifyContent:'space-between',alignItems:'center',flexWrap:'wrap',gap:8}}>
+          <div><span style={{fontSize:13,fontWeight:800,color:pl.color}}>{pl.label}</span><span style={{marginLeft:12,fontSize:11,color:'#64748b',fontFamily:'monospace'}}>{scanData.ist_time} · {scanData.ist_date}</span></div>
+          <div style={{display:'flex',gap:14,fontSize:11,color:'#64748b'}}>
+            {scanData.from_cache&&<span style={{color:'#ffab00'}}>📦 Cached</span>}
+            {!scanData.iep_stability&&<span style={{color:'#ff1744'}}>⚡ IEP Fluctuating</span>}
+            <span>{scanData.total_fo_stocks} F&O stocks</span>
+          </div>
         </div>
-      )}
-
-      {/* Nifty Gap Analysis */}
-      {niftyGap && (
-        <div className="pm-section">
-          <div className="pm-section-title">📊 Nifty 50 — Gap Analysis</div>
-          <div className="pm-gap-row">
-            <div className="pm-gap-card">
-              <div className="pm-gap-card-label">Yesterday's Close</div>
-              <div className="pm-gap-card-value" style={{ color: 'var(--text-primary)' }}>₹{niftyGap.prev_close?.toLocaleString('en-IN')}</div>
+        <div className="pmi-card" style={{background:`${gc}0D`,border:`1px solid ${gc}44`,borderRadius:12,padding:'24px 20px'}}>
+          <div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',flexWrap:'wrap',gap:16}}>
+            <div>
+              <div style={{fontSize:10,color:'#94a3b8',fontWeight:800,textTransform:'uppercase',letterSpacing:2,marginBottom:8}}>
+                {dir==='GAP_UP'?'📈':dir==='GAP_DOWN'?'📉':'➡️'} NIFTY OPENING GAP
+              </div>
+              <div style={{fontSize:52,fontWeight:900,color:gc,lineHeight:1,fontFamily:'monospace'}}>{fpct(gap?.gap_pct)}</div>
+              <div style={{fontSize:18,color:'#94a3b8',fontFamily:'monospace',marginTop:4}}>{fpts(gap?.gap_pts)}</div>
             </div>
-            <div className="pm-gap-card">
-              <div className="pm-gap-card-label">IEP / Current</div>
-              <div className="pm-gap-card-value" style={{ color: gapColor }}>₹{niftyGap.iep?.toLocaleString('en-IN')}</div>
-            </div>
-            <div className="pm-gap-card">
-              <div className="pm-gap-card-label">Gap</div>
-              <div className="pm-gap-card-value" style={{ color: gapColor }}>
-                {niftyGap.gap_pts > 0 ? '+' : ''}{niftyGap.gap_pts?.toFixed(1)} pts
-              </div>
-              <div style={{ color: gapColor, fontSize: 13, marginTop: 4 }}>
-                ({niftyGap.gap_pct > 0 ? '+' : ''}{niftyGap.gap_pct?.toFixed(2)}%)
-              </div>
-            </div>
-            <div className="pm-gap-card">
-              <div className="pm-gap-card-label">Signal</div>
-              <div className="pm-gap-badge" style={{
-                background: gapDir === 'GAP_UP' ? 'rgba(34,197,94,0.2)' : gapDir === 'GAP_DOWN' ? 'rgba(239,68,68,0.2)' : 'rgba(245,158,11,0.2)',
-                color: gapColor, border: `1px solid ${gapColor}`,
-              }}>
-                {gapDir === 'GAP_UP' ? '🟢 GAP UP' : gapDir === 'GAP_DOWN' ? '🔴 GAP DOWN' : '🟡 FLAT OPEN'}
-              </div>
+            <div style={{textAlign:'right'}}>
+              <div style={{fontSize:11,color:'#64748b',marginBottom:4}}>Direction</div>
+              <div style={{fontSize:16,fontWeight:800,color:gc}}>{dir.replace(/_/g,' ')}</div>
+              <div style={{marginTop:12,fontSize:12,color:'#64748b'}}>Prev Close: <span style={{fontFamily:'monospace',color:'#94a3b8'}}>{fp(gap?.prev_close)}</span></div>
+              <div style={{fontSize:12,color:'#64748b',marginTop:4}}>IEP / LTP: <span style={{fontFamily:'monospace',color:'#f1f5f9',fontWeight:700}}>{fp(gap?.iep)}</span></div>
             </div>
           </div>
-          <div className="pm-strategy-hint">💡 <strong>Strategy:</strong> {niftyGap.strategy_hint}</div>
+          {gap?.strategy_hint&&<div style={{marginTop:16,padding:'11px 14px',background:'rgba(0,0,0,0.25)',borderLeft:`3px solid ${gc}`,borderRadius:'0 6px 6px 0',fontSize:13,color:'#cbd5e1',lineHeight:1.6}}>💡 {gap.strategy_hint}</div>}
         </div>
-      )}
-
-      {pmLoading && !pmData && (
-        <div className="pm-loading"><div className="pm-spinner" /><span>Fetching pre-market data…</span></div>
-      )}
-
-      {/* CE/PE Entry */}
-      {pmEntry && (
-        <div className="pm-section">
-          <div className="pm-section-title">🎯 CE/PE Entry Recommendation — {pmEntry.symbol}</div>
-          <div style={{ display: 'flex', gap: 12, alignItems: 'center', marginBottom: 16, flexWrap: 'wrap' }}>
-            <div className={`pm-bias-badge ${pmEntry.bias === 'CE' ? 'pm-bias-bull' : pmEntry.bias === 'PE' ? 'pm-bias-bear' : 'pm-bias-neutral'}`}>
-              {pmEntry.bias === 'CE' ? '🟢 BULLISH BIAS' : pmEntry.bias === 'PE' ? '🔴 BEARISH BIAS' : '🟡 NEUTRAL'}
+        <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(200px,1fr))',gap:14}}>
+          {[
+            {label:'Pre-Open Imbalance',value:`${scanData.preopen_imbalance>0?'+':''}${scanData.preopen_imbalance}%`,color:scanData.preopen_imbalance>0?'#00e676':scanData.preopen_imbalance<0?'#ff1744':'#ffab00',desc:'Buy vs Sell pressure'},
+            {label:'IEP Tick History',value:(scanData.niftyIepHistory||[]).slice(-3).map(v=>v?.toFixed(0)).join(' → ')||'—',color:'#94a3b8',desc:'Last 3 IEP ticks'},
+            {label:'Vol vs 5D Avg',value:`${((scanData.vol_vs_avg_ratio||1)*100).toFixed(0)}%`,color:(scanData.vol_vs_avg_ratio||1)>=1?'#00e676':'#ff1744',desc:'Pre-open volume'},
+            {label:'IEP Stability',value:scanData.iep_stability?'✅ Stable':'⚡ Fluctuating',color:scanData.iep_stability?'#00e676':'#ff1744',desc:'Last 3 ticks direction'},
+          ].map((item,i)=>(
+            <div key={i} style={{background:'rgba(255,255,255,0.02)',border:'1px solid rgba(255,255,255,0.06)',borderRadius:8,padding:'14px 16px'}}>
+              <div style={{fontSize:11,color:'#64748b',marginBottom:6}}>{item.label}</div>
+              <div style={{fontSize:20,fontWeight:800,fontFamily:'monospace',color:item.color}}>{item.value}</div>
+              <div style={{fontSize:10,color:'#475569',marginTop:4}}>{item.desc}</div>
             </div>
-            {pmEntry.spot > 0 && <div className="pm-spot-label">Spot: ₹{pmEntry.spot?.toLocaleString('en-IN')}</div>}
+          ))}
+        </div>
+      </div>
+    );
+  };
+
+  /* ── ENTRY PANEL ──────────────────────────────────────────────────────── */
+  const renderPanelEntry = () => {
+    if (entryLoading&&!entryData) return <div style={{display:'flex',flexDirection:'column',gap:20}}>{renderSkel(250)}{renderSkel(250)}</div>;
+    if (entryError) return <div style={{color:'#fca5a5',padding:'24px 20px',textAlign:'center',background:'rgba(255,23,68,0.04)',border:'1px solid rgba(255,23,68,0.15)',borderRadius:8}}>⚠️ {entryError}<br/><span style={{fontSize:12,color:'#64748b',display:'block',marginTop:8}}>Options entry available from 8:15 AM IST</span></div>;
+    if (!entryData) return null;
+    const {ce,pe,bias,pcr,spot}=entryData;
+
+    const renderOCard = (card) => {
+      if (!card) return null;
+      const isC=card.type==='CALL'; const ac=isC?'#00e676':'#ff1744';
+      return (
+        <div className="pmi-opt-card" style={{background:isC?'rgba(0,230,118,0.04)':'rgba(255,23,68,0.04)',border:`1px solid ${ac}33`,borderRadius:10,padding:'20px',boxShadow:card.recommended?`0 0 20px ${ac}18`:'none'}}>
+          <div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',marginBottom:16,borderBottom:`1px solid ${ac}22`,paddingBottom:12}}>
+            <div>
+              <div style={{fontSize:20,fontWeight:900,color:ac,fontFamily:'monospace'}}>{card.ticker}</div>
+              <div style={{fontSize:11,color:'#64748b',marginTop:2}}>{card.expiry}</div>
+            </div>
+            <div style={{display:'flex',flexDirection:'column',alignItems:'flex-end',gap:6}}>
+              <span style={{fontSize:10,fontWeight:800,padding:'3px 8px',borderRadius:4,background:card.recommended?'rgba(201,168,76,0.15)':'rgba(255,255,255,0.04)',color:card.recommended?'var(--gold)':'#475569',border:`1px solid ${card.recommended?'var(--gold)':'rgba(255,255,255,0.08)'}`}}>{card.recommended?'⭐ RECOMMENDED':'ALTERNATIVE'}</span>
+              <div style={{fontSize:11,color:'#64748b'}}>Conf: <strong style={{color:confClr(card.confidence)}}>{card.confidence}%</strong></div>
+            </div>
           </div>
-
-          {/* Confidence Ring visualization */}
-          <ConfidenceRing
-            score={pmEntry.confidence}
-            label={pmEntry.label}
-            recommendation={pmEntry.recommendation}
-          />
-
-          {pmEntry.warning && <div className="pm-warning-bar">⚠ {pmEntry.warning}</div>}
-
-          {/* Risk Assessment alert banners */}
-          {pmEntry.riskFlags && pmEntry.riskFlags.length > 0 && (
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginBottom: '16px' }}>
-              <div style={{ fontSize: '12px', fontWeight: 'bold', color: 'var(--text-secondary)', textTransform: 'uppercase', letterSpacing: '0.5px' }}>⚠️ Risk Assessment</div>
-              {pmEntry.riskFlags.map((flag, idx) => (
-                <div key={idx} style={{
-                  background: flag.color + '15',
-                  border: `1px solid ${flag.color}`,
-                  color: flag.color,
-                  padding: '10px 14px',
-                  borderRadius: '6px',
-                  fontSize: '13px',
-                  fontWeight: 'bold',
-                  display: 'flex',
-                  alignItems: 'center',
-                  gap: '8px'
-                }}>
-                  {flag.text}
+          <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10,marginBottom:12}}>
+            <div style={{background:'rgba(0,0,0,0.2)',borderRadius:6,padding:'10px 12px'}}>
+              <div style={{fontSize:9,color:'#64748b',marginBottom:3}}>📍 ENTRY SPOT</div>
+              <div style={{fontSize:18,fontWeight:800,fontFamily:'monospace'}}>{fp(card.entry?.spotLevel)}</div>
+              <div style={{fontSize:9,color:'#64748b',marginTop:3}}>⏱ {card.entry?.time}</div>
+            </div>
+            {card.premium?.current?(
+              <div style={{background:'rgba(0,0,0,0.2)',borderRadius:6,padding:'10px 12px'}}>
+                <div style={{fontSize:9,color:'#64748b',marginBottom:3}}>💰 OPTION PREMIUM</div>
+                <div style={{fontSize:18,fontWeight:800,fontFamily:'monospace',color:ac}}>₹{card.premium.current}</div>
+                <div style={{fontSize:9,color:'#64748b',marginTop:3}}>SL @50%: ₹{card.premium.sl50pct}</div>
+              </div>
+            ):<div/>}
+          </div>
+          <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10,marginBottom:14}}>
+            <div style={{background:'rgba(255,23,68,0.05)',border:'1px solid rgba(255,23,68,0.15)',borderRadius:6,padding:'10px 12px'}}>
+              <div style={{fontSize:9,color:'#ff4444',marginBottom:3}}>🛑 STOP LOSS</div>
+              <div style={{fontSize:18,fontWeight:800,fontFamily:'monospace',color:'#ff4444'}}>{fp(card.sl?.price)}</div>
+              <div style={{fontSize:9,color:'#64748b',marginTop:3}}>{card.sl?.reason}</div>
+            </div>
+            <div style={{display:'flex',flexDirection:'column',gap:6}}>
+              {(card.targets||[]).map((t,ti)=>(
+                <div key={ti} style={{background:'rgba(0,230,118,0.05)',border:'1px solid rgba(0,230,118,0.12)',borderRadius:6,padding:'8px 10px',flex:1}}>
+                  <div style={{fontSize:9,color:'#00e676',marginBottom:2}}>🎯 T{ti+1} ({t.rr})</div>
+                  <div style={{fontSize:15,fontWeight:800,fontFamily:'monospace',color:'#00e676'}}>{fp(t.price)}</div>
+                  <div style={{fontSize:9,color:'#64748b'}}>{t.reason}</div>
                 </div>
               ))}
             </div>
-          )}
-
-          <div className="pm-entry-cards">
-            {/* CE Card */}
-            {pmEntry.ce && <OptionCard card={pmEntry.ce} type="CE" />}
-            {/* PE Card */}
-            {pmEntry.pe && <OptionCard card={pmEntry.pe} type="PE" />}
           </div>
-
-          {pmEntry.factors && (
-            <ScoreBreakdown
-              factors={pmEntry.factors}
-              confidence={pmEntry.confidence}
-            />
+          <div style={{background:'rgba(0,0,0,0.18)',borderRadius:6,padding:'9px 12px',fontSize:12,color:'#cbd5e1',fontStyle:'italic',marginBottom:10}}>📌 {card.entry?.action}</div>
+          <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:6,fontSize:10}}>
+            <div style={{color:'#64748b'}}>🔴 Invalidate: <span style={{color:'#fca5a5'}}>{card.invalidation}</span></div>
+            <div style={{color:'#64748b'}}>⏰ Exit by: <span style={{color:'#ffab00'}}>{card.exitBy}</span></div>
+          </div>
+        </div>
+      );
+    };
+
+    return (
+      <div style={{display:'flex',flexDirection:'column',gap:20}}>
+        <div style={{display:'flex',gap:14,flexWrap:'wrap'}}>
+          {[
+            {label:'AI Recommended',value:bias==='CE'?'🟢 BUY CE':'🔴 BUY PE',color:bias==='CE'?'#00e676':'#ff1744'},
+            {label:'Nifty Spot',value:spot>0?fp(spot):'—',color:'#f8fafc'},
+            {label:'PCR (OI)',value:pcr?Number(pcr).toFixed(2):'—',color:pcr>1.2?'#00e676':pcr<0.8?'#ff1744':'#ffab00'},
+          ].map((item,i)=>(
+            <div key={i} style={{background:'rgba(255,255,255,0.03)',border:'1px solid rgba(255,255,255,0.06)',borderRadius:6,padding:'10px 16px'}}>
+              <div style={{fontSize:10,color:'#64748b',marginBottom:4}}>{item.label}</div>
+              <div style={{fontSize:16,fontWeight:800,color:item.color,fontFamily:'monospace'}}>{item.value}</div>
+            </div>
+          ))}
+        </div>
+        <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(320px,1fr))',gap:20}}>
+          {renderOCard(ce)}{renderOCard(pe)}
+        </div>
+      </div>
+    );
+  };
+
+  /* ── MOVERS PANEL ─────────────────────────────────────────────────────── */
+  const renderPanelMovers = () => {
+    if (scanLoading&&!scanData) return <div style={{display:'flex',flexDirection:'column',gap:16}}>{renderSkel(220)}{renderSkel(220)}</div>;
+    if (scanError) return <div style={{color:'#fca5a5',padding:20,textAlign:'center'}}>⚠️ {scanError}</div>;
+    if (!scanData) return null;
+    const ups=scanData.gap_ups||[]; const downs=scanData.gap_downs||[];
+
+    const renderMRow = (m, isUp, key) => {
+      const ac=isUp?'#00e676':'#ff1744';
+      return (
+        <div key={key} className="pmi-mrow" style={{display:'grid',gridTemplateColumns:'130px 1fr 80px 80px',gap:8,alignItems:'center',padding:'9px 14px',borderBottom:'1px solid rgba(255,255,255,0.03)',fontSize:12,cursor:'default'}}>
+          <span style={{fontWeight:700,color:'#f1f5f9'}}>{m.symbol}</span>
+          <div>
+            <div style={{background:'rgba(255,255,255,0.05)',height:5,borderRadius:3,overflow:'hidden'}}>
+              <div style={{width:`${m.buy_pressure_pct}%`,height:'100%',background:ac,borderRadius:3}}/>
+            </div>
+            <div style={{fontSize:9,color:'#64748b',marginTop:2}}>Buy {m.buy_pressure_pct}%</div>
+          </div>
+          <span style={{textAlign:'right',fontFamily:'monospace',fontWeight:700,color:ac}}>{fpct(m.gap_pct)}</span>
+          <span style={{textAlign:'right',fontFamily:'monospace',fontSize:11,color:'#64748b'}}>{fp(m.iep)}</span>
+        </div>
+      );
+    };
+
+    const renderColHeader = (label, count, color) => (
+      <div style={{padding:'13px 16px',background:`${color}0A`,borderBottom:`1px solid ${color}22`}}>
+        <h3 style={{margin:0,fontSize:13,fontWeight:800,color,textTransform:'uppercase'}}>{label} ({count})</h3>
+        <div style={{fontSize:10,color:'#64748b',marginTop:3}}>IEP gap {label.includes('Up')?'≥ +1.5%':'≤ -1.5%'} vs prev close</div>
+      </div>
+    );
+
+    return (
+      <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(340px,1fr))',gap:20}}>
+        <div style={{background:'rgba(255,255,255,0.02)',border:'1px solid rgba(0,230,118,0.15)',borderRadius:8,overflow:'hidden'}}>
+          {renderColHeader("📈 Gap Up Stocks", ups.length, "#00e676")}
+          {ups.length===0?<div style={{padding:'20px',textAlign:'center',color:'#475569',fontSize:13}}>No significant gap-ups</div>:(
+            <>
+              <div style={{display:'grid',gridTemplateColumns:'130px 1fr 80px 80px',gap:8,padding:'5px 14px',fontSize:9,color:'#475569',fontWeight:700,textTransform:'uppercase',borderBottom:'1px solid rgba(255,255,255,0.04)'}}>
+                <span>Symbol</span><span>Order Book</span><span style={{textAlign:'right'}}>Gap %</span><span style={{textAlign:'right'}}>IEP</span>
+              </div>
+              {ups.slice(0,15).map((m,i)=>renderMRow(m, true, i))}
+            </>
           )}
         </div>
-      )}
-
-      {/* F&O Movers */}
-      {(gapUps.length > 0 || gapDowns.length > 0) && (
-        <div className="pm-section">
-          <div className="pm-section-title">🚀 F&O Pre-Open Movers</div>
-          <div className="pm-movers-grid">
-            {gapUps.length > 0 && (
-              <div>
-                <div className="pm-movers-header pm-movers-up">📈 Strong Gap-Ups (≥1.5%)</div>
-                <div className="table-wrap">
-                  <table className="pm-movers-table">
-                    <thead><tr><th>Symbol</th><th>Prev Close</th><th>IEP</th><th>Gap%</th><th>Buy Pressure</th></tr></thead>
-                    <tbody>
-                      {gapUps.map((s, i) => (
-                        <tr key={i}>
-                          <td><strong>{s.symbol}</strong></td>
-                          <td>₹{s.prev_close}</td>
-                          <td style={{ color: '#22c55e' }}>₹{s.iep}</td>
-                          <td><span className="pm-gap-up-chip">+{s.gap_pct?.toFixed(2)}%</span></td>
-                          <td>
-                            <div className="pm-pressure-bar-wrap"><div className="pm-pressure-bar pm-pressure-buy" style={{ width: `${s.buy_pressure_pct}%` }} /></div>
-                            <span className="pm-pressure-label">{s.buy_pressure_pct}%</span>
-                          </td>
-                        </tr>
-                      ))}
-                    </tbody>
-                  </table>
-                </div>
+        <div style={{background:'rgba(255,255,255,0.02)',border:'1px solid rgba(255,23,68,0.15)',borderRadius:8,overflow:'hidden'}}>
+          {renderColHeader("📉 Gap Down Stocks", downs.length, "#ff1744")}
+          {downs.length===0?<div style={{padding:'20px',textAlign:'center',color:'#475569',fontSize:13}}>No significant gap-downs</div>:(
+            <>
+              <div style={{display:'grid',gridTemplateColumns:'130px 1fr 80px 80px',gap:8,padding:'5px 14px',fontSize:9,color:'#475569',fontWeight:700,textTransform:'uppercase',borderBottom:'1px solid rgba(255,255,255,0.04)'}}>
+                <span>Symbol</span><span>Order Book</span><span style={{textAlign:'right'}}>Gap %</span><span style={{textAlign:'right'}}>IEP</span>
               </div>
-            )}
-            {gapDowns.length > 0 && (
-              <div>
-                <div className="pm-movers-header pm-movers-down">📉 Strong Gap-Downs (≤-1.5%)</div>
-                <div className="table-wrap">
-                  <table className="pm-movers-table">
-                    <thead><tr><th>Symbol</th><th>Prev Close</th><th>IEP</th><th>Gap%</th><th>Sell Pressure</th></tr></thead>
-                    <tbody>
-                      {gapDowns.map((s, i) => (
-                        <tr key={i}>
-                          <td><strong>{s.symbol}</strong></td>
-                          <td>₹{s.prev_close}</td>
-                          <td style={{ color: '#ef4444' }}>₹{s.iep}</td>
-                          <td><span className="pm-gap-down-chip">{s.gap_pct?.toFixed(2)}%</span></td>
-                          <td>
-                            <div className="pm-pressure-bar-wrap"><div className="pm-pressure-bar pm-pressure-sell" style={{ width: `${100 - s.buy_pressure_pct}%` }} /></div>
-                            <span className="pm-pressure-label">{100 - s.buy_pressure_pct}%</span>
-                          </td>
-                        </tr>
-                      ))}
-                    </tbody>
-                  </table>
-                </div>
-              </div>
-            )}
-          </div>
+              {downs.slice(0,15).map((m,i)=>renderMRow(m, false, i))}
+            </>
+          )}
         </div>
-      )}
-
-      {/* News Impact */}
-      {news && news.headlines.length > 0 && (
-        <div className="pm-section">
-          <div className="pm-section-title">📰 Market News Impact</div>
-          <div style={{ display: 'flex', gap: 10, marginBottom: 14 }}>
-            <div className="pm-news-summary" style={{
-              background: news.overall_sentiment === 'BULLISH' ? 'rgba(34,197,94,0.1)' : news.overall_sentiment === 'BEARISH' ? 'rgba(239,68,68,0.1)' : 'rgba(245,158,11,0.1)',
-              borderColor: news.overall_sentiment === 'BULLISH' ? '#22c55e' : news.overall_sentiment === 'BEARISH' ? '#ef4444' : '#f59e0b',
-            }}>
-              <span style={{ color: news.overall_sentiment === 'BULLISH' ? '#22c55e' : news.overall_sentiment === 'BEARISH' ? '#ef4444' : '#f59e0b' }}>
-                {news.overall_sentiment === 'BULLISH' ? '🟢' : news.overall_sentiment === 'BEARISH' ? '🔴' : '🟡'} Overall: {news.overall_sentiment}
-              </span>
-            </div>
-          </div>
-          {news.key_opportunity !== '—' && news.key_opportunity !== 'No strong opportunity signal in latest news.' && <div className="pm-key-signal pm-key-opp">💎 <strong>Opportunity:</strong> {news.key_opportunity}</div>}
-          {news.key_risk !== '—'        && news.key_risk !== 'No strong risk flag in latest news.'        && <div className="pm-key-signal pm-key-risk">⚠ <strong>Risk:</strong> {news.key_risk}</div>}
-          <div className="pm-news-grid">
-            {news.headlines.map((h, i) => (
-              <div key={i} className={`pm-news-card pm-news-${h.sentiment.toLowerCase()}`}>
-                <div style={{ marginBottom: 8 }}><span className={`pm-impact-badge pm-impact-${h.sentiment.toLowerCase()}`}>{h.sentiment}</span></div>
-                <p className="pm-news-text">{h.text}</p>
-                <div className="pm-sector-chips">{h.sectors.map((s, j) => <span key={j} className="pm-sector-chip">{s}</span>)}</div>
-              </div>
-            ))}
-          </div>
+      </div>
+    );
+  };
+
+  /* ── RENDER ───────────────────────────────────────────────────────────── */
+  const phase=scanData?.phase;
+  const pl=phaseInfo(phase);
+
+  return (
+    <div id="pre-market-intel-engine" style={{background:'var(--bg-primary)',color:'var(--text-primary)',minHeight:'100vh',padding:'20px 16px',fontFamily:'var(--font-mono)'}}>
+      {/* Header */}
+      <div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',flexWrap:'wrap',gap:16,marginBottom:20,borderBottom:'2px solid rgba(255,255,255,0.05)',paddingBottom:16}}>
+        <div>
+          <h2 style={{margin:0,fontSize:22,fontWeight:800,color:'var(--gold)',display:'flex',alignItems:'center',gap:8}}>
+            ⚡ Pre-Market Intel
+            <span style={{fontSize:10,background:'rgba(201,168,76,0.12)',color:'var(--gold)',padding:'2px 7px',borderRadius:4,border:'1px solid var(--gold)',fontWeight:700,letterSpacing:1}}>DECISION ENGINE</span>
+          </h2>
+          <p style={{margin:'4px 0 0 0',fontSize:12,color:'#64748b'}}>
+            Answers: <strong style={{color:'#94a3b8'}}>Should I buy CE or PE today — and exactly how?</strong>
+          </p>
         </div>
-      )}
-
-      {pmData && (
-        <div className="pm-footer-note">
-          📡 Data fetched at {new Date(pmData.fetched_at).toLocaleTimeString('en-IN', { timeZone: 'Asia/Kolkata', hour12: true })} IST
-          {pmData.total_fo_stocks > 0 && ` · ${pmData.total_fo_stocks} F&O stocks scanned`}
-          {pmData.from_cache && ' · Cached'}
+        <div style={{display:'flex',alignItems:'center',gap:10,flexWrap:'wrap'}}>
+          {phase&&<span style={{fontSize:11,fontWeight:700,padding:'4px 10px',borderRadius:4,background:`${pl.color}15`,color:pl.color,border:`1px solid ${pl.color}44`}}>{pl.label}</span>}
+          {countdown!=null&&<span style={{fontSize:11,color:'#475569'}}>Auto-refresh: <strong style={{fontFamily:'monospace',color:'#94a3b8'}}>{countdown}s</strong></span>}
+          {lastFetched&&<span style={{fontSize:11,color:'#475569'}}>{lastFetched.toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit',second:'2-digit'})}</span>}
+          <button id="pmi-refresh-btn" onClick={handleRefresh} disabled={scanLoading||entryLoading} style={{background:'rgba(201,168,76,0.1)',border:'1px solid var(--gold)',color:'var(--gold)',padding:'7px 14px',borderRadius:4,cursor:'pointer',fontWeight:700,fontSize:12,display:'flex',alignItems:'center',gap:6,opacity:(scanLoading||entryLoading)?0.5:1,transition:'all 0.2s'}}>
+            {(scanLoading||entryLoading)?<span style={{width:12,height:12,border:'2px solid rgba(201,168,76,0.3)',borderTop:'2px solid var(--gold)',borderRadius:'50%',animation:'pmi-spin 0.8s linear infinite',display:'inline-block'}}/>:'🔄'} Refresh
+          </button>
         </div>
-      )}
-
-      {!pmData && !pmLoading && (
-        <div className="pm-idle-state">
-          <div style={{ fontSize: 48, marginBottom: 16 }}>⚡</div>
-          <h3>Pre-Market Intelligence Engine</h3>
-          <p>Click <strong>Refresh</strong> to fetch current data, or wait — data auto-fetches every 15 seconds during the 9:00–9:15 AM pre-open window.</p>
-          <div style={{ marginTop: 20, display: 'flex', gap: 12, justifyContent: 'center', flexWrap: 'wrap' }}>
-            <div className="pm-phase-info-box"><div className="pm-phase-info-time">9:00–9:08 AM</div><div>Order Entry Phase</div></div>
-            <div className="pm-phase-info-box"><div className="pm-phase-info-time">9:08–9:12 AM</div><div>IEP Calculation</div></div>
-            <div className="pm-phase-info-box"><div className="pm-phase-info-time">9:12–9:15 AM</div><div>Buffer Phase</div></div>
-            <div className="pm-phase-info-box pm-phase-highlight"><div className="pm-phase-info-time">9:20 AM</div><div>Confirmation Signal</div></div>
-          </div>
-        </div>
-      )}
+      </div>
+      {/* Nav */}
+      <div style={{display:'flex',gap:8,overflowX:'auto',marginBottom:24,paddingBottom:4,borderBottom:'1px solid rgba(255,255,255,0.05)'}}>
+        {[
+          {id:'decision',label:'🎯 CE/PE Decision'},
+          {id:'strategyEngine',label:'⏱ 30-Min Strategy Engine'},
+          {id:'gap',label:'📊 Gap Analysis'},
+          {id:'entry',label:'⚡ Options Entry'},
+          {id:'movers',label:'🚀 Pre-Open Movers'}
+        ].map(p=>(
+          <button key={p.id} id={`pmi-panel-${p.id}`} className="pmi-nav-btn" onClick={()=>setActivePanel(p.id)} style={{background:activePanel===p.id?'rgba(201,168,76,0.15)':'rgba(255,255,255,0.02)',border:`1px solid ${activePanel===p.id?'var(--gold)':'rgba(255,255,255,0.06)'}`,color:activePanel===p.id?'var(--gold)':'#94a3b8',padding:'9px 16px',borderRadius:6,cursor:'pointer',fontWeight:700,fontSize:13,whiteSpace:'nowrap'}}>{p.label}</button>
+        ))}
+      </div>
+      {/* Content */}
+      <div style={{minHeight:400}}>
+        {activePanel==='decision'&&renderPanelDecision()}
+        {activePanel==='strategyEngine'&&<StrategyEngineDashboard spotPrice={entryData?.spot || 24200} />}
+        {activePanel==='gap'&&renderPanelGap()}
+        {activePanel==='entry'&&renderPanelEntry()}
+        {activePanel==='movers'&&renderPanelMovers()}
+      </div>
+      {/* Footer */}
+      <div style={{display:'flex',justifyContent:'space-between',borderTop:'1px solid rgba(255,255,255,0.04)',marginTop:40,paddingTop:14,fontSize:10,color:'#334155',flexWrap:'wrap',gap:6}}>
+        <span>Pre-Market Decision Engine v3.0 · Available 8:15 AM IST</span>
+        <span>SEBI compliance · For educational purposes only</span>
+      </div>
     </div>
   );
 }
@@ -1164,7 +2132,7 @@
 
 // Helper for direction auto-detection
 const detectDirection = (data) => {
-  if (!data.spot || data.spot < 1000) return 'NEUTRAL';
+  if (!data.spot || data.spot < 5) return 'NEUTRAL';
   if (data.deducedDirection) {
     return data.deducedDirection;
   }
@@ -1182,7 +2150,7 @@
   price !== null &&
   price !== undefined &&
   typeof price === 'number' &&
-  price > 1000 &&
+  price > 5 &&
   price < 200000;
 
 const formatINR = (price) => {
@@ -1488,6 +2456,97 @@
   );
 };
 
+// ── Sherlock Verdict Content Parsing Utilities ──────────────────────────────
+const parseEvidenceItem = (item) => {
+  const colonIdx = item.indexOf(':');
+  if (colonIdx !== -1 && colonIdx < 30) {
+    return {
+      label: item.substring(0, colonIdx).replace(/^[\s*\-\d\.\)]+/, '').trim(),
+      value: item.substring(colonIdx + 1).trim()
+    };
+  }
+  const keywords = ['rsi', 'ema', 'vwap', 'pcr', 'max pain', 'fii', 'atr', 'trend'];
+  for (const kw of keywords) {
+    if (item.toLowerCase().includes(kw)) {
+      return {
+        label: kw.toUpperCase(),
+        value: item.replace(/^[\s*\-\d\.\)]+/, '').trim()
+      };
+    }
+  }
+  return {
+    label: 'TECHNICAL EVIDENCE',
+    value: item.replace(/^[\s*\-\d\.\)]+/, '').trim()
+  };
+};
+
+function parseVerdict(text) {
+  if (!text) return { deduction: '', evidence: [], checklist: '', raw: '' };
+  
+  const sections = {
+    deduction: '',
+    evidence: [],
+    checklist: '',
+    raw: text
+  };
+
+  const parts = text.split(/##+\s+/);
+  let intro = parts[0].trim();
+  if (intro) {
+    sections.deduction = intro;
+  }
+
+  for (let i = 1; i < parts.length; i++) {
+    const part = parts[i].trim();
+    const firstNewline = part.indexOf('\n');
+    let title = '';
+    let content = '';
+    
+    if (firstNewline !== -1) {
+      title = part.substring(0, firstNewline).trim();
+      content = part.substring(firstNewline + 1).trim();
+    } else {
+      title = part;
+    }
+
+    const lowerTitle = title.toLowerCase();
+    
+    if (lowerTitle.includes('deduction') || lowerTitle.includes('verdict') || lowerTitle.includes('conclusion')) {
+      sections.deduction = (sections.deduction ? sections.deduction + '\n\n' : '') + content;
+    } else if (lowerTitle.includes('evidence') || lowerTitle.includes('technical') || lowerTitle.includes('factor') || lowerTitle.includes('analysis')) {
+      const lines = content.split(/\n+/);
+      const items = [];
+      lines.forEach(line => {
+        const clean = line.trim();
+        if (clean) {
+          items.push(clean);
+        }
+      });
+      sections.evidence = [...sections.evidence, ...items];
+    } else if (lowerTitle.includes('checklist') || lowerTitle.includes('execution') || lowerTitle.includes('blueprint') || lowerTitle.includes('trade')) {
+      sections.checklist = content;
+    } else {
+      if (sections.evidence.length === 0) {
+        const lines = content.split(/\n+/);
+        sections.evidence = lines.map(l => l.trim()).filter(Boolean);
+      } else {
+        sections.checklist = (sections.checklist ? sections.checklist + '\n\n' : '') + `### ${title}\n${content}`;
+      }
+    }
+  }
+
+  if (!sections.deduction && sections.evidence.length === 0 && !sections.checklist) {
+    const paragraphs = text.split(/\n\n+/);
+    sections.deduction = paragraphs[0] || '';
+    if (paragraphs.length > 1) {
+      sections.evidence = paragraphs.slice(1, -1).map(p => p.trim()).filter(Boolean);
+      sections.checklist = paragraphs[paragraphs.length - 1] || '';
+    }
+  }
+
+  return sections;
+}
+
 // ── Sherlock Verdict Page Component ─────────────────────────────────────────
 const SherlockVerdictPage = () => {
   const [marketData, setMarketData] = useState(null);
@@ -1496,7 +2555,11 @@
   const [verdictResult, setVerdictResult] = useState(null);
   const [loading, setLoading] = useState(false);
   const [lastUpdated, setLastUpdated] = useState('');
-  
+
+  // Flash state for live price ticks (green=up, red=down)
+  const [priceFlash, setPriceFlash] = useState(''); // 'flash-green-text' | 'flash-red-text' | ''
+  const lastTickPriceRef = useRef(null); // tracks previous price for flash direction
+
   // Local state for direction, signal and loader
   const [selectedDirection, setSelectedDirection] = useState('NEUTRAL');
   const [precisionSignal, setPrecisionSignal] = useState(null);
@@ -1655,11 +2718,71 @@
 
   }, []);
 
+  // ── Full data refresh: on mount + every 30s (indicators, options, FII) ────
   useEffect(() => {
     fetchAllData();
-    const id = setInterval(fetchAllData, 30000);
-    return () => clearInterval(id);
+    const fullId = setInterval(fetchAllData, 30000);
+    return () => clearInterval(fullId); // always clear on unmount
   }, [fetchAllData]);
+
+  // ── 2-second lightweight price/change/pct poll ──────────────────────────
+  // Fetches ONLY /api/nse/quote every 2000ms.
+  // Updates spot, change, changePct in marketData without re-fetching everything.
+  // Guards against NaN/undefined — keeps last known value on any error.
+  // clearInterval is called in the cleanup to avoid duplicate intervals.
+  useEffect(() => {
+    const pollPrice = async () => {
+      try {
+        const res = await fetch(`/api/nse/quote?symbol=NIFTY&_t=${Date.now()}`);
+        if (!res.ok) return; // keep last known value
+
+        const q = await res.json();
+
+        // Guard: reject NaN, undefined, 0, or error responses
+        if (
+          !q ||
+          q.error ||
+          typeof q.lastPrice !== 'number' ||
+          !isFinite(q.lastPrice) ||
+          q.lastPrice <= 0
+        ) return;
+
+        const newPrice  = q.lastPrice;
+        const newChange = typeof q.change  === 'number' && isFinite(q.change)  ? q.change  : 0;
+        const newPct    = typeof q.pChange === 'number' && isFinite(q.pChange) ? q.pChange : 0;
+
+        // Flash direction: green if up, red if down, none if same
+        const prev = lastTickPriceRef.current;
+        if (prev !== null && newPrice !== prev) {
+          const flashClass = newPrice > prev ? 'flash-green-text' : 'flash-red-text';
+          setPriceFlash(flashClass);
+          // Clear flash after 800ms
+          setTimeout(() => setPriceFlash(''), 800);
+        }
+        lastTickPriceRef.current = newPrice;
+
+        // Update ONLY spot/change/pct in marketData — preserve all other fields
+        setMarketData(prev => {
+          if (!prev) return prev; // don't create data from scratch here
+          return {
+            ...prev,
+            spot:       newPrice,
+            change:     newChange,
+            changePct:  newPct,
+          };
+        });
+
+        setLastUpdated(new Date().toLocaleTimeString('en-IN', { timeZone: 'Asia/Kolkata', hour12: true }));
+      } catch (err) {
+        // Network/parse error — keep last known values, do NOT reset state
+        console.warn('[Verdict Price Poll] Failed (keeping last value):', err.message);
+      }
+    };
+
+    pollPrice(); // immediate first tick
+    const priceId = setInterval(pollPrice, 2000); // 2-second interval
+    return () => clearInterval(priceId); // always clear on unmount
+  }, []); // empty deps — runs once, self-contained
 
   // Set the direction state when marketData is populated
   useEffect(() => {
@@ -1793,117 +2916,184 @@
               </div>
             ) : (
               <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
-                <div className="deduction-subcard" style={{
-                  background: 'rgba(19, 24, 31, 0.4)',
-                  border: '1px solid rgba(201, 168, 76, 0.15)',
-                  borderRadius: '8px',
-                  padding: '16px',
-                  boxShadow: 'inset 0 0 10px rgba(0,0,0,0.5)'
-                }}>
-                  <h3 style={{
-                    fontFamily: 'var(--font-display)',
-                    color: 'var(--gold)',
-                    fontSize: '14px',
-                    fontWeight: '600',
-                    letterSpacing: '1px',
-                    display: 'flex',
-                    alignItems: 'center',
-                    gap: '8px',
-                    marginBottom: '14px',
-                    borderBottom: '1px solid rgba(201, 168, 76, 0.2)',
-                    paddingBottom: '8px'
-                  }}>
-                    <span>🕵️‍♂️</span> Sherlock's Deductive Verdict
-                  </h3>
-
-                  {/* Source Banner */}
-                  {verdictResult && (
-                    <div className="verdict-source-banner"
-                         style={{
-                           background: verdictResult.source === 'CLAUDE_AI'
-                             ? 'rgba(0,255,136,0.08)'
-                             : 'rgba(255,170,0,0.08)',
-                           borderColor: verdictResult.source === 'CLAUDE_AI'
-                             ? 'rgba(0,255,136,0.3)'
-                             : 'rgba(255,170,0,0.3)'
-                         }}>
-                      <span className="source-dot" style={{
-                        color: verdictResult.source === 'CLAUDE_AI'
-                          ? '#00ff88' : '#ffaa00'
-                      }}>●</span>
-                      <span className="source-text">
-                        {verdictResult.source === 'CLAUDE_AI'
-                          ? `Sherlock AI Active — ${verdictResult.tokens} tokens`
-                          : `⚠ Rule-Based Fallback — ${verdictResult.reason || 'Fallback active'}`}
-                      </span>
-                      {verdictResult.source !== 'CLAUDE_AI' && (
-                        <button
-                          className="retry-claude-btn"
-                          onClick={handleRecalculate}
-                        >
-                          Retry AI
-                        </button>
+                <StrategyEngineDashboard mini={true} spotPrice={marketData.spot} />
+                {(() => {
+                  const parsed = parseVerdict(verdictResult?.text);
+                  const isBull = parsed.deduction.toLowerCase().includes('bull') || parsed.deduction.toLowerCase().includes('long') || parsed.deduction.toLowerCase().includes('ce');
+                  const isBear = parsed.deduction.toLowerCase().includes('bear') || parsed.deduction.toLowerCase().includes('short') || parsed.deduction.toLowerCase().includes('pe');
+                  const isBlocked = isBlockedText(parsed.deduction) || parsed.deduction.toLowerCase().includes('avoid');
+
+                  let verdictType = 'NEUTRAL';
+                  let verdictColor = '#cbd5e1';
+                  let verdictBg = 'rgba(255, 255, 255, 0.02)';
+                  let verdictBorder = 'rgba(255, 255, 255, 0.08)';
+
+                  if (isBlocked) {
+                    verdictType = 'SIGNAL BLOCKED';
+                    verdictColor = '#ff1744';
+                    verdictBg = 'rgba(255, 23, 68, 0.08)';
+                    verdictBorder = 'rgba(255, 23, 68, 0.3)';
+                  } else if (isBull) {
+                    verdictType = 'BULLISH ACCUMULATION';
+                    verdictColor = '#00e676';
+                    verdictBg = 'rgba(0, 230, 118, 0.08)';
+                    verdictBorder = 'rgba(0, 230, 118, 0.3)';
+                  } else if (isBear) {
+                    verdictType = 'BEARISH DISTRIBUTION';
+                    verdictColor = '#ff1744';
+                    verdictBg = 'rgba(255, 23, 68, 0.08)';
+                    verdictBorder = 'rgba(255, 23, 68, 0.3)';
+                  }
+
+                  return (
+                    <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
+                      {/* Deduction Status Card */}
+                      <div style={{
+                        background: 'rgba(11, 15, 25, 0.6)',
+                        border: '1px solid rgba(255, 255, 255, 0.06)',
+                        borderRadius: '10px',
+                        padding: '20px',
+                        boxShadow: '0 4px 20px rgba(0,0,0,0.3)',
+                        animation: 'pmi-fade-in 0.3s ease both'
+                      }}>
+                        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '14px', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '10px' }}>
+                          <span style={{ fontSize: '11px', color: '#64748b', fontWeight: 'bold', fontFamily: 'monospace', letterSpacing: '0.5px' }}>
+                            🕵️‍♂️ SHERLOCK'S DEDUCTION
+                          </span>
+                          <span style={{
+                            fontSize: '10px',
+                            fontWeight: 'bold',
+                            color: verdictColor,
+                            background: verdictBg,
+                            border: `1px solid ${verdictBorder}`,
+                            padding: '2px 8px',
+                            borderRadius: '4px',
+                            textTransform: 'uppercase',
+                            letterSpacing: '0.5px'
+                          }}>
+                            {verdictType}
+                          </span>
+                        </div>
+
+                        {/* Source Banner */}
+                        {verdictResult && (
+                          <div className="verdict-source-banner" style={{
+                            background: verdictResult.source === 'CLAUDE_AI' ? 'rgba(0,255,136,0.04)' : 'rgba(255,170,0,0.04)',
+                            borderColor: verdictResult.source === 'CLAUDE_AI' ? 'rgba(0,255,136,0.15)' : 'rgba(255,170,0,0.15)',
+                            padding: '8px 12px',
+                            borderRadius: '6px',
+                            border: '1px solid',
+                            display: 'flex',
+                            alignItems: 'center',
+                            gap: '8px',
+                            fontSize: '11px',
+                            color: '#94a3b8',
+                            marginBottom: '14px'
+                          }}>
+                            <span style={{ color: verdictResult.source === 'CLAUDE_AI' ? '#00ff88' : '#ffaa00' }}>●</span>
+                            <span>
+                              {verdictResult.source === 'CLAUDE_AI'
+                                ? `Sherlock AI Active — ${verdictResult.tokens} tokens`
+                                : `⚠ Rule-Based Fallback — ${verdictResult.reason || 'Fallback active'}`}
+                            </span>
+                            {verdictResult.source !== 'CLAUDE_AI' && (
+                              <button
+                                className="retry-claude-btn"
+                                onClick={handleRecalculate}
+                                style={{ marginLeft: 'auto', background: 'none', border: 'none', color: 'var(--gold)', cursor: 'pointer', fontSize: '11px', fontWeight: 'bold' }}
+                              >
+                                Retry AI
+                              </button>
+                            )}
+                          </div>
+                        )}
+
+                        <div style={{
+                          fontSize: '13px',
+                          lineHeight: '1.625',
+                          color: '#cbd5e1',
+                          whiteSpace: 'pre-line',
+                          wordBreak: 'break-words'
+                        }}>
+                          {loading ? 'Analyzing parameters and generating verdict...' : parsed.deduction}
+                        </div>
+                      </div>
+
+                      {/* Technical Evidence Grid */}
+                      {parsed.evidence && parsed.evidence.length > 0 && (
+                        <div>
+                          <span style={{ fontSize: '10px', color: '#64748b', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: '0.5px', display: 'block', marginBottom: '10px' }}>
+                            📊 TECHNICAL EVIDENCE MATRIX
+                          </span>
+                          <div style={{
+                            display: 'grid',
+                            gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
+                            gap: '12px'
+                          }}>
+                            {parsed.evidence.map((item, idx) => {
+                              const detail = parseEvidenceItem(item);
+                              return (
+                                <div key={idx} style={{
+                                  background: 'rgba(11, 15, 25, 0.65)',
+                                  border: '1px solid rgba(255, 255, 255, 0.05)',
+                                  borderRadius: '8px',
+                                  padding: '14px',
+                                  boxShadow: '0 4px 12px rgba(0,0,0,0.25)',
+                                  transition: 'transform 0.2s ease',
+                                }}>
+                                  <div style={{ fontSize: '9px', color: 'var(--gold-dim)', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: '1px', marginBottom: '6px' }}>
+                                    {detail.label}
+                                  </div>
+                                  <div style={{ fontSize: '12px', color: '#cbd5e1', lineHeight: '1.5', fontFamily: 'monospace', wordBreak: 'break-word', whiteSpace: 'pre-line' }}>
+                                    {detail.value}
+                                  </div>
+                                </div>
+                              );
+                            })}
+                          </div>
+                        </div>
+                      )}
+
+                      {/* Execution Checklist Box */}
+                      {parsed.checklist && (
+                        <div style={{
+                          background: 'rgba(11, 15, 25, 0.75)',
+                          border: '1px solid rgba(255, 255, 255, 0.05)',
+                          borderLeft: '4px solid var(--gold)',
+                          borderRadius: '8px',
+                          padding: '18px 20px',
+                          marginTop: '4px',
+                          boxShadow: '0 4px 16px rgba(0,0,0,0.3)'
+                        }}>
+                          <span style={{ fontSize: '11px', color: 'var(--gold)', fontWeight: 'bold', display: 'block', marginBottom: '8px', fontFamily: 'monospace', letterSpacing: '0.5px' }}>
+                            📋 OPTIONS EXECUTION GATEWAYS
+                          </span>
+                          <div style={{
+                            fontSize: '12px',
+                            lineHeight: '1.6',
+                            color: '#cbd5e1',
+                            whiteSpace: 'pre-line',
+                            wordBreak: 'break-words'
+                          }}>
+                            <ReactMarkdown
+                              remarkPlugins={[remarkGfm]}
+                              components={{
+                                p: ({children}) => <p style={{ margin: '0 0 8px 0' }}>{children}</p>,
+                                li: ({children}) => <li style={{ marginBottom: '6px' }}>{children}</li>,
+                                ul: ({children}) => <ul style={{ paddingLeft: '16px', margin: '4px 0 8px 0' }}>{children}</ul>,
+                                ol: ({children}) => <ol style={{ paddingLeft: '16px', margin: '4px 0 8px 0' }}>{children}</ol>,
+                                strong: ({children}) => <strong style={{ color: 'var(--gold-bright)', fontWeight: 700 }}>{children}</strong>,
+                                code: ({children}) => <code style={{ background: 'rgba(255,200,0,0.08)', color: 'var(--gold)', padding: '1px 5px', borderRadius: 3, fontSize: 11, fontFamily: 'var(--font-mono)' }}>{children}</code>,
+                              }}
+                            >
+                              {parsed.checklist}
+                            </ReactMarkdown>
+                          </div>
+                        </div>
                       )}
                     </div>
-                  )}
-
-                  <div style={{
-                    fontFamily: 'system-ui, -apple-system, sans-serif',
-                    fontSize: '13px',
-                    lineHeight: '1.625',
-                    color: '#cbd5e1',
-                    wordBreak: 'break-word',
-                    letterSpacing: 'normal'
-                  }}>
-                    <ReactMarkdown
-                      remarkPlugins={[remarkGfm]}
-                      components={{
-                        h1: ({children}) => <h1 style={{color:'var(--gold)',fontSize:16,fontWeight:700,margin:'0 0 8px'}}>{children}</h1>,
-                        h2: ({children}) => <h2 style={{color:'var(--gold)',fontSize:14,fontWeight:700,margin:'12px 0 6px'}}>{children}</h2>,
-                        h3: ({children}) => <h3 style={{color:'var(--gold-dim)',fontSize:13,fontWeight:600,margin:'10px 0 4px'}}>{children}</h3>,
-                        p: ({children}) => {
-                          const textContent = React.Children.toArray(children).join('');
-                          const blocked = isBlockedText(textContent);
-                          return (
-                            <p style={{
-                              ...paragraphStyle,
-                              color: blocked ? '#ef4444' : '#cbd5e1',
-                              fontWeight: blocked ? '700' : 'normal',
-                              background: blocked ? 'rgba(239, 68, 68, 0.08)' : 'transparent',
-                              border: blocked ? '1px solid rgba(239, 68, 68, 0.2)' : 'none',
-                              padding: blocked ? '10px 14px' : '0',
-                              borderRadius: blocked ? '6px' : '0',
-                              margin: '4px 0 8px'
-                            }}>
-                              {children}
-                            </p>
-                          );
-                        },
-                        strong: ({children}) => {
-                          const textContent = React.Children.toArray(children).join('');
-                          const blocked = isBlockedText(textContent);
-                          return (
-                            <strong style={{
-                              color: blocked ? '#ef4444' : 'var(--gold-bright)',
-                              fontWeight: 700
-                            }}>
-                              {children}
-                            </strong>
-                          );
-                        },
-                        em:     ({children}) => <em style={{color:'#afd8ff',fontStyle:'italic'}}>{children}</em>,
-                        li: ({children}) => <li style={listItemStyle}>{children}</li>,
-                        ul: ({children}) => <ul style={{paddingLeft:18,margin:'4px 0 8px'}}>{children}</ul>,
-                        ol: ({children}) => <ol style={{paddingLeft:18,margin:'4px 0 8px'}}>{children}</ol>,
-                        code: ({children}) => <code style={{background:'rgba(255,200,0,0.1)',color:'var(--gold)',padding:'1px 5px',borderRadius:3,fontSize:11,fontFamily:'var(--font-mono)',fontStyle:'normal'}}>{children}</code>,
-                        blockquote: ({children}) => <blockquote style={{borderLeft:'3px solid var(--gold-dim)',paddingLeft:12,margin:'8px 0',color:'#aaa',fontStyle:'italic'}}>{children}</blockquote>,
-                        hr: () => <hr style={{border:'none',borderTop:'1px solid rgba(255,200,0,0.2)',margin:'12px 0'}}/>,
-                      }}
-                    >
-                      {loading ? 'Analyzing parameters and generating verdict...' : (verdictResult?.text || 'Waiting for calculations...')}
-                    </ReactMarkdown>
-                  </div>
-                </div>
+                  );
+                })()}
               </div>
             )}
           </div>
@@ -2754,6 +3944,8 @@
             </span>
           </div>
 
+          <StrategyEngineDashboard mini={true} spotPrice={data._raw?.nifty_current || 24200} />
+
           {/* Opening expectation */}
           <div className="plan-section">
             <h4>🔔 Opening Expectation</h4>
@@ -2919,6 +4111,30 @@
   // Navigation
   const [activeTab, setActiveTab] = useState('clueBoard');
 
+  // Proxy Health state (Issue 1)
+  const [proxyHealth, setProxyHealth] = useState('CHECKING');
+
+  useEffect(() => {
+    const pollHealth = async () => {
+      try {
+        const res = await fetch('/api/health');
+        if (res.ok) {
+          const data = await res.json();
+          if (data.status === 'ok' || data.status === 'OK') {
+            setProxyHealth('ONLINE');
+            return;
+          }
+        }
+        setProxyHealth('OFFLINE');
+      } catch (err) {
+        setProxyHealth('OFFLINE');
+      }
+    };
+    pollHealth();
+    const id = setInterval(pollHealth, 10000);
+    return () => clearInterval(id);
+  }, []);
+
   // Market Status Hook
   const marketStatus = useMarketStatus();
 
@@ -3003,10 +4219,12 @@
   const chatEndRef = useRef(null);
 
   // Tick Flashing States & Refs
-  const prevSpotPriceRef = useRef(indicators.spot_price);
+  const prevSpotPriceRef  = useRef(indicators.spot_price);
   const prevOptionsLTPRef = useRef({});
+  const prevTickerPricesRef = useRef({}); // tracks previous ticker prices for flash
   const [spotPriceFlashClass, setSpotPriceFlashClass] = useState('');
-  const [optionFlashClasses, setOptionFlashClasses] = useState({});
+  const [optionFlashClasses, setOptionFlashClasses]   = useState({});
+  const [tickerFlash, setTickerFlash]                 = useState({}); // { 'NIFTY 50': 'flash-green-text', ... }
 
   // RR Calculator Inputs
   const [rrForm, setRrForm] = useState({
@@ -3156,15 +4374,15 @@
     chatEndRef.current?.scrollIntoView({ behavior: 'smooth' });
   }, [chatMessages]);
 
-  // ── Market-Aware Silent Polling for Live Market Data ────────────────────
-  // OPEN  → poll every 1 second (realistic live ticks)
-  // CLOSED → poll every 30 seconds (frozen prices, just refresh chart)
+  // ── Chart/Indicator Poll: 15s (open) / 60s (closed) ────────────────────
+  // Slower because nseData.quote (useNSELiveData) now handles 2s price ticks.
+  // clearInterval in cleanup prevents duplicate intervals on re-render.
   useEffect(() => {
-    const pollInterval = marketStatus.status === 'OPEN' ? 1000 : 30000;
+    const pollInterval = marketStatus.status === 'OPEN' ? 15000 : 60000;
     const interval = setInterval(() => {
       fetchMarketData(true);
     }, pollInterval);
-    return () => clearInterval(interval);
+    return () => clearInterval(interval); // always clear on unmount or dep change
   }, [selectedAsset, period, chartInterval, marketStatus.status]);
 
   // Reset selectedExpiry on asset change
@@ -3287,44 +4505,67 @@
     }));
   }, [nseData.quote, nseData.pcr, nseData.maxPain]);
 
-  // ── NSE Indices → Full Ticker Tape Rebuild ───────────────────────────────
-  // When we have live NSE indices data, rebuild ALL ticker tape items
-  // with real prices instead of hardcoded values.
+  // ── NSE Indices → Ticker Tape Rebuild + Per-symbol Flash ────────────────
+  // Runs every ~2s (useNSELiveData now polls indices every 2s).
+  // Detects price direction per symbol → applies green/red flash for 800ms.
+  // Last-known values are preserved — never writes NaN, undefined, or 0.
   useEffect(() => {
     if (!nseData.indices || nseData.indices.length === 0) return;
 
     const indexMap = {
-      'NIFTY 50':          'NIFTY 50',
-      'NIFTY BANK':        'BANK NIFTY',
-      'NIFTY FIN SERVICE': 'FINNIFTY',
-      'NIFTY MIDCAP SELECT':'MIDCAP 150',
-      'INDIA VIX':         'INDIA VIX',
-      'NIFTY NEXT 50':     'NIFTY NEXT 50',
+      'NIFTY 50':            'NIFTY 50',
+      'NIFTY BANK':          'BANK NIFTY',
+      'NIFTY FIN SERVICE':   'FINNIFTY',
+      'NIFTY MIDCAP SELECT': 'MIDCAP 150',
+      'INDIA VIX':           'INDIA VIX',
+      'NIFTY NEXT 50':       'NIFTY NEXT 50',
     };
 
-    // Build a lookup: display name → index data
+    // Build display-name → index data lookup
     const lookup = {};
     nseData.indices.forEach(idx => {
       const mapped = indexMap[idx.name] || idx.name;
       lookup[mapped] = idx;
     });
 
-    setTickerTape(prev => {
-      // Update existing items with real data where available
-      const updated = prev.map(item => {
-        const match = lookup[item.symbol];
-        if (match && match.last > 0) {
-          return {
-            ...item,
-            price:  match.last.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 }),
-            change: `${match.percentChange >= 0 ? '+' : ''}${match.percentChange.toFixed(2)}%`,
-            realData: true,
-          };
-        }
-        return item;
-      });
-      return updated;
+    // ── Detect flash per symbol ──────────────────────────────────────────
+    const newFlash = {};
+    let anyFlash = false;
+    Object.entries(lookup).forEach(([displayName, idx]) => {
+      const newPrice  = idx.last;
+      const prevPrice = prevTickerPricesRef.current[displayName];
+      if (
+        typeof newPrice === 'number' && isFinite(newPrice) && newPrice > 0 &&
+        prevPrice !== undefined && newPrice !== prevPrice
+      ) {
+        newFlash[displayName] = newPrice > prevPrice ? 'flash-green-text' : 'flash-red-text';
+        anyFlash = true;
+      }
+      // Store new price only if valid
+      if (typeof newPrice === 'number' && isFinite(newPrice) && newPrice > 0) {
+        prevTickerPricesRef.current[displayName] = newPrice;
+      }
     });
+
+    if (anyFlash) {
+      setTickerFlash(newFlash);
+      setTimeout(() => setTickerFlash({}), 800); // clear flash after 800ms
+    }
+
+    // ── Update ticker tape values ────────────────────────────────────────
+    setTickerTape(prev => prev.map(item => {
+      const match = lookup[item.symbol];
+      // Only update if match has a valid, non-zero price
+      if (match && typeof match.last === 'number' && isFinite(match.last) && match.last > 0) {
+        return {
+          ...item,
+          price:    match.last.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 }),
+          change:   `${match.percentChange >= 0 ? '+' : ''}${match.percentChange.toFixed(2)}%`,
+          realData: true,
+        };
+      }
+      return item; // keep last known value — never show NaN or blank
+    }));
   }, [nseData.indices]);
 
   useEffect(() => {
@@ -3696,14 +4937,24 @@
   // Run FII/DII AI Flow Analysis
   const handleAnalyzeFiiDii = async () => {
     if (fiiDiiData.length === 0) return;
-    
-    // Cache check (30 minutes)
-    if (fiiDiiAnalysis && fiiDiiAnalysisTime && (Date.now() - fiiDiiAnalysisTime < 30 * 60 * 1000)) {
-      console.log("[FII/DII] Using cached AI verdict (30 min cache)");
-      return;
-    }
-
+
+    // Reset/clear old state to a skeleton placeholder to prevent layout shifts.
+    setFiiDiiAnalysis({
+      combined_bias: "CALCULATING...",
+      confidence: 0,
+      fii_signal: "CALCULATING...",
+      dii_signal: "CALCULATING...",
+      action: "CALCULATING...",
+      fii_interpretation: "Sherlock is examining the ledger...",
+      dii_interpretation: "Sherlock is examining the ledger...",
+      market_implication: "Sherlock is examining the ledger...",
+      intraday_bias: "NEUTRAL",
+      swing_bias: "NEUTRAL",
+      momentum: "STEADY",
+      red_flag: "None"
+    });
     setAnalyzingFiiDii(true);
+
     try {
       const todayFlow = fiiDiiData[0];
       const res = await fetch('/api/fii-dii/analyze', {
@@ -3717,21 +4968,74 @@
           history: fiiDiiData.slice(1)
         })
       });
-      if (res.ok) {
-        const json = await res.json();
-        // Normalize keys for UI backward compatibility
-        const normalized = {
-          ...json,
-          combined_bias: json.verdict || json.combined_bias || "NEUTRAL",
-          action: json.recommended_action || json.action || "HOLD",
-          red_flag: (Array.isArray(json.risk_flags) && json.risk_flags.length > 0) ? json.risk_flags.join(', ') : (typeof json.red_flag === 'string' ? json.red_flag : "None"),
-          confidence: json.confidence > 1 ? json.confidence / 100 : (json.confidence || 0.5)
-        };
-        setFiiDiiAnalysis(normalized);
-        setFiiDiiAnalysisTime(Date.now());
+
+      if (!res.ok) {
+        throw new Error("Failed to connect to flow analysis engine.");
       }
+
+      const reader = res.body.getReader();
+      const decoder = new TextDecoder();
+      let accumulatedAnalysis = {
+        combined_bias: "NEUTRAL",
+        confidence: 0,
+        fii_signal: "NEUTRAL",
+        dii_signal: "NEUTRAL",
+        action: "HOLD",
+        fii_interpretation: "",
+        dii_interpretation: "",
+        market_implication: "",
+        intraday_bias: "NEUTRAL",
+        swing_bias: "NEUTRAL",
+        momentum: "STEADY",
+        red_flag: "None"
+      };
+
+      while (true) {
+        const { done, value } = await reader.read();
+        if (done) break;
+
+        const chunk = decoder.decode(value);
+        const lines = chunk.split('\n').filter(l => l.startsWith('data: '));
+
+        for (const line of lines) {
+          const dataStr = line.replace('data: ', '').trim();
+          if (dataStr === '[DONE]') break;
+          try {
+            const parsed = JSON.parse(dataStr);
+            if (parsed.type === 'metadata') {
+              const meta = parsed.data;
+              accumulatedAnalysis = {
+                ...accumulatedAnalysis,
+                combined_bias: meta.verdict || "NEUTRAL",
+                action: meta.recommended_action || "HOLD",
+                red_flag: (Array.isArray(meta.risk_flags) && meta.risk_flags.length > 0) ? meta.risk_flags.join(', ') : (meta.red_flag || "None"),
+                confidence: meta.confidence > 1 ? meta.confidence / 100 : (meta.confidence || 0.5),
+                fii_signal: meta.fii_signal || "NEUTRAL",
+                dii_signal: meta.dii_signal || "NEUTRAL",
+                intraday_bias: meta.intraday_bias || "NEUTRAL",
+                swing_bias: meta.swing_bias || "NEUTRAL",
+                momentum: meta.momentum || "STEADY"
+              };
+              setFiiDiiAnalysis({ ...accumulatedAnalysis });
+            } else if (parsed.type === 'delta') {
+              const { field, text } = parsed;
+              accumulatedAnalysis[field] = (accumulatedAnalysis[field] || '') + text;
+              setFiiDiiAnalysis({ ...accumulatedAnalysis });
+            }
+          } catch (err) {
+            console.error("Error parsing analysis token chunk:", err);
+          }
+        }
+      }
+      setFiiDiiAnalysisTime(Date.now());
     } catch (e) {
       console.error('Error analyzing FII/DII flows:', e);
+      setFiiDiiAnalysis(prev => prev ? {
+        ...prev,
+        fii_interpretation: "Watson, it seems our connection to the flow engines has dropped.",
+        dii_interpretation: "Watson, it seems our connection to the flow engines has dropped.",
+        market_implication: "Flow interpretation is currently unavailable."
+      } : null);
     } finally {
       setAnalyzingFiiDii(false);
     }
@@ -4585,6 +5889,18 @@
           <div className="meta-item">
             <span className="badge-live">Live</span>
           </div>
+          <div className="meta-item">
+            <span style={{
+              background: proxyHealth === 'ONLINE' ? 'rgba(34,197,94,0.15)' : 'rgba(239,68,68,0.15)',
+              border: proxyHealth === 'ONLINE' ? '1px solid rgba(34,197,94,0.4)' : '1px solid rgba(239,68,68,0.4)',
+              borderRadius: 4, padding: '2px 8px', fontSize: 10,
+              color: proxyHealth === 'ONLINE' ? '#22c55e' : '#ef4444',
+              fontWeight: 700, letterSpacing: '0.5px', textTransform: 'uppercase',
+              transition: 'all 0.3s ease'
+            }}>
+              🔌 PROXY: {proxyHealth}
+            </span>
+          </div>
           {nseData.isLive && (
             <div className="meta-item">
               <span style={{
@@ -4625,14 +5941,20 @@
       <div className="ticker-tape">
         <div className="ticker-wrap">
           <div className="ticker-container">
-            {tickerTape.concat(tickerTape).map((item, idx) => (
-              <div className="ticker-item" key={idx}>
-                <span className="ticker-name">{item.symbol}</span>
-                <span className="ticker-val" style={{ color: item.change.startsWith('+') ? 'var(--green)' : 'var(--red)' }}>
-                  {item.price} ({item.change})
-                </span>
-              </div>
-            ))}
+            {tickerTape.concat(tickerTape).map((item, idx) => {
+              const flashCls = tickerFlash[item.symbol] || '';
+              return (
+                <div className="ticker-item" key={idx}>
+                  <span className="ticker-name">{item.symbol}</span>
+                  <span
+                    className={`ticker-val ${flashCls}`}
+                    style={{ color: item.change.startsWith('+') ? 'var(--green)' : 'var(--red)' }}
+                  >
+                    {item.price} ({item.change})
+                  </span>
+                </div>
+              );
+            })}
           </div>
         </div>
       </div>
@@ -4669,7 +5991,7 @@
             🔬 Backtester
           </button>
           <button id="sherlock-bot-tab" className={`nav-tab ${activeTab === 'sherlockBot' ? 'active' : ''}`} onClick={() => setActiveTab('sherlockBot')}>
-            💬 Sherlock Bot
+            🕵️ Holmes AI
           </button>
         </nav>
 
@@ -4723,250 +6045,13 @@
               </div>
             </div>
 
-            {/* Market Closed Banner */}
-            {marketStatus.status === 'CLOSED' && (
-              <div className="market-closed-banner">
-                <span className="closed-icon">🔒</span>
-                <span>Market Closed — Displaying last closing price</span>
-                <span className="closed-reason">{marketStatus.reason}</span>
-              </div>
-            )}
-
-            {/* BSE/SENSEX info note — PCR and EMA from NSE not available */}
-            {selectedAsset === '^BSESN' && (
-              <div style={{
-                display: 'flex', alignItems: 'center', gap: 10,
-                padding: '10px 16px', marginBottom: 14,
-                background: 'rgba(59,130,246,0.07)',
-                border: '1px solid rgba(59,130,246,0.25)',
-                borderLeft: '3px solid #3b82f6',
-                borderRadius: 6, fontSize: 12, color: '#93c5fd'
-              }}>
-                <span style={{ fontSize: 15 }}>ℹ️</span>
-                <span>
-                  <strong>SENSEX (BSE)</strong> — Showing price & RSI data from Yahoo Finance.
-                  PCR, Max Pain, and Option Chain are NSE-only and are not available for BSE indices.
-                </span>
-              </div>
-            )}
-
-            {/* Setup Approval Banner */}
-            {(() => {
-              const approved = checklistResult.totalScore >= 90;
-              const statusClass = approved ? 'approved' : 'blocked';
-              const badgeClass = approved ? 'pulse-green' : 'pulse-red';
-              const title = approved ? 'A+ SETUP APPROVED' : 'SETUP BLOCKED — DEDUCTIVE CONFIDENCE UNDER 90%';
-              const desc = approved 
-                ? `Deductive score is ${checklistResult.totalScore}/100. Institutional conditions met. Execute with strict risk limits.` 
-                : `Deductive score is ${checklistResult.totalScore}/100. Sherlock Holmes deductions do not support entry. "NO TRADE TODAY" is the active stance.`;
-
-              return (
-                <div className={`setup-approval-banner ${statusClass}`}>
-                  <div className="setup-body" style={{ flex: 1 }}>
-                    <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 4 }}>
-                      <span className={`status-badge ${badgeClass}`}>{approved ? 'PASS' : 'HOLD'}</span>
-                      <h3 style={{ margin: 0 }}>{title}</h3>
-                    </div>
-                    <p style={{ margin: 0 }}>{desc}</p>
-                  </div>
-                  <div style={{ fontSize: '20px', fontWeight: '800', fontFamily: 'var(--font-mono)', color: approved ? '#22c55e' : '#ef4444' }}>
-                    {checklistResult.totalScore}%
-                  </div>
-                </div>
-              );
-            })()}
-
-            {/* Metrics cards row */}
-            <div className="metric-grid">
-              <div className="metric-card" style={{ borderTop: `2px solid ${nseData.isLive ? '#00c9a7' : 'var(--gold)'}` }}>
-                <div className="metric-label" style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
-                  <span>Spot Price</span>
-                  <span style={{ fontSize: 9, padding: '1px 5px', borderRadius: 3, fontWeight: 700,
-                    background: nseData.isLive ? 'rgba(0,201,167,0.15)' : nseData.error ? 'rgba(255,170,0,0.1)' : 'rgba(201,168,76,0.1)',
-                    color: nseData.isLive ? '#00c9a7' : nseData.error ? '#ffaa00' : 'var(--gold)'
-                  }}>
-                    {nseData.isLive ? '● NSE' : nseData.error ? 'YF' : 'SIM'}
-                  </span>
-                </div>
-                <div className={`metric-value ${spotPriceFlashClass}`} style={{ fontSize: 20 }}>
-                  ₹{indicators.spot_price.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
-                </div>
-                <div className="metric-sub" style={{ color: indicators.price_change_pct >= 0 ? 'var(--green)' : 'var(--red)', fontWeight: 600, fontSize: 11 }}>
-                  {indicators.price_change_pct >= 0 ? '▲' : '▼'} {indicators.price_change_pct >= 0 ? '+' : ''}{indicators.price_change_pct.toFixed(2)}%
-                </div>
-              </div>
-              <div className="metric-card">
-                <div className="metric-label">RSI (14)</div>
-                <div className="metric-value metric-val-gold">{indicators.rsi.toFixed(1)}</div>
-                <div className="metric-sub">{indicators.rsi > 70 ? 'Overbought Limit' : indicators.rsi < 30 ? 'Oversold Accumulation' : 'Neutral Range'}</div>
-              </div>
-              <div className="metric-card">
-                <div className="metric-label">EMA 9 / 21</div>
-                <div className="metric-value metric-val-teal">{indicators.ema_status.includes('Bullish') ? 'BULLISH' : 'BEARISH'}</div>
-                <div className="metric-sub">{indicators.ema_status}</div>
-              </div>
-              <div className="metric-card">
-                <div className="metric-label">VWAP VALUE</div>
-                {indicators.vwapValid ? (
-                  <>
-                    <div className="metric-value">
-                      ₹{indicators.vwap?.toLocaleString('en-IN', {
-                        minimumFractionDigits: 2
-                      })}
-                    </div>
-                    <div className={`metric-sub ${
-                      indicators.vwapPosition === 'ABOVE' ? 'green' : 'red'
-                    }`}>
-                      {indicators.vwapPosition === 'ABOVE'
-                        ? '↑ Trading above'
-                        : '↓ Trading below'}
-                    </div>
-                  </>
-                ) : (
-                  <>
-                    <div className="metric-value muted">—</div>
-                    <div className="metric-sub amber">
-                      Calculating...
-                    </div>
-                  </>
-                )}
-              </div>
-              <div className="metric-card" style={{ borderTop: `2px solid ${nseData.pcr ? (nseData.pcr > 1.2 ? 'var(--green)' : nseData.pcr < 0.8 ? 'var(--red)' : 'var(--gold)') : 'var(--border)'}` }}>
-                <div className="metric-label" style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
-                  <span>Put-Call Ratio (PCR)</span>
-                  {nseData.optionChain && <span style={{ fontSize: 9, color: '#00c9a7', fontWeight: 700 }}>● LIVE</span>}
-                </div>
-                <div className={`metric-value ${indicators.pcr > 1.2 ? 'metric-val-green' : indicators.pcr < 0.8 ? 'metric-val-red' : 'metric-val-gold'}`}>
-                  {indicators.pcr.toFixed(2)}
-                </div>
-                <div className="metric-sub">{indicators.pcr > 1.2 ? 'Bullish Write support' : indicators.pcr < 0.8 ? 'Bearish Call resistance' : 'Balanced Sentiment'}</div>
-              </div>
-              <div className="metric-card" style={{ borderTop: `2px solid ${nseData.maxPain ? 'var(--gold)' : 'var(--border)'}` }}>
-                <div className="metric-label" style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
-                  <span>Max Pain Strike</span>
-                  {nseData.maxPain && <span style={{ fontSize: 9, color: '#c9a84c', fontWeight: 700 }}>OI Calc</span>}
-                </div>
-                <div className="metric-value">₹{(indicators.max_pain || 0).toLocaleString('en-IN')}</div>
-                <div className="metric-sub">Expected Range Center</div>
-              </div>
-            </div>
-
-            {/* Grid 2 Columns: Chart & Matrix */}
-            <div className="grid-main">
-              {/* Left Column: Live Chart */}
-              <MultiTimeframeView symbol={selectedAsset} />
-
-              {/* Right Column: Indicator Strength Matrix */}
-              <div>
-                <div className="checklist-container">
-                  <div className="checklist-title-row">
-                    <h4>📋 Institutional Checklist</h4>
-                    <span style={{ fontSize: 11, fontWeight: 700, color: 'var(--gold)' }}>
-                      Score: {checklistResult.totalScore}/100
-                    </span>
-                  </div>
-                  
-                  <div className="confidence-bar-wrapper" style={{ margin: '0 0 14px 0' }}>
-                    <div className="confidence-bar-track" style={{ height: 6 }}>
-                      <div
-                        className="confidence-bar-fill"
-                        style={{
-                          width: `${checklistResult.totalScore}%`,
-                          background: checklistResult.totalScore >= 90 ? '#22c55e' : '#ef4444'
-                        }}
-                      />
-                    </div>
-                  </div>
-
-                  <div className="checklist-grid">
-                    {checklistResult.items.map((item, idx) => (
-                      <div className="checklist-item" key={idx}>
-                        <span className={`checklist-icon ${item.passed ? 'pass' : 'fail'}`}>
-                          {item.passed ? '✓' : '✗'}
-                        </span>
-                        <div className="checklist-body">
-                          <div className="checklist-header">
-                            <span className="checklist-factor">{item.name}</span>
-                            <span className="checklist-weight">+{item.score}/{item.maxScore}</span>
-                          </div>
-                          <span className="checklist-detail">{item.details}</span>
-                        </div>
-                      </div>
-                    ))}
-                  </div>
-                </div>
-
-                <div className="card">
-                  <div className="card-header">
-                    <div className="card-title">🔍 Quick Actions</div>
-                  </div>
-                  <div className="card-body" style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
-                    <button className="btn btn-gold" onClick={() => { setActiveTab('sherlockAnalysis'); handleRequestVerdict(); }} style={{ width: '100%' }}>
-                      🔬 Deduct Asset Analytics
-                    </button>
-                    <button className="btn btn-secondary" onClick={() => { setActiveTab('rrCalculator'); }} style={{ width: '100%' }}>
-                      ⚖️ Risk-Reward Calculator
-                    </button>
-                  </div>
-                </div>
-              </div>
-            </div>
-
-            {/* Sectoral Heatmap — rebuilt from live NSE indices + real sector data */}
-            <div className="card">
-              <div className="card-header">
-                <div className="card-title">📊 Sectoral Market Map</div>
-                <div style={{ fontSize: 10, color: nseData.isLive ? '#00c9a7' : 'var(--text-muted)' }}>
-                  {nseData.isLive ? '● Live NSE Data' : 'Simulated — Start NSE proxy for real data'}
-                </div>
-              </div>
-              <div className="card-body">
-                {/* Real NSE indices as large tiles (when available) */}
-                {nseData.indices && nseData.indices.length > 0 && (
-                  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(160px, 1fr))', gap: 10, marginBottom: 16 }}>
-                    {nseData.indices.map((idx, i) => {
-                      const pct = idx.percentChange ?? 0;
-                      let cls = 'neutral';
-                      if (pct > 1.0) cls = 'pos-strong';
-                      else if (pct > 0) cls = 'pos-mild';
-                      else if (pct < -1.0) cls = 'neg-strong';
-                      else if (pct < 0) cls = 'neg-mild';
-                      return (
-                        <div className={`heatmap-item ${cls}`} key={i} style={{ padding: '12px 14px' }}>
-                          <div className="heatmap-ticker" style={{ fontSize: 11, marginBottom: 4 }}>{idx.name}</div>
-                          <div style={{ fontSize: 15, fontWeight: 700, fontFamily: 'IBM Plex Mono, monospace', marginBottom: 2 }}>
-                            {(idx.last || 0).toLocaleString('en-IN', { maximumFractionDigits: 2 })}
-                          </div>
-                          <div className="heatmap-change" style={{ fontSize: 11 }}>
-                            {pct >= 0 ? '+' : ''}{pct.toFixed(2)}%
-                          </div>
-                        </div>
-                      );
-                    })}
-                  </div>
-                )}
-                {/* Sector breakdown (Flask backend data or fallback) */}
-                <div style={{ fontSize: 10, color: 'var(--text-muted)', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '0.5px' }}>
-                  {nseData.isLive ? 'Sector-wise breakdown' : 'Estimated sector change (start NSE proxy for real data)'}
-                </div>
-                <div className="heatmap-grid">
-                  {heatmapData.map((item, idx) => {
-                    let cls = 'neutral';
-                    if (item.change > 1.0) cls = 'pos-strong';
-                    else if (item.change > 0) cls = 'pos-mild';
-                    else if (item.change < -1.0) cls = 'neg-strong';
-                    else if (item.change < 0) cls = 'neg-mild';
-
-                    return (
-                      <div className={`heatmap-item ${cls}`} key={idx}>
-                        <div className="heatmap-ticker">{item.symbol}</div>
-                        <div className="heatmap-change">{item.change >= 0 ? '+' : ''}{item.change.toFixed(2)}%</div>
-                      </div>
-                    );
-                  })}
-                </div>
-              </div>
-            </div>
+            <ClueBoardTab
+              selectedAsset={selectedAsset}
+              period={period}
+              chartInterval={chartInterval}
+              direction={rrForm.direction}
+              rrRatio={parseFloat(rrForm.target1) && parseFloat(rrForm.entry) && parseFloat(rrForm.sl) ? Math.abs(parseFloat(rrForm.target1) - parseFloat(rrForm.entry)) / (Math.abs(parseFloat(rrForm.entry) - parseFloat(rrForm.sl)) || 1) : 2.0}
+            />
           </div>
         )}
 
@@ -6263,14 +7348,14 @@
                             <h2 style={{
                               margin: 0,
                               fontFamily: 'Cinzel, serif',
-                              color: fiiDiiAnalysis.combined_bias === 'BULLISH' ? 'var(--green)' : fiiDiiAnalysis.combined_bias === 'BEARISH' ? 'var(--red)' : 'var(--gold)',
+                              color: fiiDiiAnalysis.combined_bias.includes('BULLISH') ? 'var(--green)' : fiiDiiAnalysis.combined_bias.includes('BEARISH') ? 'var(--red)' : 'var(--gold)',
                               fontSize: 28,
                               display: 'flex',
                               alignItems: 'center',
                               justifyContent: 'center',
                               gap: 10
                             }}>
-                              <span>{fiiDiiAnalysis.combined_bias === 'BULLISH' ? '🟢' : fiiDiiAnalysis.combined_bias === 'BEARISH' ? '🔴' : '⚪'}</span>
+                              <span>{fiiDiiAnalysis.combined_bias.includes('BULLISH') ? '🟢' : fiiDiiAnalysis.combined_bias.includes('BEARISH') ? '🔴' : '⚪'}</span>
                               {fiiDiiAnalysis.combined_bias} ({Math.round(fiiDiiAnalysis.confidence * 100)}% Confidence)
                             </h2>
                           </div>
@@ -6542,52 +7627,70 @@
                       ))}
                     </div>
 
-                    {(briefSubTab === 'global' || briefSubTab === 'india') && (
-                        <div className="flex flex-col gap-6 text-slate-300 font-sans leading-relaxed tracking-wide">
+                    {(briefSubTab === 'global' || briefSubTab === 'india') && (() => {
+                      const intra = morningBrief?.intraday_plan || {};
+                      const swing = morningBrief?.swing_plan || {};
+                      return (
+                        <div className="grid grid-cols-1 lg:grid-cols-3 gap-4 text-slate-300 font-sans leading-relaxed tracking-wide">
                           
-                          {/* System Gateways Tracker (The One Trade Framework Directives) */}
-                          <div className="border border-slate-800 bg-[#0b0f19] rounded-lg overflow-hidden">
-                            <div className="border-b border-slate-800 bg-slate-900/50 px-4 py-2 flex items-center justify-between">
+                          {/* Row 1: Directives (full-width) */}
+                          <div className="col-span-full bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25">
+                            <div className="flex items-center justify-between border-b border-slate-800 pb-3 mb-4">
                               <span className="text-xs uppercase font-mono font-bold tracking-wider text-amber-500 flex items-center gap-1.5">
                                 <span>🛡️</span> The One Trade Framework Directives
                               </span>
                               <span className="text-[10px] uppercase font-mono text-slate-500 font-bold">SYSTEM GATEWAY STATUS: ONLINE</span>
                             </div>
-                            <div className="grid grid-cols-1 md:grid-cols-3 divide-y md:divide-y-0 md:divide-x divide-slate-800">
-                              <div className="p-4 bg-amber-950/5 border-l-4 border-l-amber-500">
-                                <span className="text-[11px] font-mono font-bold text-amber-500 block mb-1">🎯 ONE TRADE PER DAY LIMIT</span>
-                                <p className="text-xs text-slate-400">
+                            <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
+                              <div className="m-card" style={{ borderColor: 'rgba(245, 158, 11, 0.3)', background: '#141722' }}>
+                                <div className="m-label-row">
+                                  <span className="text-amber-500 font-mono font-bold">🎯 ONE TRADE PER DAY LIMIT</span>
+                                </div>
+                                <p className="text-xs text-slate-300 leading-relaxed mt-2 whitespace-pre-line break-words">
                                   The gate locks permanently after 1 position. No re-entries, averaging, or second chances.
                                 </p>
+                                <div className="m-sub-row mt-2">
+                                  <span className="text-[9px] uppercase font-mono text-amber-500/80 font-bold">Priority: Critical</span>
+                                </div>
                               </div>
-                              <div className="p-4 bg-emerald-950/5 border-l-4 border-l-emerald-500">
-                                <span className="text-[11px] font-mono font-bold text-emerald-500 block mb-1">📈 MINIMUM 90% SETUP QUALITY</span>
-                                <p className="text-xs text-slate-400">
+                              <div className="m-card" style={{ borderColor: 'rgba(16, 185, 129, 0.3)', background: '#141722' }}>
+                                <div className="m-label-row">
+                                  <span className="text-emerald-500 font-mono font-bold">📈 MINIMUM 90% SETUP QUALITY</span>
+                                </div>
+                                <p className="text-xs text-slate-300 leading-relaxed mt-2 whitespace-pre-line break-words">
                                   Only setups crossing the 90-point checklist threshold are authorized. STAND DOWN if score is lower.
                                 </p>
+                                <div className="m-sub-row mt-2">
+                                  <span className="text-[9px] uppercase font-mono text-emerald-500/80 font-bold">Priority: High</span>
+                                </div>
                               </div>
-                              <div className="p-4 bg-rose-950/5 border-l-4 border-l-rose-500">
-                                <span className="text-[11px] font-mono font-bold text-rose-500 block mb-1">🛑 ABSOLUTE SPOT COORDINATE INVALIDATION</span>
-                                <p className="text-xs text-slate-400">
+                              <div className="m-card" style={{ borderColor: 'rgba(239, 68, 68, 0.3)', background: '#141722' }}>
+                                <div className="m-label-row">
+                                  <span className="text-rose-500 font-mono font-bold">🛑 ABSOLUTE SPOT COORDINATE INVALIDATION</span>
+                                </div>
+                                <p className="text-xs text-slate-300 leading-relaxed mt-2 whitespace-pre-line break-words">
                                   If spot price breaches invalidation level or 5-min candle closes beyond SL, close position immediately.
                                 </p>
+                                <div className="m-sub-row mt-2">
+                                  <span className="text-[9px] uppercase font-mono text-rose-500/80 font-bold">Priority: Immediate</span>
+                                </div>
                               </div>
                             </div>
                           </div>
 
-                          {/* Macro Flow Grid (Middle Segment) */}
+                          {/* Row 2: Sub-tab specific markets data */}
                           {briefSubTab === 'global' && (
-                            <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
+                            <>
                               {/* Overnight US & Global Markets */}
-                              <div className="border border-slate-800 bg-[#0b0f19] rounded-lg overflow-hidden flex flex-col justify-between">
-                                <div className="border-b border-slate-800 bg-slate-900/50 px-4 py-3 flex items-center justify-between">
+                              <div className="lg:col-span-2 bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col justify-between gap-4">
+                                <div className="border-b border-slate-800 pb-3 flex items-center justify-between">
                                   <span className="text-xs uppercase font-mono font-bold text-slate-200">🌍 Overnight US & Global Markets</span>
                                   <button className="text-[10px] font-mono bg-slate-800 border border-slate-700 text-slate-300 hover:text-white px-2.5 py-1 rounded" onClick={fetchMorningBrief} disabled={loadingBrief}>
                                     {loadingBrief ? '...' : '↻ Refresh'}
                                   </button>
                                 </div>
-                                <div className="p-4 flex-1 flex flex-col gap-4">
-                                  <div className="grid grid-cols-5 gap-3">
+                                <div className="flex flex-col gap-4">
+                                  <div className="grid grid-cols-2 md:grid-cols-5 gap-3">
                                     {[
                                       { label: 'Dow Jones',  data: raw.dow,    suffix: '' },
                                       { label: 'S&P 500',    data: raw.sp500,  suffix: '' },
@@ -6596,15 +7699,17 @@
                                       { label: 'Gold',       data: raw.gold,   suffix: '/oz',  prefix: '$' },
                                     ].map(({ label, data, suffix, prefix = '' }) => {
                                       const pct = data?.change_pct ?? 0;
-                                      const color = pct > 0 ? 'text-[#00ff66]' : pct < 0 ? 'text-red-500' : 'text-slate-500';
+                                      const color = pct > 0 ? 'text-[#10b981]' : pct < 0 ? 'text-[#ef4444]' : 'text-slate-500';
                                       return (
-                                        <div key={label} className="bg-slate-900/30 border border-slate-800/80 rounded-md p-2.5 flex flex-col justify-between">
-                                          <span className="text-[9px] uppercase font-mono tracking-wider text-slate-500 block mb-1.5 truncate">{label}</span>
-                                          <div>
-                                            <span className={`${color} text-sm font-mono font-bold block mb-0.5`}>
-                                              {pct > 0 ? '+' : ''}{pct.toFixed(2)}%
-                                            </span>
-                                            <span className="text-[10px] font-mono text-slate-400 block truncate">
+                                        <div key={label} className="m-card">
+                                          <div className="m-label-row">
+                                            <span className="truncate">{label}</span>
+                                          </div>
+                                          <div className={`m-value font-mono ${color} text-sm`}>
+                                            {pct > 0 ? '+' : ''}{pct.toFixed(2)}%
+                                          </div>
+                                          <div className="m-sub-row text-slate-400">
+                                            <span className="truncate">
                                               {prefix}{data?.price?.toLocaleString('en-IN', { maximumFractionDigits: 1 }) || '—'}{suffix}
                                             </span>
                                           </div>
@@ -6616,23 +7721,23 @@
                                   {/* Global Sentiment */}
                                   {(() => {
                                     const bias = morningBrief.global_bias;
-                                    const borderColor = bias === 'BULLISH' ? 'border-emerald-900/50' : bias === 'BEARISH' ? 'border-rose-900/50' : 'border-amber-900/50';
+                                    const borderColor = bias === 'BULLISH' ? 'rgba(16, 185, 129, 0.25)' : bias === 'BEARISH' ? 'rgba(239, 68, 68, 0.25)' : 'rgba(245, 158, 11, 0.25)';
                                     const bgColor = bias === 'BULLISH' ? 'bg-emerald-950/10' : bias === 'BEARISH' ? 'bg-rose-950/10' : 'bg-amber-950/10';
-                                    const textColor = bias === 'BULLISH' ? 'text-[#00ff66]' : bias === 'BEARISH' ? 'text-rose-500' : 'text-amber-500';
+                                    const textColor = bias === 'BULLISH' ? 'text-[#10b981]' : bias === 'BEARISH' ? 'text-[#ef4444]' : 'text-amber-500';
                                     return (
-                                      <div className={`border ${borderColor} ${bgColor} rounded-md p-3.5`}>
+                                      <div className={`border rounded-md p-3.5 ${bgColor}`} style={{ borderColor }}>
                                         <span className={`text-xs uppercase font-mono font-bold ${textColor} block mb-1.5`}>
                                           🌍 Global Sentiment: {bias}
                                         </span>
-                                        <p className="text-xs text-slate-300 leading-relaxed font-sans">{morningBrief.global_interpretation}</p>
+                                        <p className="text-xs text-slate-300 leading-relaxed font-sans whitespace-pre-line break-words">{morningBrief.global_interpretation}</p>
                                       </div>
                                     );
                                   })()}
 
                                   {/* Catalyst */}
-                                  <div className="bg-slate-900/20 border border-slate-800/60 rounded-md p-3.5">
+                                  <div className="bg-[#0b0d12]/60 border border-[#23283b] rounded-md p-3.5">
                                     <span className="text-[10px] uppercase font-mono text-slate-500 block mb-1.5">⚡ Major Overnight Catalyst</span>
-                                    <p className="text-xs text-white italic font-serif leading-relaxed">
+                                    <p className="text-xs text-white italic font-serif leading-relaxed whitespace-pre-line break-words">
                                       "{morningBrief.overnight_catalyst}"
                                     </p>
                                   </div>
@@ -6640,21 +7745,21 @@
                               </div>
 
                               {/* Key Overnight News Headlines */}
-                              <div className="border border-slate-800 bg-[#0b0f19] rounded-lg overflow-hidden flex flex-col justify-between">
-                                <div className="border-b border-slate-800 bg-slate-900/50 px-4 py-3">
+                              <div className="lg:col-span-1 bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col justify-between gap-4">
+                                <div className="border-b border-slate-800 pb-3">
                                   <span className="text-xs uppercase font-mono font-bold text-slate-200">📰 Key Overnight News Headlines</span>
                                 </div>
-                                <div className="p-0 flex-1 divide-y divide-slate-800/60">
+                                <div className="flex-1 divide-y divide-slate-800/60 overflow-y-auto max-h-[360px] scrollbar-thin">
                                   {raw.news && raw.news.length > 0 ? (
                                     raw.news.slice(0, 4).map((item, idx) => (
-                                      <div key={idx} className="p-4 flex justify-between items-start gap-4 hover:bg-slate-900/10 transition-colors">
+                                      <div key={idx} className="py-3 first:pt-0 last:pb-0 flex justify-between items-start gap-4 hover:bg-slate-900/10 transition-colors">
                                         <div className="flex-1">
-                                          <h4 className="text-xs text-slate-200 font-sans font-medium mb-1.5 leading-snug">{item.title}</h4>
+                                          <h4 className="text-xs text-slate-200 font-sans font-medium mb-1 leading-snug break-words">{item.title}</h4>
                                           <span className="text-[10px] font-mono text-slate-500">{item.source} · {item.category}</span>
                                         </div>
                                         <span className={`text-[9px] font-mono font-bold uppercase border px-1.5 py-0.5 rounded flex-shrink-0 ${
-                                          item.sentiment === 'BULLISH' ? 'text-[#00ff66] border-emerald-900/50 bg-emerald-950/10' :
-                                          item.sentiment === 'BEARISH' ? 'text-rose-500 border-rose-900/50 bg-rose-950/10' :
+                                          item.sentiment === 'BULLISH' ? 'text-[#10b981] border-emerald-900/50 bg-emerald-950/10' :
+                                          item.sentiment === 'BEARISH' ? 'text-[#ef4444] border-rose-900/50 bg-rose-950/10' :
                                           'text-amber-500 border-amber-900/50 bg-amber-950/10'
                                         }`}>
                                           {item.sentiment}
@@ -6666,61 +7771,92 @@
                                   )}
                                 </div>
                               </div>
-                            </div>
+                            </>
                           )}
 
                           {briefSubTab === 'india' && (
-                            <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
+                            <>
                               {/* India Market Context & FII/DII Net Flow */}
-                              <div className="border border-slate-800 bg-[#0b0f19] rounded-lg overflow-hidden flex flex-col justify-between">
-                                <div className="border-b border-slate-800 bg-slate-900/50 px-4 py-3">
+                              <div className="lg:col-span-2 bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col justify-between gap-4">
+                                <div className="border-b border-slate-800 pb-3">
                                   <span className="text-xs uppercase font-mono font-bold text-slate-200">🇮🇳 Domestic Market Context & Institutional Flow</span>
                                 </div>
-                                <div className="p-4 flex-1 flex flex-col gap-4">
+                                <div className="flex flex-col gap-4">
                                   {/* Metrics Grid */}
-                                  <div className="grid grid-cols-4 gap-3">
-                                    {[
-                                      { label: 'Nifty Prev Close', value: `₹${(raw.nifty_current || 0).toLocaleString('en-IN')}`, color: 'text-amber-500' },
-                                      { 
-                                        label: 'SGX/Gift Nifty', 
-                                        value: `₹${(raw.sgx_nifty || 0).toLocaleString('en-IN')}`, 
-                                        color: (raw.sgx_gap || 0) >= 0 ? 'text-[#00ff66]' : 'text-rose-500',
-                                        sub: `${(raw.sgx_gap || 0) >= 0 ? '+' : ''}${raw.sgx_gap} pts`
-                                      },
-                                      { label: 'USD/INR', value: `₹${(raw.usdinr || 0).toFixed(2)}`, color: (raw.usdinr_change_pct || 0) <= 0 ? 'text-[#00ff66]' : 'text-rose-500' },
-                                      { label: 'India VIX', value: (raw.vix || 0).toFixed(2), color: (raw.vix_change_pct || 0) > 0 ? 'text-rose-500' : 'text-[#00ff66]' }
-                                    ].map(({ label, value, color, sub }) => (
-                                      <div key={label} className="bg-slate-900/30 border border-slate-800/80 rounded-md p-2.5 flex flex-col justify-between">
-                                        <span className="text-[9px] uppercase font-mono tracking-wider text-slate-500 block mb-1.5 truncate">{label}</span>
-                                        <div>
-                                          <span className={`${color} text-sm font-mono font-bold block`}>{value}</span>
-                                          {sub && <span className="text-[9px] font-mono text-slate-400 block mt-0.5 truncate">{sub}</span>}
-                                        </div>
+                                  <div className="grid grid-cols-2 md:grid-cols-3 gap-3">
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>Nifty Prev Close</span>
                                       </div>
-                                    ))}
-                                  </div>
-
-                                  {/* FII/DII Net Flow */}
-                                  <div className="grid grid-cols-2 gap-4">
-                                    <div className="bg-slate-900/20 border border-slate-800/60 rounded-md p-3.5">
-                                      <span className="text-[9px] uppercase font-mono tracking-wider text-slate-500 block mb-1">FII Net Flow (Prev Day)</span>
-                                      <span className={`text-base font-mono font-bold block mb-1 ${
-                                        (raw.fii_net || 0) >= 0 ? 'text-[#00ff66]' : 'text-rose-500'
-                                      }`}>
+                                      <div className="m-value font-mono text-amber-500">
+                                        ₹{(raw.nifty_current || 0).toLocaleString('en-IN')}
+                                      </div>
+                                      <div className="m-sub-row text-slate-500">
+                                        <span>Previous session close</span>
+                                      </div>
+                                    </div>
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>SGX/Gift Nifty</span>
+                                      </div>
+                                      <div className={`m-value font-mono ${(raw.sgx_gap || 0) >= 0 ? 'text-[#10b981]' : 'text-[#ef4444]'}`}>
+                                        ₹{(raw.sgx_nifty || 0).toLocaleString('en-IN')}
+                                      </div>
+                                      <div className="m-sub-row">
+                                        <span style={{ color: (raw.sgx_gap || 0) >= 0 ? '#10b981' : '#ef4444', fontWeight: 600 }}>
+                                          {(raw.sgx_gap || 0) >= 0 ? '▲ +' : '▼ '}{raw.sgx_gap || 0} pts gap
+                                        </span>
+                                      </div>
+                                    </div>
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>USD/INR</span>
+                                      </div>
+                                      <div className={`m-value font-mono ${(raw.usdinr_change_pct || 0) <= 0 ? 'text-[#10b981]' : 'text-[#ef4444]'}`}>
+                                        ₹{(raw.usdinr || 0).toFixed(2)}
+                                      </div>
+                                      <div className="m-sub-row">
+                                        <span style={{ color: (raw.usdinr_change_pct || 0) <= 0 ? '#10b981' : '#ef4444', fontWeight: 600 }}>
+                                          Currency variance
+                                        </span>
+                                      </div>
+                                    </div>
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>India VIX</span>
+                                      </div>
+                                      <div className={`m-value font-mono ${(raw.vix_change_pct || 0) > 0 ? 'text-[#ef4444]' : 'text-[#10b981]'}`}>
+                                        {(raw.vix || 0).toFixed(2)}
+                                      </div>
+                                      <div className="m-sub-row">
+                                        <span style={{ color: (raw.vix_change_pct || 0) > 0 ? '#ef4444' : '#10b981', fontWeight: 600 }}>
+                                          Volatility index
+                                        </span>
+                                      </div>
+                                    </div>
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>FII Net Flow (Prev Day)</span>
+                                      </div>
+                                      <div className={`m-value font-mono ${(raw.fii_net || 0) >= 0 ? 'text-[#10b981]' : 'text-[#ef4444]'}`}>
                                         {(raw.fii_net || 0) >= 0 ? '+' : ''}₹{(raw.fii_net || 0).toLocaleString('en-IN')} Cr
-                                      </span>
-                                      <span className="text-[10px] font-mono text-slate-400">
-                                        Signal: <strong className={(raw.fii_net || 0) > 0 ? 'text-[#00ff66]' : 'text-rose-500'}>{morningBrief.fii_signal}</strong>
-                                      </span>
+                                      </div>
+                                      <div className="m-sub-row">
+                                        <span style={{ color: (raw.fii_net || 0) >= 0 ? '#10b981' : '#ef4444', fontWeight: 600 }}>
+                                          Signal: {morningBrief.fii_signal}
+                                        </span>
+                                      </div>
                                     </div>
-                                    <div className="bg-slate-900/20 border border-slate-800/60 rounded-md p-3.5">
-                                      <span className="text-[9px] uppercase font-mono tracking-wider text-slate-500 block mb-1">DII Net Flow (Prev Day)</span>
-                                      <span className={`text-base font-mono font-bold block mb-1 ${
-                                        (raw.dii_net || 0) >= 0 ? 'text-[#00ff66]' : 'text-rose-500'
-                                      }`}>
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>DII Net Flow (Prev Day)</span>
+                                      </div>
+                                      <div className={`m-value font-mono ${(raw.dii_net || 0) >= 0 ? 'text-[#10b981]' : 'text-[#ef4444]'}`}>
                                         {(raw.dii_net || 0) >= 0 ? '+' : ''}₹{(raw.dii_net || 0).toLocaleString('en-IN')} Cr
-                                      </span>
-                                      <span className="text-[10px] font-mono text-slate-400">Domestic Absorption</span>
+                                      </div>
+                                      <div className="m-sub-row text-slate-500">
+                                        <span>Domestic Absorption</span>
+                                      </div>
                                     </div>
                                   </div>
 
@@ -6731,183 +7867,241 @@
                                       : 'border-rose-900/20 bg-rose-950/5'
                                   }`}>
                                     <span className="text-[10px] uppercase font-mono font-bold text-amber-500 block mb-1">🕵️‍♂️ Sherlock's FII Read</span>
-                                    <p className="text-xs text-slate-300 leading-relaxed font-sans">{morningBrief.fii_interpretation}</p>
+                                    <p className="text-xs text-slate-300 leading-relaxed font-sans whitespace-pre-line break-words">{morningBrief.fii_interpretation}</p>
                                   </div>
                                 </div>
                               </div>
 
                               {/* Nifty 50 Pre-Open Prediction */}
-                              <div className="border border-slate-800 bg-[#0b0f19] rounded-lg overflow-hidden flex flex-col justify-between">
-                                <div className="border-b border-slate-800 bg-slate-900/50 px-4 py-3">
+                              <div className="bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col justify-between gap-4">
+                                <div className="border-b border-slate-800 pb-3">
                                   <span className="text-xs uppercase font-mono font-bold text-slate-200">📈 Nifty 50 Pre-Open Prediction</span>
                                 </div>
-                                <div className="p-4 flex-1 flex flex-col justify-between gap-4">
+                                <div className="flex-1 flex flex-col gap-4">
                                   <div className="grid grid-cols-3 gap-3">
-                                    <div className="border border-slate-800/80 rounded-md p-3 flex flex-col justify-between bg-slate-900/10">
-                                      <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1 truncate">Direction</span>
-                                      <span className={`text-sm font-mono font-bold ${
-                                        pred.direction === 'GAP UP' ? 'text-[#00ff66]' :
-                                        pred.direction === 'GAP DOWN' ? 'text-rose-500' :
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span className="truncate">Direction</span>
+                                      </div>
+                                      <div className={`m-value font-mono text-xs ${
+                                        pred.direction === 'GAP UP' ? 'text-[#10b981]' :
+                                        pred.direction === 'GAP DOWN' ? 'text-[#ef4444]' :
                                         'text-amber-500'
-                                      }`}>{pred.direction || '—'}</span>
+                                      }`}>{pred.direction || '—'}</div>
+                                      <div className="m-sub-row text-slate-500">
+                                        <span>Predicted</span>
+                                      </div>
                                     </div>
-                                    <div className="border border-slate-800/80 rounded-md p-3 flex flex-col justify-between bg-slate-900/10">
-                                      <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1 truncate">Expected Gap</span>
-                                      <span className={`text-sm font-mono font-bold ${
-                                        (pred.expected_points || 0) >= 0 ? 'text-[#00ff66]' : 'text-rose-500'
-                                      }`}>{(pred.expected_points || 0) >= 0 ? '+' : ''}{pred.expected_points || 0} pts</span>
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span className="truncate">Gap Pts</span>
+                                      </div>
+                                      <div className={`m-value font-mono text-xs ${
+                                        (pred.expected_points || 0) >= 0 ? 'text-[#10b981]' : 'text-[#ef4444]'
+                                      }`}>{(pred.expected_points || 0) >= 0 ? '+' : ''}{pred.expected_points || 0}</div>
+                                      <div className="m-sub-row text-slate-500">
+                                        <span>Estimated</span>
+                                      </div>
                                     </div>
-                                    <div className="border border-slate-800/80 rounded-md p-3 flex flex-col justify-between bg-slate-900/10">
-                                      <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1 truncate">Probability</span>
-                                      <span className="text-sm font-mono font-bold text-amber-500">{pred.probability || '—'}</span>
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span className="truncate">Probability</span>
+                                      </div>
+                                      <div className="m-value font-mono text-xs text-amber-500">{pred.probability || '—'}</div>
+                                      <div className="m-sub-row text-slate-500">
+                                        <span>Confidence</span>
+                                      </div>
                                     </div>
                                   </div>
 
                                   <div className="grid grid-cols-2 gap-4">
-                                    <div className="bg-emerald-950/5 border border-emerald-900/25 rounded-md p-3.5">
-                                      <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1">Support Level</span>
-                                      <span className="text-base font-mono font-bold text-[#00ff66]">
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>Support Level</span>
+                                      </div>
+                                      <div className="m-value font-mono text-emerald-500 text-sm">
                                         ₹{(pred.support_level || 0).toLocaleString('en-IN')}
-                                      </span>
+                                      </div>
+                                      <div className="m-sub-row text-slate-500">
+                                        <span>Key Floor</span>
+                                      </div>
                                     </div>
-                                    <div className="bg-rose-950/5 border border-rose-900/25 rounded-md p-3.5">
-                                      <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1">Resistance Level</span>
-                                      <span className="text-base font-mono font-bold text-rose-500">
+                                    <div className="m-card">
+                                      <div className="m-label-row">
+                                        <span>Resistance Level</span>
+                                      </div>
+                                      <div className="m-value font-mono text-rose-500 text-sm">
                                         ₹{(pred.resistance_level || 0).toLocaleString('en-IN')}
-                                      </span>
+                                      </div>
+                                      <div className="m-sub-row text-slate-500">
+                                        <span>Key Ceiling</span>
+                                      </div>
                                     </div>
                                   </div>
                                   
-                                  {/* Empty space filler to align card heights */}
-                                  <div className="hidden lg:block h-6"></div>
+                                  <div className="bg-[#0b0d12]/50 border border-[#23283b] rounded p-3 text-xs text-slate-400 leading-relaxed font-sans flex-1 flex items-center italic">
+                                    "Watson, the calculations hint at a {(pred.direction || 'flat').toLowerCase()} open with a {(pred.probability || 'normal').toLowerCase()} probability of execution."
+                                  </div>
+                                </div>
+                              </div>
+                            </>
+                          )}
+
+                          {/* Row 3: Execution Blueprint & Verdict */}
+                          <div className="lg:col-span-2 bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col justify-between gap-4">
+                            <div className="flex items-center justify-between border-b border-slate-800 pb-3">
+                              <span className="text-xs uppercase font-mono font-bold tracking-wider text-slate-200 flex items-center gap-1.5">
+                                <span>⚡</span> Trading Plan Execution Matrix
+                              </span>
+                              <span className="text-[10px] uppercase font-mono text-slate-500 font-bold">EXECUTION BLUEPRINT</span>
+                            </div>
+                            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
+                              {/* Intraday Section */}
+                              <div className="m-card flex flex-col gap-3">
+                                <div className="flex justify-between items-center border-b border-slate-800/60 pb-2">
+                                  <span className="text-xs font-mono font-bold text-white uppercase">Intraday Plan ({intra.timeframe || '15-Min'})</span>
+                                  <span className="text-[10px] font-mono text-amber-500 font-bold border border-amber-900/50 bg-amber-950/10 px-2 py-0.5 rounded">
+                                    Best Window: {intra.best_trading_window}
+                                  </span>
+                                </div>
+                                <div className="bg-[#0b0d12]/60 border border-[#23283b] rounded p-3">
+                                  <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1">Execution Strategy</span>
+                                  <p className="text-xs text-slate-300 leading-relaxed font-sans whitespace-pre-line break-words">{intra.entry_strategy}</p>
+                                </div>
+                                <div className="grid grid-cols-2 gap-3 mt-auto">
+                                  <div className="bg-emerald-950/5 border border-emerald-900/20 rounded p-2.5">
+                                    <span className="text-[9px] uppercase font-mono text-slate-500 block mb-0.5">Target 1</span>
+                                    <span className="text-sm font-mono font-bold text-[#10b981]">₹{(intra.first_target || 0).toLocaleString('en-IN')}</span>
+                                  </div>
+                                  <div className="bg-rose-950/5 border border-rose-900/20 rounded p-2.5">
+                                    <span className="text-[9px] uppercase font-mono text-slate-500 block mb-0.5">Stop Loss</span>
+                                    <span className="text-sm font-mono font-bold text-rose-500">₹{(intra.stop_loss || 0).toLocaleString('en-IN')}</span>
+                                  </div>
+                                </div>
+                              </div>
+
+                              {/* Swing Section */}
+                              <div className="m-card flex flex-col gap-3 justify-between">
+                                <div className="flex justify-between items-center border-b border-slate-800/60 pb-2">
+                                  <span className="text-xs font-mono font-bold text-white uppercase">Swing Trading Plan (2-5 Days)</span>
+                                  <span className="text-[10px] font-mono text-indigo-400 font-bold border border-indigo-900/50 bg-indigo-950/10 px-2 py-0.5 rounded">
+                                    Duration: {swing.target_days || 3} Days
+                                  </span>
+                                </div>
+                                <div className="flex gap-2">
+                                  <span className={`text-[9px] font-mono font-bold uppercase border px-2 py-0.5 rounded ${
+                                    swing.hold_existing_longs ? 'text-[#10b981] border-emerald-900 bg-emerald-950/10' : 'text-rose-500 border-rose-900 bg-rose-950/10'
+                                  }`}>
+                                    Hold Longs: {swing.hold_existing_longs ? 'YES' : 'NO'}
+                                  </span>
+                                  <span className={`text-[9px] font-mono font-bold uppercase border px-2 py-0.5 rounded ${
+                                    swing.add_on_weakness ? 'text-[#10b981] border-emerald-900 bg-emerald-950/10' : 'text-rose-500 border-rose-900 bg-rose-950/10'
+                                  }`}>
+                                    Buy Dips: {swing.add_on_weakness ? 'YES' : 'NO'}
+                                  </span>
+                                </div>
+                                <div className="bg-[#0b0d12]/60 border border-[#23283b] rounded p-3 flex-1 flex flex-col justify-center min-h-[90px]">
+                                  <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1">Reasoning & Bias</span>
+                                  <p className="text-xs text-slate-300 leading-relaxed font-sans whitespace-pre-line break-words">{swing.reason}</p>
                                 </div>
                               </div>
                             </div>
-                          )}
-
-                          {/* Execution Blueprint Terminal */}
-                          {(() => {
-                            const intra = morningBrief.intraday_plan || {};
-                            const swing = morningBrief.swing_plan || {};
-                            return (
-                              <div className="border border-slate-800 bg-[#0b0f19] rounded-lg overflow-hidden">
-                                <div className="border-b border-slate-800 bg-slate-900/50 px-4 py-3">
-                                  <span className="text-xs uppercase font-mono font-bold text-slate-200">⚡ Execution Blueprint Terminal (Intraday & Swing)</span>
+                          </div>
+                          
+                          {/* Sherlock's Deduction Verdict */}
+                          <div className="lg:col-span-1 bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col gap-4">
+                            <div className="border-b border-slate-800 pb-3 flex items-center justify-between">
+                              <span className="text-xs uppercase font-mono font-bold text-amber-500 flex items-center gap-1.5">
+                                <span>🕵️‍♂️</span> Sherlock's Deduction Verdict
+                              </span>
+                              <span className="text-[10px] uppercase font-mono text-slate-500 font-bold">AI DECISION SUMMARY</span>
+                            </div>
+                            <div className="m-card flex-1 flex flex-col justify-center bg-[#141722] border border-[#23283b]">
+                              <p className="text-xs text-slate-300 italic font-serif leading-relaxed whitespace-pre-line break-words">
+                                "{morningBrief.sherlock_summary}"
+                              </p>
+                              {morningBrief.market_status && (
+                                <div className="mt-4 pt-3 border-t border-slate-800/60 flex items-center justify-between">
+                                  <span className="text-[10px] font-mono uppercase text-slate-500">BIAS STATUS</span>
+                                  <span className="text-[11px] font-mono font-bold text-amber-500 uppercase">
+                                    {morningBrief.market_status}
+                                  </span>
                                 </div>
-                                <div className="grid grid-cols-1 lg:grid-cols-2 divide-y lg:divide-y-0 lg:divide-x divide-slate-800">
-                                  {/* Intraday Section */}
-                                  <div className="p-4 flex flex-col gap-4">
-                                    <div className="flex justify-between items-center">
-                                      <span className="text-xs font-mono font-bold text-white uppercase">Intraday Plan ({intra.timeframe || '15-Min'})</span>
-                                      <span className="text-[10px] font-mono text-amber-500 font-bold border border-amber-900/50 bg-amber-950/10 px-2 py-0.5 rounded">
-                                        Best Window: {intra.best_trading_window}
-                                      </span>
+                              )}
+                            </div>
+                          </div>
+
+                          {/* Row 4: Sectors in Focus Today (col-span-1) */}
+                          <div className="lg:col-span-1 bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col justify-between gap-3">
+                            <div className="border-b border-slate-800 pb-3">
+                              <span className="text-xs uppercase font-mono font-bold text-slate-200 flex items-center gap-1.5">
+                                <span>🔦</span> Sectors in Focus Today
+                              </span>
+                            </div>
+                            <div className="flex-1 flex flex-col justify-between gap-3">
+                              <div className="flex gap-2 flex-wrap">
+                                {(morningBrief.sectors_in_focus || []).map(s => (
+                                  <span key={s} className="bg-amber-950/10 border border-amber-900/50 text-amber-500 rounded px-2.5 py-1 text-[10px] font-mono font-bold">
+                                    {s}
+                                  </span>
+                                ))}
+                                {(morningBrief.sectors_in_focus || []).length === 0 && (
+                                  <span className="text-xs text-slate-500 italic">No sectors listed.</span>
+                                )}
+                              </div>
+                              <p className="text-xs text-slate-300 font-sans leading-relaxed whitespace-pre-line break-words mt-2">
+                                {morningBrief.sector_rationale}
+                              </p>
+                            </div>
+                          </div>
+
+                          {/* Risks & Events Calendar (col-span-2) */}
+                          <div className="lg:col-span-2 bg-slate-900/50 border border-slate-800 rounded-lg p-4 shadow-lg shadow-black/25 flex flex-col gap-3">
+                            <div className="border-b border-slate-800 pb-3 flex items-center justify-between">
+                              <span className="text-xs uppercase font-mono font-bold text-slate-200 flex items-center gap-1.5">
+                                <span>⚠️</span> Risks & Events Calendar
+                              </span>
+                              <span className="text-[10px] uppercase font-mono text-slate-500 font-bold">RISK RADAR</span>
+                            </div>
+                            <div className="grid grid-cols-1 md:grid-cols-2 gap-4 flex-1">
+                              <div className="m-card bg-[#141722] border border-rose-900/30 rounded p-3 flex flex-col">
+                                <div className="m-label-row border-b border-slate-800/60 pb-1.5 mb-2">
+                                  <span className="text-rose-500 font-mono font-bold">WARNINGS & SHOCKS</span>
+                                </div>
+                                <div className="flex-1 overflow-y-auto space-y-2 scrollbar-thin max-h-[140px]">
+                                  {(morningBrief.red_flags || []).map((flag, i) => (
+                                    <div key={i} className="text-xs text-slate-300 leading-relaxed pl-3 relative">
+                                      <span className="absolute left-0 text-rose-500">•</span>
+                                      {flag}
                                     </div>
-                                    <div className="bg-slate-950/30 border border-slate-800/85 rounded p-3">
-                                      <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1">Execution Strategy</span>
-                                      <p className="text-xs text-slate-300 leading-relaxed font-sans">{intra.entry_strategy}</p>
-                                    </div>
-                                    <div className="grid grid-cols-2 gap-3">
-                                      <div className="bg-emerald-950/5 border border-emerald-900/20 rounded p-2.5">
-                                        <span className="text-[9px] uppercase font-mono text-slate-500 block mb-0.5">Target 1</span>
-                                        <span className="text-sm font-mono font-bold text-[#00ff66]">₹{(intra.first_target || 0).toLocaleString('en-IN')}</span>
-                                      </div>
-                                      <div className="bg-rose-950/5 border border-rose-900/20 rounded p-2.5">
-                                        <span className="text-[9px] uppercase font-mono text-slate-500 block mb-0.5">Stop Loss</span>
-                                        <span className="text-sm font-mono font-bold text-rose-500">₹{(intra.stop_loss || 0).toLocaleString('en-IN')}</span>
-                                      </div>
-                                    </div>
-                                  </div>
-
-                                  {/* Swing Section */}
-                                  <div className="p-4 flex flex-col gap-4 justify-between">
-                                    <div className="flex justify-between items-center">
-                                      <span className="text-xs font-mono font-bold text-white uppercase">Swing Trading Plan (2-5 Days)</span>
-                                      <span className="text-[10px] font-mono text-indigo-400 font-bold border border-indigo-900/50 bg-indigo-950/10 px-2 py-0.5 rounded">
-                                        Duration: {swing.target_days || 3} Days
-                                      </span>
-                                    </div>
-                                    <div className="flex gap-2">
-                                      <span className={`text-[9px] font-mono font-bold uppercase border px-2 py-0.5 rounded ${
-                                        swing.hold_existing_longs ? 'text-[#00ff66] border-emerald-900 bg-emerald-950/10' : 'text-rose-500 border-rose-900 bg-rose-950/10'
-                                      }`}>
-                                        Hold Longs: {swing.hold_existing_longs ? 'YES' : 'NO'}
-                                      </span>
-                                      <span className={`text-[9px] font-mono font-bold uppercase border px-2 py-0.5 rounded ${
-                                        swing.add_on_weakness ? 'text-[#00ff66] border-emerald-900 bg-emerald-950/10' : 'text-rose-500 border-rose-900 bg-rose-950/10'
-                                      }`}>
-                                        Buy Dips: {swing.add_on_weakness ? 'YES' : 'NO'}
-                                      </span>
-                                    </div>
-                                    <div className="bg-slate-950/30 border border-slate-800/85 rounded p-3 flex-1 flex flex-col justify-center">
-                                      <span className="text-[9px] uppercase font-mono text-slate-500 block mb-1">Reasoning & Bias</span>
-                                      <p className="text-xs text-slate-300 leading-relaxed font-sans">{swing.reason}</p>
-                                    </div>
-                                  </div>
+                                  ))}
+                                  {(morningBrief.red_flags || []).length === 0 && (
+                                    <div className="text-xs text-slate-500 italic">No critical warnings flagged.</div>
+                                  )}
                                 </div>
                               </div>
-                            );
-                          })()}
-
-                          {/* Bottom Grid: Sectors, Deduction, Events */}
-                          <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
-                            
-                            {/* Sectors in Focus */}
-                            <div className="border border-slate-800 bg-[#0b0f19] rounded-lg p-4 flex flex-col justify-between gap-3">
-                              <div>
-                                <span className="text-xs uppercase font-mono font-bold text-slate-200 block mb-3">🔦 Sectors in Focus Today</span>
-                                <div className="flex gap-2 flex-wrap mb-2">
-                                  {(morningBrief.sectors_in_focus || []).map(s => (
-                                    <span key={s} className="bg-amber-950/10 border border-amber-900/50 text-amber-500 rounded px-2 py-0.5 text-[10px] font-mono font-bold">
-                                      {s}
-                                    </span>
+                              
+                              <div className="m-card bg-[#141722] border border-amber-900/30 rounded p-3 flex flex-col">
+                                <div className="m-label-row border-b border-slate-800/60 pb-1.5 mb-2">
+                                  <span className="text-amber-500 font-mono font-bold">CRITICAL EVENTS TODAY</span>
+                                </div>
+                                <div className="flex-1 overflow-y-auto space-y-2 scrollbar-thin max-h-[140px]">
+                                  {(morningBrief.key_events_today || []).map((ev, i) => (
+                                    <div key={i} className="text-xs text-slate-300 leading-relaxed pl-3 relative">
+                                      <span className="absolute left-0 text-amber-500">•</span>
+                                      {ev}
+                                    </div>
                                   ))}
-                                </div>
-                              </div>
-                              <p className="text-xs text-slate-400 font-sans leading-relaxed">{morningBrief.sector_rationale}</p>
-                            </div>
-
-                            {/* Sherlock's Deduction Verdict */}
-                            <div className="border border-slate-800 bg-[#0b0f19] rounded-lg p-4 flex flex-col justify-between gap-3">
-                              <span className="text-xs uppercase font-mono font-bold text-amber-500 block mb-1">🕵️‍♂️ Sherlock's Deduction Verdict</span>
-                              <p className="text-xs text-white italic font-serif leading-relaxed flex-1 flex items-center">
-                                "{morningBrief.sherlock_summary}"
-                              </p>
-                            </div>
-
-                            {/* Risk & Events Terminal */}
-                            <div className="border border-slate-800 bg-[#0b0f19] rounded-lg p-4 flex flex-col gap-3">
-                              <div className="grid grid-cols-2 gap-3 h-full">
-                                <div className="bg-rose-950/5 border border-rose-900/10 rounded p-2.5 flex flex-col justify-between">
-                                  <span className="text-[9px] uppercase font-mono font-bold text-rose-500 block mb-2">⚠️ Warnings</span>
-                                  <div className="flex-1 overflow-y-auto space-y-1.5 scrollbar-thin">
-                                    {(morningBrief.red_flags || []).map((flag, i) => (
-                                      <div key={i} className="text-[10px] text-slate-300 leading-tight">
-                                        • {flag}
-                                      </div>
-                                    ))}
-                                  </div>
-                                </div>
-                                <div className="bg-slate-900/20 border border-slate-800/80 rounded p-2.5 flex flex-col justify-between">
-                                  <span className="text-[9px] uppercase font-mono font-bold text-amber-500 block mb-2">🕐 Events Calendar</span>
-                                  <div className="flex-1 overflow-y-auto space-y-1.5 scrollbar-thin">
-                                    {(morningBrief.key_events_today || []).map((ev, i) => (
-                                      <div key={i} className="text-[10px] text-slate-300 leading-tight">
-                                        • {ev}
-                                      </div>
-                                    ))}
-                                    {(morningBrief.key_events_today || []).length === 0 && (
-                                      <div className="text-[10px] text-slate-500 italic">No events.</div>
-                                    )}
-                                  </div>
+                                  {(morningBrief.key_events_today || []).length === 0 && (
+                                    <div className="text-xs text-slate-500 italic">No events listed.</div>
+                                  )}
                                 </div>
                               </div>
                             </div>
-
                           </div>
 
                           {/* Next Brief Update strip */}
-                          <div className="border border-slate-800 bg-[#0b0f19] rounded-lg px-4 py-2.5 flex justify-between items-center">
+                          <div className="col-span-full bg-slate-900/50 border border-slate-800 rounded-lg px-4 py-2.5 flex justify-between items-center shadow-lg shadow-black/20">
                             <span className="text-[10px] font-mono uppercase tracking-wider text-slate-500">Next Scheduled System Brief Update</span>
                             <span className="text-xs font-mono font-bold text-slate-300 flex items-center gap-1.5">
                               <span>⏰</span> {morningBrief.next_update}
@@ -6915,7 +8109,8 @@
                           </div>
 
                         </div>
-                      )}
+                      );
+                    })()}
 
                       {briefSubTab === 'verdict' && (
                         <MarketVerdictTab data={morningBrief} />
@@ -6933,309 +8128,13 @@
 
         {/* Tab 9: Sherlock Bot with NSE Live Data */}
         {activeTab === 'sherlockBot' && (
-          <div>
-            {/* NSE Live Status Banner */}
-            <div style={{
-              background: nseData.isLive
-                ? 'linear-gradient(90deg, rgba(0,201,167,0.08) 0%, rgba(0,201,167,0.02) 100%)'
-                : 'linear-gradient(90deg, rgba(255,170,0,0.08) 0%, rgba(255,170,0,0.02) 100%)',
-              border: `1px solid ${nseData.isLive ? 'rgba(0,201,167,0.3)' : 'rgba(255,170,0,0.3)'}`,
-              borderRadius: 8, padding: '10px 18px', marginBottom: 16,
-              display: 'flex', alignItems: 'center', gap: 12, flexWrap: 'wrap'
-            }}>
-              <span style={{ fontSize: 20 }}>{nseData.isLive ? '📡' : '⚠️'}</span>
-              <div style={{ flex: 1 }}>
-                <div style={{ fontWeight: 700, fontSize: 13, color: nseData.isLive ? '#00c9a7' : '#ffaa00', marginBottom: 2 }}>
-                  {nseData.isLive ? 'NSE Live Feed Connected' : 'NSE Proxy Fallback Mode'}
-                </div>
-                <div style={{ fontSize: 11, color: 'var(--text-muted)' }}>
-                  {nseData.isLive
-                    ? `Real-time data from NSE India · Market: ${nseData.marketStatus?.status} — ${nseData.marketStatus?.reason}`
-                    : `${nseData.error || 'NSE proxy server (port 3001) not running. Start it with: npm run proxy'}`}
-                </div>
-              </div>
-              {!nseData.isLive && (
-                <code style={{ fontSize: 11, background: 'rgba(0,0,0,0.3)', padding: '4px 10px', borderRadius: 4, color: '#c9a84c', border: '1px solid rgba(201,168,76,0.2)' }}>
-                  npm run proxy
-                </code>
-              )}
-            </div>
-
-            {/* NSE Live Indices Strip */}
-            {nseData.indices && nseData.indices.length > 0 && (
-              <div style={{
-                display: 'flex', gap: 10, marginBottom: 16, flexWrap: 'wrap'
-              }}>
-                {nseData.indices.map((idx, i) => (
-                  <div key={i} style={{
-                    flex: '1 1 140px', background: 'var(--bg-card)', border: '1px solid var(--border)',
-                    borderRadius: 6, padding: '10px 14px',
-                    borderTop: `2px solid ${idx.percentChange >= 0 ? 'var(--green)' : 'var(--red)'}`
-                  }}>
-                    <div style={{ fontSize: 10, color: 'var(--text-muted)', textTransform: 'uppercase', marginBottom: 3, letterSpacing: '0.5px' }}>
-                      {idx.name}
-                    </div>
-                    <div style={{
-                      fontSize: 16, fontWeight: 700, fontFamily: 'IBM Plex Mono, monospace',
-                      color: idx.percentChange >= 0 ? 'var(--green)' : 'var(--red)'
-                    }}>
-                      {(idx.last || 0).toLocaleString('en-IN', { maximumFractionDigits: 2 })}
-                    </div>
-                    <div style={{ fontSize: 11, color: idx.percentChange >= 0 ? 'var(--green)' : 'var(--red)', marginTop: 2 }}>
-                      {idx.percentChange >= 0 ? '+' : ''}{(idx.percentChange || 0).toFixed(2)}%
-                      {' '}({idx.variation >= 0 ? '+' : ''}{(idx.variation || 0).toFixed(2)})
-                    </div>
-                  </div>
-                ))}
-              </div>
-            )}
-
-            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 16 }}>
-              {/* Real-time Quote Card */}
-              <div className="card" style={{ marginBottom: 0 }}>
-                <div className="card-header">
-                  <div className="card-title">
-                    📊 Live Quote — {nseSymbol}
-                    {nseData.isLive && <span style={{ marginLeft: 8, fontSize: 10, color: '#00c9a7', fontWeight: 700 }}>● LIVE</span>}
-                  </div>
-                </div>
-                <div className="card-body">
-                  {nseData.quote ? (
-                    <div>
-                      <div style={{ display: 'flex', alignItems: 'baseline', gap: 12, marginBottom: 14 }}>
-                        <span style={{
-                          fontSize: 32, fontWeight: 700, fontFamily: 'IBM Plex Mono, monospace',
-                          color: (nseData.quote.pChange || 0) >= 0 ? 'var(--green)' : 'var(--red)'
-                        }}>
-                          ₹{(nseData.quote.lastPrice || 0).toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
-                        </span>
-                        <span style={{
-                          fontSize: 16, fontWeight: 600,
-                          color: (nseData.quote.pChange || 0) >= 0 ? 'var(--green)' : 'var(--red)'
-                        }}>
-                          {(nseData.quote.pChange || 0) >= 0 ? '+' : ''}{(nseData.quote.pChange || 0).toFixed(2)}%
-                        </span>
-                      </div>
-                      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 10 }}>
-                        {[
-                          { label: 'Open', val: nseData.quote.open },
-                          { label: 'Day High', val: nseData.quote.dayHigh },
-                          { label: 'Day Low', val: nseData.quote.dayLow },
-                          { label: 'Prev Close', val: nseData.quote.previousClose },
-                          { label: 'PCR (Options)', val: nseData.pcr ? nseData.pcr.toFixed(2) : '—' },
-                          { label: 'Max Pain', val: nseData.maxPain ? nseData.maxPain.toLocaleString('en-IN') : '—' },
-                        ].map(({ label, val }) => (
-                          <div key={label} style={{ background: 'var(--bg-elevated)', borderRadius: 4, padding: '8px 10px' }}>
-                            <div style={{ fontSize: 10, color: 'var(--text-muted)', marginBottom: 3, textTransform: 'uppercase' }}>{label}</div>
-                            <div style={{ fontSize: 13, fontWeight: 600, fontFamily: 'IBM Plex Mono, monospace', color: 'var(--text-primary)' }}>
-                              {typeof val === 'number' ? val.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) : val}
-                            </div>
-                          </div>
-                        ))}
-                      </div>
-                      <div style={{ marginTop: 10, fontSize: 10, color: 'var(--text-muted)', textAlign: 'right' }}>
-                        Source: {nseData.quote.source?.toUpperCase() || 'NSE'} · {new Date().toLocaleTimeString('en-IN')}
-                      </div>
-                    </div>
-                  ) : (
-                    <div style={{ textAlign: 'center', padding: '30px 20px', color: 'var(--text-muted)' }}>
-                      <div style={{ fontSize: 28, marginBottom: 10 }}>📡</div>
-                      <p>Connecting to NSE feed…</p>
-                      <code style={{ fontSize: 11, color: '#c9a84c' }}>npm run proxy</code>
-                    </div>
-                  )}
-                </div>
-              </div>
-
-              {/* Options OI Heatmap mini */}
-              <div className="card" style={{ marginBottom: 0 }}>
-                <div className="card-header">
-                  <div className="card-title">🔥 Options OI — {nseSymbol}</div>
-                </div>
-                <div className="card-body" style={{ padding: 0 }}>
-                  {nseData.optionChain?.chain && nseData.optionChain.chain.length > 0 ? (
-                    <div style={{ maxHeight: 280, overflowY: 'auto' }}>
-                      <table className="data-table" style={{ fontSize: 11 }}>
-                        <thead>
-                          <tr>
-                            <th style={{ color: '#4da6ff' }}>CE OI</th>
-                            <th>Strike</th>
-                            <th style={{ color: '#ff6b6b' }}>PE OI</th>
-                          </tr>
-                        </thead>
-                        <tbody>
-                          {nseData.optionChain.chain
-                            .filter(r => Math.abs(r.strike - (nseData.quote?.lastPrice || indicators.spot_price)) < 1000)
-                            .slice(0, 15)
-                            .map((row) => {
-                              const isAtm = Math.abs(row.strike - (nseData.quote?.lastPrice || indicators.spot_price)) < 100;
-                              const maxOI = Math.max(...nseData.optionChain.chain.map(r => Math.max(r.call_oi, r.put_oi)));
-                              const ceWidth = maxOI > 0 ? (row.call_oi / maxOI) * 80 : 0;
-                              const peWidth = maxOI > 0 ? (row.put_oi / maxOI) * 80 : 0;
-                              return (
-                                <tr key={row.strike} style={{
-                                  background: isAtm ? 'rgba(201,168,76,0.08)' : 'transparent',
-                                  borderLeft: isAtm ? '2px solid var(--gold)' : 'none'
-                                }}>
-                                  <td>
-                                    <div style={{ display: 'flex', alignItems: 'center', gap: 4, justifyContent: 'flex-end' }}>
-                                      <span>{(row.call_oi / 1000).toFixed(0)}K</span>
-                                      <div style={{ width: ceWidth, height: 4, background: 'rgba(77,166,255,0.5)', borderRadius: 2 }} />
-                                    </div>
-                                  </td>
-                                  <td style={{ textAlign: 'center', fontWeight: isAtm ? 700 : 400, color: isAtm ? 'var(--gold)' : 'var(--text-primary)' }}>
-                                    {row.strike.toLocaleString('en-IN')}
-                                    {isAtm && <span style={{ fontSize: 9, marginLeft: 4, color: 'var(--gold)' }}>ATM</span>}
-                                  </td>
-                                  <td>
-                                    <div style={{ display: 'flex', alignItems: 'center', gap: 4 }}>
-                                      <div style={{ width: peWidth, height: 4, background: 'rgba(255,107,107,0.5)', borderRadius: 2 }} />
-                                      <span>{(row.put_oi / 1000).toFixed(0)}K</span>
-                                    </div>
-                                  </td>
-                                </tr>
-                              );
-                          })}
-                        </tbody>
-                      </table>
-                    </div>
-                  ) : (
-                    <div style={{ textAlign: 'center', padding: '30px', color: 'var(--text-muted)' }}>
-                      {nseData.isLive ? 'Loading option chain…' : 'Start NSE proxy to see options data'}
-                    </div>
-                  )}
-                  {nseData.optionChain && (
-                    <div style={{ padding: '8px 12px', borderTop: '1px solid var(--border)', display: 'flex', gap: 16, fontSize: 11 }}>
-                      <span>PCR: <strong style={{ color: nseData.pcr > 1.2 ? 'var(--green)' : nseData.pcr < 0.8 ? 'var(--red)' : 'var(--gold)' }}>{nseData.pcr?.toFixed(2) || '—'}</strong></span>
-                      <span>Max Pain: <strong style={{ color: 'var(--gold)' }}>₹{nseData.maxPain?.toLocaleString('en-IN') || '—'}</strong></span>
-                      <span>Expiry: <strong style={{ color: 'var(--text-secondary)' }}>{nseData.optionChain.expiry || '—'}</strong></span>
-                    </div>
-                  )}
-                </div>
-              </div>
-            </div>
-
-            {/* Sherlock Holmes Chatbot */}
-            <div className="card" style={{ marginBottom: 0 }}>
-              <div className="card-header">
-                <div className="card-title">💬 Consult Sherlock Holmes</div>
-                <div style={{ fontSize: 12, color: 'var(--text-muted)' }}>
-                  Powered by {nseData.isLive ? 'live NSE data + ' : ''}rule-based AI engine
-                </div>
-              </div>
-              <div className="card-body" style={{ padding: 0 }}>
-                {/* Chat Messages */}
-                <div style={{ height: 360, overflowY: 'auto', padding: '16px 20px', display: 'flex', flexDirection: 'column', gap: 12 }}>
-                  {chatMessages.map((msg, idx) => (
-                    <div key={idx} style={{
-                      display: 'flex',
-                      flexDirection: msg.sender === 'user' ? 'row-reverse' : 'row',
-                      gap: 10, alignItems: 'flex-start'
-                    }}>
-                      <div style={{
-                        width: 32, height: 32, borderRadius: '50%', flexShrink: 0,
-                        background: msg.sender === 'user' ? 'rgba(77,166,255,0.2)' : 'rgba(201,168,76,0.2)',
-                        border: `1px solid ${msg.sender === 'user' ? 'rgba(77,166,255,0.4)' : 'rgba(201,168,76,0.4)'}`,
-                        display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 16
-                      }}>
-                        {msg.sender === 'user' ? '🧑' : '🕵️'}
-                      </div>
-                      <div style={{ maxWidth: '75%' }}>
-                        {msg.sender === 'sherlock' ? (
-                          <SherlockReply content={msg.text} isStreaming={false} />
-                        ) : (
-                          <div className="user-reply-bubble">
-                            {msg.text}
-                          </div>
-                        )}
-                      </div>
-                    </div>
-                  ))}
-                  {loadingChat && streamedReply && (
-                    <div style={{ display: 'flex', gap: 10, alignItems: 'flex-start' }}>
-                      <div style={{ width: 32, height: 32, borderRadius: '50%', background: 'rgba(201,168,76,0.2)', border: '1px solid rgba(201,168,76,0.4)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 16 }}>🕵️</div>
-                      <div style={{ maxWidth: '75%', flex: 1 }}>
-                        <SherlockReply content={streamedReply} isStreaming={true} />
-                      </div>
-                    </div>
-                  )}
-                  {loadingChat && !streamedReply && (
-                    <div style={{ display: 'flex', gap: 10, alignItems: 'center' }}>
-                      <div style={{ width: 32, height: 32, borderRadius: '50%', background: 'rgba(201,168,76,0.2)', border: '1px solid rgba(201,168,76,0.4)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 16 }}>🕵️</div>
-                      <div style={{ display: 'flex', gap: 5, alignItems: 'center', padding: '10px 14px', background: 'rgba(201,168,76,0.06)', border: '1px solid rgba(201,168,76,0.15)', borderRadius: '4px 12px 12px 12px' }}>
-                        {[0,1,2].map(i => (
-                          <div key={i} style={{
-                            width: 7, height: 7, borderRadius: '50%', background: 'var(--gold)',
-                            animation: `bounce 1.2s infinite ${i * 0.2}s`,
-                          }} />
-                        ))}
-                      </div>
-                    </div>
-                  )}
-                  <div ref={chatEndRef} />
-                </div>
-
-                {/* Quick Action Chips */}
-                <div style={{ padding: '8px 16px', borderTop: '1px solid var(--border)', display: 'flex', gap: 8, flexWrap: 'wrap' }}>
-                  {[
-                    {
-                      label: `📊 Analyze ${nseSymbol}`,
-                      msg: `Analyze ${nseSymbol} completely. Give signal, exact entry ₹price, SL ₹price, 3 targets with ₹prices, confidence breakdown, and position sizing for ₹10L capital.`
-                    },
-                    {
-                      label: '⚡ Generate Strategy', 
-                      msg: `Generate today's best intraday strategy for ${nseSymbol}. Include exact entry, SL, targets in ₹, lot size, and time validity.`
-                    },
-                    {
-                      label: '📈 PCR Analysis',
-                      msg: `Analyze current PCR of ${nseData.pcr ? nseData.pcr.toFixed(2) : '1.0'} for ${nseSymbol}. What does it mean? Is market bullish or bearish? Which strikes have highest OI? What is max pain?`
-                    },
-                    {
-                      label: '📐 Explain EMA',
-                      msg: `Explain what EMA 9 = ₹${(nseData.quote?.lastPrice ? nseData.quote.lastPrice * 1.002 : 23790 * 1.002).toFixed(2)} and EMA 21 = ₹${(nseData.quote?.lastPrice ? nseData.quote.lastPrice * 0.999 : 23790 * 0.999).toFixed(2)} means for ${nseSymbol} right now. Simple language. What should I do?`
-                    },
-                    {
-                      label: '🎯 Entry/Exit Levels',
-                      msg: `Give me exact entry and exit levels for ${nseSymbol} today. Spot is ₹${nseData.quote?.lastPrice || 23790}. VWAP is ₹${(nseData.quote?.lastPrice ? nseData.quote.lastPrice * 0.9995 : 23790).toFixed(2)}. Give 3 scenarios: bullish breakout, bearish breakdown, sideways.`
-                    }
-                  ].map(({ label, msg }) => (
-                    <button key={label} onClick={(e) => handleSendChat(e, msg)}
-                      style={{
-                        background: 'var(--bg-elevated)', border: '1px solid var(--border)',
-                        borderRadius: 16, padding: '4px 12px', color: 'var(--text-secondary)',
-                        fontSize: 12, cursor: 'pointer', transition: 'all 0.15s',
-                        whiteSpace: 'nowrap'
-                      }}
-                      onMouseEnter={e => { e.target.style.borderColor = 'var(--gold)'; e.target.style.color = 'var(--gold)'; }}
-                      onMouseLeave={e => { e.target.style.borderColor = 'var(--border)'; e.target.style.color = 'var(--text-secondary)'; }}
-                    >
-                      {label}
-                    </button>
-                  ))}
-                </div>
-
-                {/* Chat Input */}
-                <form onSubmit={handleSendChat} style={{
-                  display: 'flex', gap: 10, padding: '12px 16px',
-                  borderTop: '1px solid var(--border)'
-                }}>
-                  <input
-                    type="text"
-                    value={chatInput}
-                    onChange={e => setChatInput(e.target.value)}
-                    placeholder={`Ask Sherlock about ${nseSymbol}, options, strategy…`}
-                    style={{ flex: 1, padding: '10px 14px', fontSize: 13 }}
-                    disabled={loadingChat}
-                  />
-                  <button type="submit" className="btn btn-gold" disabled={loadingChat || !chatInput.trim()}
-                    style={{ minWidth: 100, padding: '10px 18px' }}>
-                    {loadingChat ? '…' : 'Ask 🕵️'}
-                  </button>
-                </form>
-              </div>
-            </div>
-          </div>
+          <HolmesChat
+            nseData={nseData}
+            nseSymbol={nseSymbol}
+            indicators={indicators}
+          />
         )}
+
 
         {/* Tab 8: Backtester */}
         {activeTab === 'backtester' && (() => {
